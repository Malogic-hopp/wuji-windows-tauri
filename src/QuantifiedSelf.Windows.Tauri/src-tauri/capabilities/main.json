{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "WUJI dev-only 主窗口；不授予 shell、文件、网络、SQL 或通用进程能力。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
