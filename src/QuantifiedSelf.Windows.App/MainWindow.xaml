<Window x:Class="QuantifiedSelf.Windows.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="QuantifiedSelf Windows"
        Width="1280"
        Height="860"
        MinWidth="1100"
        MinHeight="760"
        Background="#F3F6FA">
    <Window.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#0F766E" />
        <SolidColorBrush x:Key="AccentDarkBrush" Color="#115E59" />
        <SolidColorBrush x:Key="SurfaceBrush" Color="White" />
        <SolidColorBrush x:Key="SurfaceAltBrush" Color="#F8FBFD" />
        <SolidColorBrush x:Key="TextBrush" Color="#102A43" />
        <SolidColorBrush x:Key="MutedTextBrush" Color="#52606D" />

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="MinWidth" Value="100" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#D9E2EC" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        </Style>

        <Style x:Key="CardBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
            <Setter Property="CornerRadius" Value="18" />
            <Setter Property="Padding" Value="18" />
            <Setter Property="Margin" Value="0,0,0,16" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="24" Color="#33000000" ShadowDepth="2" Opacity="0.12" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#F6FAFC" Offset="0" />
                <GradientStop Color="#EBF2F7" Offset="0.6" />
                <GradientStop Color="#E7EEF5" Offset="1" />
            </LinearGradientBrush>
        </Grid.Background>

        <Grid Margin="28">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Style="{StaticResource CardBorderStyle}" Grid.Row="0" Background="#0F172A">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="QuantifiedSelf Windows" FontSize="28" FontWeight="Bold" Foreground="White" />
                        <TextBlock Text="Local agent control center" Margin="0,6,0,0" Foreground="#CBD5E1" FontSize="14" />
                    </StackPanel>

                    <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Border Background="#132238" CornerRadius="999" Padding="14,6" HorizontalAlignment="Right">
                            <TextBlock Text="{Binding AgentStatusText}" Foreground="White" FontWeight="SemiBold" />
                        </Border>
                        <TextBlock Text="{Binding StatusMessage}" Margin="0,10,0,0" Foreground="#CBD5E1" HorizontalAlignment="Right" />
                    </StackPanel>
                </Grid>
            </Border>

            <Border Style="{StaticResource CardBorderStyle}" Grid.Row="1">
                <DockPanel LastChildFill="True">
                    <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                        <Button Content="Start Agent" Command="{Binding StartAgentCommand}" />
                        <Button Content="Stop Agent" Command="{Binding StopAgentCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                        <Button Content="Pause" Command="{Binding PauseCollectionCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                        <Button Content="Resume" Command="{Binding ResumeCollectionCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                        <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource SecondaryButtonStyle}" />
                    </StackPanel>

                    <Button Content="Open Settings" Command="{Binding OpenSettingsCommand}" DockPanel.Dock="Right" Style="{StaticResource SecondaryButtonStyle}" />
                </DockPanel>
            </Border>

            <TabControl Grid.Row="2" SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}" Background="Transparent" BorderThickness="0">
                <TabItem Header="Dashboard">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <Grid Margin="0,18,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <UniformGrid Grid.Row="0" Columns="4" Margin="0,0,0,16">
                                <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,16,0">
                                    <StackPanel>
                                        <TextBlock Text="Agent state" Foreground="{StaticResource MutedTextBrush}" />
                                        <TextBlock Text="{Binding AgentStatusText}" FontSize="22" FontWeight="Bold" Margin="0,8,0,0" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,16,0">
                                    <StackPanel>
                                        <TextBlock Text="Last heartbeat" Foreground="{StaticResource MutedTextBrush}" />
                                        <TextBlock Text="{Binding LastHeartbeatText}" FontSize="22" FontWeight="Bold" Margin="0,8,0,0" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,16,0">
                                    <StackPanel>
                                        <TextBlock Text="Last sample" Foreground="{StaticResource MutedTextBrush}" />
                                        <TextBlock Text="{Binding LastSampleText}" FontSize="22" FontWeight="Bold" Margin="0,8,0,0" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                </Border>
                                <Border Style="{StaticResource CardBorderStyle}">
                                    <StackPanel>
                                        <TextBlock Text="Runtime" Foreground="{StaticResource MutedTextBrush}" />
                                        <TextBlock Text="{Binding TodayTotalText}" FontSize="22" FontWeight="Bold" Margin="0,8,0,0" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                </Border>
                            </UniformGrid>

                            <Border Style="{StaticResource CardBorderStyle}" Grid.Row="1">
                                <StackPanel>
                                    <TextBlock Text="Agent process" FontSize="16" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding AgentProcessText}" Margin="0,10,0,0" Foreground="{StaticResource MutedTextBrush}" />
                                </StackPanel>
                            </Border>

                            <Border Style="{StaticResource CardBorderStyle}" Grid.Row="2">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>

                                    <UniformGrid Grid.Row="0" Columns="5" Margin="0,0,0,16">
                                        <Border Background="{StaticResource SurfaceAltBrush}" CornerRadius="14" Padding="14" Margin="0,0,12,0">
                                            <StackPanel>
                                                <TextBlock Text="Today total" Foreground="{StaticResource MutedTextBrush}" />
                                                <TextBlock Text="{Binding TodayTotalText}" FontSize="20" FontWeight="Bold" Margin="0,8,0,0" />
                                            </StackPanel>
                                        </Border>
                                        <Border Background="{StaticResource SurfaceAltBrush}" CornerRadius="14" Padding="14" Margin="0,0,12,0">
                                            <StackPanel>
                                                <TextBlock Text="Today active" Foreground="{StaticResource MutedTextBrush}" />
                                                <TextBlock Text="{Binding TodayActiveText}" FontSize="20" FontWeight="Bold" Margin="0,8,0,0" />
                                            </StackPanel>
                                        </Border>
                                        <Border Background="{StaticResource SurfaceAltBrush}" CornerRadius="14" Padding="14" Margin="0,0,12,0">
                                            <StackPanel>
                                                <TextBlock Text="Today idle" Foreground="{StaticResource MutedTextBrush}" />
                                                <TextBlock Text="{Binding TodayIdleText}" FontSize="20" FontWeight="Bold" Margin="0,8,0,0" />
                                            </StackPanel>
                                        </Border>
                                        <Border Background="{StaticResource SurfaceAltBrush}" CornerRadius="14" Padding="14">
                                            <StackPanel>
                                                <TextBlock Text="Today unknown" Foreground="{StaticResource MutedTextBrush}" />
                                                <TextBlock Text="{Binding TodayUnknownText}" FontSize="20" FontWeight="Bold" Margin="0,8,0,0" />
                                            </StackPanel>
                                        </Border>
                                        <Border Background="{StaticResource SurfaceAltBrush}" CornerRadius="14" Padding="14" Margin="12,0,0,0">
                                            <StackPanel>
                                                <TextBlock Text="Sessions" Foreground="{StaticResource MutedTextBrush}" />
                                                <TextBlock Text="{Binding TodaySessionCountText}" FontSize="20" FontWeight="Bold" Margin="0,8,0,0" />
                                            </StackPanel>
                                        </Border>
                                    </UniformGrid>

                                    <Grid Grid.Row="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,12,0">
                                            <StackPanel>
                                                <TextBlock Text="Top Apps" FontSize="16" FontWeight="SemiBold" />
                                                <ItemsControl ItemsSource="{Binding TopApps}" Margin="0,12,0,0">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="{StaticResource SurfaceAltBrush}" CornerRadius="12" Padding="12" Margin="0,0,0,8">
                                                                <StackPanel>
                                                                    <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
                                                                    <TextBlock Text="{Binding TotalDurationSeconds, StringFormat=Total {0} sec}" Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,0" />
                                                                    <TextBlock Text="{Binding ActiveDurationSeconds, StringFormat=Active {0} sec}" Foreground="{StaticResource MutedTextBrush}" />
                                                                </StackPanel>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>

                                        <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                                            <StackPanel>
                                                <TextBlock Text="Recent Sessions" FontSize="16" FontWeight="SemiBold" />
                                                <ItemsControl ItemsSource="{Binding RecentSessions}" Margin="0,12,0,0">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Border Background="{StaticResource SurfaceAltBrush}" CornerRadius="12" Padding="12" Margin="0,0,0,8">
                                                                <StackPanel>
                                                                    <TextBlock Text="{Binding ProcessName}" FontWeight="SemiBold" />
                                                                    <TextBlock Text="{Binding StartedAtUtc, StringFormat=Start {0:yyyy-MM-dd HH:mm:ss}}" Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,0" />
                                                                    <TextBlock Text="{Binding TotalDurationSeconds, StringFormat=Total {0} sec}" Foreground="{StaticResource MutedTextBrush}" />
                                                                    <TextBlock Text="{Binding CloseReason, StringFormat=Reason {0}}" Foreground="{StaticResource MutedTextBrush}" />
                                                                </StackPanel>
                                                            </Border>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </Grid>
                            </Border>

                            <Border Style="{StaticResource CardBorderStyle}" Grid.Row="3">
                                <StackPanel>
                                    <TextBlock Text="Messages" FontSize="16" FontWeight="SemiBold" />
                                    <ItemsControl ItemsSource="{Binding Messages}" Margin="0,12,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="{StaticResource SurfaceAltBrush}" CornerRadius="12" Padding="12" Margin="0,0,0,8">
                                                    <TextBlock Text="{Binding}" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" />
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="Diagnostics">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <Grid Margin="0,18,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,12,0">
                                <StackPanel>
                                    <TextBlock Text="runtime_state.json" FontSize="16" FontWeight="SemiBold" />
                                    <TextBox Text="{Binding RuntimeStateJson, Mode=OneWay}" Margin="0,12,0,0" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" IsReadOnly="True" TextWrapping="NoWrap" MinHeight="380" />
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="1" Style="{StaticResource CardBorderStyle}" Margin="0,0,12,0">
                                <StackPanel>
                                    <TextBlock Text="health_state.json" FontSize="16" FontWeight="SemiBold" />
                                    <TextBox Text="{Binding HealthStateJson, Mode=OneWay}" Margin="0,12,0,0" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" IsReadOnly="True" TextWrapping="NoWrap" MinHeight="380" />
                                </StackPanel>
                            </Border>

                            <Border Grid.Column="2" Style="{StaticResource CardBorderStyle}">
                                <StackPanel>
                                    <TextBlock Text="agent_control.json" FontSize="16" FontWeight="SemiBold" />
                                    <TextBox Text="{Binding ControlCommandJson, Mode=OneWay}" Margin="0,12,0,0" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" IsReadOnly="True" TextWrapping="NoWrap" MinHeight="380" />
                                </StackPanel>
                            </Border>
                        </Grid>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="Settings">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <Grid Margin="0,18,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Border Style="{StaticResource CardBorderStyle}" Grid.Row="0" Grid.ColumnSpan="2">
                                <DockPanel LastChildFill="True">
                                    <StackPanel DockPanel.Dock="Left">
                                        <TextBlock Text="Local data root" FontSize="16" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding DataRootText}" Margin="0,10,0,0" Foreground="{StaticResource MutedTextBrush}" TextWrapping="Wrap" />
                                    </StackPanel>

                                    <Button Content="Open Data Folder" Command="{Binding OpenDataFolderCommand}" DockPanel.Dock="Right" Style="{StaticResource SecondaryButtonStyle}" />
                                </DockPanel>
                            </Border>

                            <Border Style="{StaticResource CardBorderStyle}" Grid.Row="1" Margin="0,0,12,0">
                                <StackPanel>
                                    <TextBlock Text="app-settings.json" FontSize="16" FontWeight="SemiBold" />
                                    <TextBox Text="{Binding AppSettingsJson, Mode=OneWay}" Margin="0,12,0,0" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" IsReadOnly="True" TextWrapping="NoWrap" MinHeight="360" />
                                </StackPanel>
                            </Border>

                            <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource CardBorderStyle}">
                                <StackPanel>
                                    <TextBlock Text="windows-agent.json" FontSize="16" FontWeight="SemiBold" />
                                    <TextBox Text="{Binding AgentOptionsJson, Mode=OneWay}" Margin="0,12,0,0" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" FontFamily="Consolas" FontSize="12" IsReadOnly="True" TextWrapping="NoWrap" MinHeight="360" />
                                </StackPanel>
                            </Border>
                        </Grid>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</Window>
