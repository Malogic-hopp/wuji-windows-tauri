<UserControl x:Class="QuantifiedSelf.Windows.App.Views.AppsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:QuantifiedSelf.Windows.App.Converters"
             mc:Ignorable="d"
             d:DesignHeight="640"
             d:DesignWidth="1080">
    <UserControl.Resources>
        <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Margin="0,18,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{DynamicResource CardBorderStyle}">
            <DockPanel LastChildFill="False">
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock Text="应用汇总" Style="{StaticResource TitleSmallStyle}" />
                    <TextBlock Text="{Binding StatusText}" Margin="0,8,0,0" Foreground="{DynamicResource TextMutedBrush}" />
                </StackPanel>

                <Button Content="Refresh"
                        DockPanel.Dock="Right"
                        Command="{Binding RefreshCommand}"
                        Style="{DynamicResource SecondaryButtonStyle}" />
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Style="{DynamicResource CardBorderStyle}">
            <Grid>
                <TextBlock Text="{Binding EmptyStateText}"
                           Foreground="{DynamicResource TextMutedBrush}"
                           TextWrapping="Wrap"
                           Visibility="{Binding Apps.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Invert}" />

                <DataGrid ItemsSource="{Binding Apps}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          RowHeaderWidth="0"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling"
                          Visibility="{Binding Apps.Count, Converter={StaticResource CountToVisibilityConverter}}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="排名" Binding="{Binding Rank}" Width="70" />
                        <DataGridTextColumn Header="应用名称" Binding="{Binding DisplayName}" Width="180" />
                        <DataGridTextColumn Header="进程名称" Binding="{Binding ProcessName}" Width="220" />
                        <DataGridTextColumn Header="活跃时长" Binding="{Binding ActiveDurationText}" Width="110" />
                        <DataGridTextColumn Header="记录时长" Binding="{Binding TotalDurationText}" Width="110" />
                        <DataGridTextColumn Header="空闲时长" Binding="{Binding IdleDurationText}" Width="110" />
                        <DataGridTextColumn Header="未知时长" Binding="{Binding UnknownDurationText}" Width="110" />
                        <DataGridTextColumn Header="会话数" Binding="{Binding SessionCount}" Width="90" />
                        <DataGridTextColumn Header="最近使用时间" Binding="{Binding LastUsedLocalTimeText}" Width="170" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
