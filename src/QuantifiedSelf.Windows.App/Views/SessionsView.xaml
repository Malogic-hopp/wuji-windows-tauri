<UserControl x:Class="QuantifiedSelf.Windows.App.Views.SessionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:QuantifiedSelf.Windows.App.Converters"
             mc:Ignorable="d"
             d:DesignHeight="640"
             d:DesignWidth="1080">
    <UserControl.Resources>
        <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Margin="0,18,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{DynamicResource CardBorderStyle}">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock Text="Sessions" FontSize="16" FontWeight="SemiBold" />
                    <TextBlock Text="{Binding StatusText}" Margin="0,8,0,0" Foreground="{DynamicResource MutedTextBrush}" />
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <ComboBox ItemsSource="{Binding RangeFilters}"
                              SelectedItem="{Binding SelectedRange, Mode=TwoWay}"
                              Width="150"
                              Margin="0,0,8,0"
                              VerticalContentAlignment="Center" />
                    <ComboBox ItemsSource="{Binding CloseReasonFilters}"
                              SelectedItem="{Binding SelectedCloseReason, Mode=TwoWay}"
                              Width="150"
                              Margin="0,0,8,0"
                              VerticalContentAlignment="Center" />
                    <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{DynamicResource SecondaryButtonStyle}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Style="{DynamicResource CardBorderStyle}">
            <Grid>
                <TextBlock Text="{Binding EmptyStateText}"
                           Foreground="{DynamicResource MutedTextBrush}"
                           TextWrapping="Wrap"
                           Visibility="{Binding Sessions.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Invert}" />

                <DataGrid ItemsSource="{Binding Sessions}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          RowHeaderWidth="0"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling"
                          Visibility="{Binding Sessions.Count, Converter={StaticResource CountToVisibilityConverter}}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Started local time" Binding="{Binding StartedLocalTime, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="170" />
                        <DataGridTextColumn Header="Ended local time" Binding="{Binding EndedLocalTimeText}" Width="170" />
                        <DataGridTextColumn Header="Process" Binding="{Binding ProcessName}" Width="150" />
                        <DataGridTextColumn Header="Display name" Binding="{Binding DisplayName}" Width="160" />
                        <DataGridTextColumn Header="Total" Binding="{Binding TotalDurationText}" Width="95" />
                        <DataGridTextColumn Header="Active" Binding="{Binding ActiveDurationText}" Width="95" />
                        <DataGridTextColumn Header="Idle" Binding="{Binding IdleDurationText}" Width="95" />
                        <DataGridTextColumn Header="Unknown" Binding="{Binding UnknownDurationText}" Width="95" />
                        <DataGridTextColumn Header="Close reason" Binding="{Binding CloseReason}" Width="140" />
                        <DataGridTextColumn Header="Session id" Binding="{Binding SessionId}" Width="90" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
