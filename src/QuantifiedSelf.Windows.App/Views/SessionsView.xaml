<UserControl x:Class="QuantifiedSelf.Windows.App.Views.SessionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:QuantifiedSelf.Windows.App.Converters"
             mc:Ignorable="d"
             d:DesignHeight="640"
             d:DesignWidth="1080">
    <UserControl.Resources>
        <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Margin="0,18,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{DynamicResource CardBorderStyle}">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Left">
                    <TextBlock Text="会话明细" Style="{StaticResource TitleSmallStyle}" />
                    <TextBlock Text="{Binding StatusText}" Margin="0,8,0,0" Foreground="{DynamicResource TextMutedBrush}" />
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                    <ComboBox ItemsSource="{Binding RangeFilters}"
                              SelectedItem="{Binding SelectedRange, Mode=TwoWay}"
                              Width="150"
                              Margin="0,0,8,0"
                              VerticalContentAlignment="Center" />
                    <ComboBox ItemsSource="{Binding CloseReasonFilters}"
                              SelectedItem="{Binding SelectedCloseReason, Mode=TwoWay}"
                              Width="150"
                              Margin="0,0,8,0"
                              VerticalContentAlignment="Center" />
                    <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{DynamicResource SecondaryButtonStyle}" />
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="1" Style="{DynamicResource CardBorderStyle}">
            <Grid>
                <TextBlock Text="{Binding EmptyStateText}"
                           Foreground="{DynamicResource TextMutedBrush}"
                           TextWrapping="Wrap"
                           Visibility="{Binding Sessions.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Invert}" />

                <DataGrid ItemsSource="{Binding Sessions}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          RowHeaderWidth="0"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          VirtualizingStackPanel.IsVirtualizing="True"
                          VirtualizingStackPanel.VirtualizationMode="Recycling"
                          Visibility="{Binding Sessions.Count, Converter={StaticResource CountToVisibilityConverter}}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="开始时间" Binding="{Binding StartedLocalTime, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Width="170" />
                        <DataGridTextColumn Header="结束时间" Binding="{Binding EndedLocalTimeText}" Width="170" />
                        <DataGridTextColumn Header="进程" Binding="{Binding ProcessName}" Width="150" />
                        <DataGridTextColumn Header="应用名称" Binding="{Binding DisplayName}" Width="160" />
                        <DataGridTextColumn Header="记录时长" Binding="{Binding TotalDurationText}" Width="95" />
                        <DataGridTextColumn Header="活跃时长" Binding="{Binding ActiveDurationText}" Width="95" />
                        <DataGridTextColumn Header="空闲时长" Binding="{Binding IdleDurationText}" Width="95" />
                        <DataGridTextColumn Header="未知时长" Binding="{Binding UnknownDurationText}" Width="95" />
                        <DataGridTextColumn Header="结束原因" Binding="{Binding CloseReason}" Width="140" />
                        <DataGridTextColumn Header="会话标识" Binding="{Binding SessionId}" Width="90" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
