# WUJI（吾迹）用户手册

> 版本：2026-07-15

本文档详细介绍 WUJI 桌面应用的每个界面、布局和功能，方便用户和协作者了解产品全貌。

---

## 主窗口布局

WUJI 支持两套主窗口 Shell，通过启动参数切换：

- **Legacy Shell**（默认）：顶部工具栏 + Tab 标签页的传统布局，功能完整稳定。
- **新 Shell**（`--ui-preview`）：左侧 Sidebar 导航 + 顶部状态栏的现代布局，支持浅色/深色/高对比度主题。

启动命令：
```bash
# 默认（Legacy Shell）
dotnet run --project src/QuantifiedSelf.Windows.App/

# 新 Shell（开发预览）
dotnet run --project src/QuantifiedSelf.Windows.App/ -- --channel dev --ui-preview
```

---

## 新 Shell 布局（--ui-preview）

新 Shell 窗口（默认 1480×960，最小 960×640）分为两个区域：

```
┌────────────────────────────────────────────────────────────────┐
│  Top Bar: 日期 + 当前页面标题 / 副标题        [Agent 状态指示器]  │
├──────────┬─────────────────────────────────────────────────────┤
│ Sidebar  │                                                     │
│          │                                                     │
│  Today   │                                                     │
│  时间线  │              页面内容区域                             │
│  洞察    │         （根据导航自动切换）                           │
│  趋势    │                                                     │
│ ─────── │                                                     │
│  隐私    │                                                     │
│  设置    │                                                     │
│          │                                                     │
│ [Footer] │                                                     │
└──────────┴─────────────────────────────────────────────────────┘
```

### Top Bar（顶部栏）

- **左侧**：当前日期（如「7月15日」）、页面标题（如「今天」）和副标题（如「回顾今天发生了什么，先看结论，再看证据。」）。
- **右侧**：Agent 状态指示器（圆角药丸形），显示当前 Agent 运行状态。点击可进入 Diagnostics 诊断页。

### Sidebar（侧边导航栏）

两种宽度模式随窗口宽度自动切换：
- **展开模式**（≥1100px）：184px 宽，显示图标 + 中文标签。
- **紧凑模式**（960–1100px）：52px 宽，仅显示图标。

导航项分为三组：

| 分组 | 页面 | 中文标签 | 说明 |
|------|------|----------|------|
| **主要** | Today | 今天 | 今日活动总览与洞察 |
| | Timeline | 时间线 | 应用/会话/原始记录回放 |
| | Insights | 洞察 | 按日查询的专注分析 |
| | Trends | 趋势 | 7 天活跃趋势与热力图 |
| **次要** | Privacy | 数据与隐私 | 排除规则与保留周期 |
| | Settings | 设置 | 采样、启动、通知、外观 |
| **底部** | Diagnostics | 诊断 | 通过状态栏指示器进入 |

### 主题切换

通过 Settings → 外观（Appearance）切换三套主题：
- **浅色**（Light）：默认，白色背景 + 深色文字。
- **深色**（Dark）：深色背景 + 浅色文字。
- **高对比度**（High Contrast）：映射 Windows 系统高对比度颜色，所有语义画刷使用 `SystemColors`。

---

## Legacy Shell 布局（默认）

```
┌────────────────────────────────────────────────────────────────┐
│                         标题栏                                  │
│  WUJI 吾迹                        [Agent 状态指示器] [状态消息]   │
├────────────────────────────────────────────────────────────────┤
│                         工具栏                                  │
│  [Start Agent] [Stop Agent] [Pause] [Resume] [Refresh]  [Open Settings] │
├────────────────────────────────────────────────────────────────┤
│                         页面标签区                               │
│  [Dashboard] [Apps] [Sessions] [Samples] [Diagnostics] [Insights] [Settings] │
│                                                                 │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│                        页面内容区域                               │
│                                                                 │
└────────────────────────────────────────────────────────────────┘
```

### 标题栏

**新 Shell**：
- **左侧**：当前日期、页面标题和副标题。
- **右侧**：Agent 状态指示器（圆角药丸形）— 点击可进入 Diagnostics 诊断页。

**Legacy Shell**：
- **左侧**：显示品牌名称「WUJI 吾迹」。
- **右侧**：Agent 状态指示器 + 状态消息（如最后一次心跳时间）。

### 工具栏

**新 Shell**：不在工具栏独立排列按钮。Agent 生命周期控制（启动/停止/暂停/恢复）集成在 Top Bar 的 Agent 状态指示器下拉菜单中；页面导航通过 Sidebar 完成。

**Legacy Shell**：7 个按钮控制 Agent 和页面：

| 按钮 | 功能 |
|------|------|
| **Start Agent** | 启动 Agent 进程，开始后台采样 |
| **Stop Agent** | 停止 Agent 进程，关闭未结束的 Session |
| **Pause** | 暂停采集（Agent 保持运行但不记录新样本） |
| **Resume** | 从暂停恢复采集 |
| **Refresh** | 手动刷新当前页面的数据 |
| **Open Settings** | 跳转到 Settings 标签页 |

所有按钮会根据当前 Agent 状态自动启用/禁用。

---

## 1. Today（今天）

首页总览，展示今日活动摘要。"先看结论，再看证据"——页面从上到下依次为摘要洞察、关键指标、热力图和排行。

### 1.1 今日摘要

自然语言描述今日的活动概况，例如「你今天比较专注，最长连续工作块达到 45 分钟」。同时展示基于统计规则的个性化建议，每条包含标题、证据数据、行动建议。颜色标记：绿色为正面洞察、橙色为提醒、灰色为中性信息。

### 1.2 关键指标

指标区在紧凑布局下自动切换为 2×2 或堆叠排列：

| 指标 | 说明 |
|------|------|
| **Active** | 今日 Active 状态总时长 |
| **Sessions** | 今日 Session 数量 |
| **Samples** | 今日采样次数 |
| **任务切换** | 有效任务上下文切换次数 |
| **Longest Focus** | 今日最长连续专注时长 |
| **Time Range** | 数据覆盖的时间范围 |

### 1.3 Activity Heatmap（活动热力图）

24 小时 × N 天的二维热力图，横轴为小时，纵轴为日期，颜色深浅表示该小时的活动密度。每个单元格为可聚焦控件（`AccessibleHeatmap`），支持方向键导航、屏幕阅读器 `AutomationProperties.Name` 和 Enter/Space 跳转至 Timeline 查看详情。

### 1.4 Today 24h（今日 24 小时时间线）

当天的逐小时活跃时长堆叠柱状图。

### 1.5 Top Apps（应用排行）

按使用时长排名的应用柱状图（低饱和序列配色），占比不足阈值的应用合并显示为「其他」。同时显示应用占比饼图。

### 1.6 Top Windows（窗口排行）

采样次数最多的前 10 个窗口标题，以卡片列表展示，包含窗口标题、进程名和采样次数。

---

## 2. Timeline（时间线）

按应用、会话和原始记录三个维度回放历史数据，支持按天筛选快速跳转。

### 视图切换

三种并列视图，通过标签切换：

| 视图 | 数据来源 | 说明 |
|------|----------|------|
| **应用** | App 聚合统计 | 按应用排行的使用时长分布 |
| **会话** | Session 记录 | 每次连续使用的时间段，含起止时间、各状态时长、关闭原因 |
| **原始记录** | 采样记录 | Agent 每次采集的前台窗口原始数据 |

### 数据表格

三视图共用统一的表格布局，列名已中文化：

**应用视图**：排名、应用名、进程名、活跃时长、总时长、空闲时长、未知时长、会话数、最近使用时间。

**会话视图**：开始时间、结束时间、进程、应用名、总时长、活跃、空闲、未知、关闭原因、Session ID。

**原始视图**：采样时间、应用名、进程、窗口标题（脱敏后）、空闲秒数、活动状态、Sample ID。

### 筛选

- 时间范围筛选：Today / Yesterday / Last 7 days / Last 30 days / All
- 活动状态筛选（原始视图）：All / Active / Idle / Unknown

页面在 Loading / Empty / Ready / Error 四种状态之间切换，统一使用 `PageState` 枚举控制。

---

## 3. Insights（洞察）

按天查询的深度行为分析页面，提供日期导航、统计卡片和细分视图。

### 日期导航栏

- **上一天** / **下一天** 按钮和 **今天** 快捷按钮
- **DatePicker** 日历控件
- 选择未来日期时「下一天」按钮自动禁用

### 洞察摘要

数据存在时显示自然语言摘要和一条 💡 操作建议（绿色背景）。无数据时显示空状态提示。

### 统计卡片

| 指标 | 说明 |
|------|------|
| **任务切换** | 当日有效任务上下文切换次数 |
| **工具跳转** | 原始窗口切换次数 |
| **最长工作块** | 当日最长连续工作段的持续时长 |
| **首要中断** | 打断专注的最主要应用 |
| **活跃样本** | 当日活跃采样数 |
| **预估活跃** | 当日预估活跃总时长 |

### 工作块（Work Blocks）

按时间顺序列出当日每个连续工作时段，每个卡片包含：
- **时间段**：`HH:mm – HH:mm` + 时长（分钟）
- **标签**：绿色「专注 ✓」或黄色「碎片化」
- **详情**：主要上下文 + 主要应用 + 切换次数 + 平均切换间隔
- **内部中断列表**：被哪些应用打断及次数

### 中断来源与上下文切换

- **中断来源**：列出打断专注的 Top 应用及中断次数
- **任务切换方向**：展示最常见的上下文切换路径（如「编码 → 聊天 → 编码」），含切换次数和占比

---

## 4. Trends（趋势）

展示过去 7 天有效使用数据的趋势变化和活动分布。

### 7 日活跃趋势

折线图展示过去 7 天每天的活跃时长变化，仅使用真实可用的天数数据（不多不少恰好 7 天有数据时完整展示）。

### 活动热力图

24 小时 × 7 天的活动密度热力图，使用五级图例（低 → 高）直观展示活跃模式。每个单元格为可聚焦控件，支持键盘导航和屏幕阅读器。

### 图例

五级色阶，从浅到深表示活动强度：「低」「较低」「中等」「较高」「高」。

> **注意**：30 天/12 周聚合及更长时间跨度的统计依赖尚未实现的后端聚合模型，当前版本不展示这些范围选择器。

---

## 5. Privacy（数据与隐私）

管理数据收集的隐私边界和本地存储策略。

### 排除管理

- **排除进程列表**：多行文本编辑，每行一个进程名（如 `KeePass`、`1Password`）。Agent 不会采集这些进程的前台窗口。
- **排除标题模式**：多行文本编辑，每行一个匹配模式（如 `InPrivate`）。窗口标题包含该模式的不会被记录。

### 保留与清理

- **保留周期**：数据保留天数（`RetentionDays`），超期数据通过 Prune 操作删除。
- **路径脱敏**：窗口标题脱敏开关（`MaskWindowTitles`），开启后敏感路径信息会被替换。
- **导出数据**：导出确认对话框，防止误操作。
- **清空历史**：需要二次确认（输入确认文本）方可执行，防止误清空。

### 数据位置

展示当前数据目录、配置文件路径、数据库路径、日志目录的实际磁盘路径。

---

## 6. Diagnostics（诊断）

Agent 内部状态和运行健康的全面视图。首页显示简化的健康摘要 + 修复/刷新入口；技术细节默认折叠，点击展开。

### 健康摘要

- Agent 状态、PID、IPC 通道状态
- SQLite 写入状态（正常 / 异常）
- JSONL 日志写入状态（正常 / 异常）

### 可折叠技术区域

| 区域 | 内容 |
|------|------|
| **Tick Diagnostics** | 最近 tick 阶段、Tick/Capture/Persist/Maintenance 耗时、最近错误 |
| **Login Startup** | 开机自启注册状态、启动模式、注册详情 |
| **状态文件** | `runtime_state.json`、`health_state.json`、`agent_control.json` 实时内容（等宽字体） |
| **路径信息** | 数据根目录、数据库路径、JSONL 日志路径、配置目录 |

### 事件与告警

- **Recent Events**：最近 50 条 Agent 事件（类型、级别、消息、errorCode）
- **Recent Errors**：最近 50 条 Warning / Error / Critical 级别事件

---

## 7. Settings（设置）

设置页分为五个子区域（通过独立的子 ViewModel 管理），通过标签页切换。

### 7.1 General（常规）

App 行为设置：

| 设置项 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| **页面刷新间隔** | 文本框 | 15 | 数据页面自动刷新间隔（秒） |
| **启动后自动启动 Agent** | 复选框 | 未勾选 | App 启动时自动启动 Agent 并开始采样 |
| **登录 Windows 后启动 WUJI** | 复选框 | 未勾选 | Windows 登录后自动启动 WUJI App（后台托盘模式） |

### 7.2 Recording（记录）

Agent 采样参数：

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| **采样间隔** | 文本框 | 3 | 两次采样之间的间隔秒数 |
| **空闲阈值** | 文本框 | 60 | 无操作超过此时长判定为 Idle |
| **保留天数** | 文本框 | 30 | 数据保留天数 |
| **窗口标题脱敏** | 复选框 | 勾选 | 是否脱敏窗口标题（隐私保护） |

### 7.3 Notifications（通知）

当前版本仅使用系统托盘状态指示，不发送云端或营销通知。显示提示文本说明通知策略。

### 7.4 Appearance（外观）

三套主题可选：

| 主题 | 说明 |
|------|------|
| **Light**（浅色） | 默认主题，白色背景 + 深色文字 |
| **Dark**（深色） | 深色背景 + 浅色文字，适合弱光环境 |
| **High Contrast**（高对比度） | 映射 Windows 系统高对比度颜色（`SystemColors`），适合辅助需求 |

切换后立即生效，选择持久化到 `app-settings.json`。

### 7.5 Advanced（高级）

Agent 高级参数与调试开关：

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| **心跳间隔** | 文本框 | 3 | 心跳文件写入间隔（秒） |
| **Stale 阈值** | 文本框 | 15 | App 判定 Agent 不健康的心跳超时（秒） |
| **JSONL 日志** | 复选框 | 勾选 | 是否同时写 JSONL 日志文件 |
| **Agent 事件日志** | 复选框 | 勾选 | 是否在 SQLite 中记录 Agent 事件 |

### 操作按钮

以下按钮在需要时可用（如保存、重载、校验、恢复备份、重置）。

### 快捷入口

- **Open Data Folder**：在资源管理器中打开数据目录
- **Open Logs Folder**：在资源管理器中打开日志目录
- **Open Config Folder**：在资源管理器中打开配置目录

### 数据管理

| 操作 | 说明 |
|------|------|
| **清理过期数据** | 按 `RetentionDays` 清理过期数据（需确认） |
| **清空所有历史** | 清空所有历史数据（需二次确认，输入确认文本） |

---

## 8. 系统托盘

Agent 运行时，系统托盘显示 WUJI 图标。托盘功能：

### 托盘菜单

```
┌──────────────┐
│ WUJI 吾迹    │  ← 双击打开 / 恢复主窗口
│ Agent: xxx   │  ← 当前 Agent 状态（Running / Paused / Stopped 等）
├──────────────┤
│ Show Window  │  ← 显示主窗口
│ Start Agent  │  ← 启动 Agent（仅在 Agent 未运行时可用）
│ Stop Agent   │  ← 停止 Agent（仅在 Agent 运行时可用）
│ Pause        │  ← 暂停采样（仅在 Running 时可用）
│ Resume       │  ← 恢复采样（仅在 Paused 时可用）
├──────────────┤
│ Exit         │  ← 退出 WUJI（停止 Agent 并关闭应用）
└──────────────┘
```

### 托盘行为

- **双击托盘图标**：显示/恢复主窗口。
- **CloseToTray**（默认开启）：关闭主窗口时最小化到托盘而非退出应用。
- **MinimizeToTray**（默认开启）：最小化主窗口时隐藏到托盘。
- **Auto-start 隐藏启动**：如果配置了开机自启且以 `--from-autostart --start-hidden` 启动，主窗口不会自动弹出，Agent 静默运行在后台。

从托盘退出时会同时停止 Agent 进程（如果有且正在运行）。
