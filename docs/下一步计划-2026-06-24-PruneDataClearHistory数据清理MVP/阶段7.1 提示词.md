请在仓库 C:\Users\WangZhenchi\work\WUJI\Win 中实施阶段 7.1。

请先阅读并严格遵循这些文档：

1. docs\下一步计划-2026-06-24-PruneDataClearHistory数据清理MVP.md
2. docs\下一步计划-2026-06-24-PruneDataClearHistory数据清理MVP\README.md
3. docs\下一步计划-2026-06-24-PruneDataClearHistory数据清理MVP\01-阶段7.1-数据清理命令与Maintenance状态骨架.md
4. docs\下一步计划-2026-06-22-Settings与数据管理MVP\阶段6-完成说明-2026-06-24.md

任务目标：

实现“阶段 7.1：数据清理命令与 Maintenance 状态骨架”。

本阶段只做状态骨架，不做真实数据删除。尤其注意：

- 不要执行 SQLite DELETE。
- 不要删除 JSONL。
- 不要做 PruneData 数据层服务。
- 不要做 ClearHistory 数据层服务。
- 不要做完整 Settings Data Management UI。
- 不要做 ClearHistory 二次确认 UI。
- 不要做 VACUUM。
- 不要引入 Named Pipe / gRPC。

需要实现的范围：

1. 在 AgentActualState 中新增 Maintenance。

2. 在 AgentEventType 中新增 DataPruned / HistoryCleared。
   本阶段可以只新增枚举，为后续阶段使用；不要求真实写 DataPruned / HistoryCleared。

3. 修改 AgentStateMachine 中 PruneData / ClearHistory 的处理逻辑：
   - 不再只是 accepted 占位。
   - 收到 PruneData 后：
     - 写 CommandAccepted。
     - 进入 Maintenance。
     - Persist runtime_state / health_state，使外部能看到 Maintenance。
     - 模拟完成，不做任何删除。
     - 恢复到命令前状态：Running 回 Running，Paused 回 Paused。
     - 写 CommandCompleted。
   - 收到 ClearHistory 后：
     - 写 CommandAccepted。
     - 进入 Maintenance。
     - Persist runtime_state / health_state，使外部能看到 Maintenance。
     - 模拟完成，不做任何删除。
     - 最终进入 Paused。
     - 写 CommandCompleted。
   - 失败时必须通过 try/finally 或等价方式退出 Maintenance，不能卡死。
   - 错误时写 CommandFailed，错误文本不能泄露路径。

4. Maintenance 期间的状态语义：
   - Maintenance 表示 Agent 仍在运行，但正在维护，不执行普通 sample tick。
   - 如果当前 tick 中 ActualState 是 Maintenance，不应写 foreground sample。
   - runtime_state / health_state 应能表达 Maintenance。

5. WPF / ViewModel 层如已有控制按钮状态判断，需要让 Maintenance 作为不可操作状态：
   - PruneData / ClearHistory 本阶段还没有完整 UI，可以不做完整入口。
   - 但如果相关 command / can execute 已存在或测试需要，应保证 Maintenance 期间不会重复发送 PruneData / ClearHistory。
   - Start / Pause / Resume / Stop 在 Maintenance 第一版保守禁用或不被 UI 触发。

6. 保持现有 Pause / Resume / Stop / ReloadConfig 行为不回归。
   特别是阶段 6 的 Settings、ReloadConfig、Diagnostics 相关测试不能破坏。

建议重点查看这些文件：

- src\QuantifiedSelf.Windows.Core\Control\AgentActualState.cs
- src\QuantifiedSelf.Windows.Core\Events\AgentEventType.cs
- src\QuantifiedSelf.Windows.Agent\State\AgentStateMachine.cs
- src\QuantifiedSelf.Windows.App\Services\AgentControlService.cs
- src\QuantifiedSelf.Windows.App\ViewModels\MainWindowViewModel.cs
- src\QuantifiedSelf.Windows.App\ViewModels\SettingsViewModel.cs
- tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs

建议新增或调整测试：

- AgentStateMachine_PruneData_EntersAndLeavesMaintenance
- AgentStateMachine_ClearHistory_EndsPaused
- AgentStateMachine_MaintenanceWritesRuntimeAndHealthState
- AgentStateMachine_MaintenanceFailureDoesNotGetStuck
- AgentControlFileStore_RoundsTripPruneDataAndClearHistory
- 如 UI/ViewModel 已接入 Maintenance 状态，也补：
  - SettingsViewModel_DisablesCleanupCommandsDuringMaintenance
  或等价测试

验收标准：

- AgentActualState 支持 Maintenance。
- PruneData 命令产生 CommandDetected / CommandAccepted / CommandCompleted。
- ClearHistory 命令产生 CommandDetected / CommandAccepted / CommandCompleted。
- 清理命令处理期间 runtime_state / health_state 能看到 Maintenance。
- 清理命令结束后不会卡在 Maintenance。
- PruneData 最终恢复命令前 Running / Paused。
- ClearHistory 最终进入 Paused。
- Maintenance 期间不执行普通 sample 写入。
- Unsupported command 行为不回归。
- 不发生任何真实数据删除。
- 不删除任何 JSONL。

请完成代码和测试修改后运行：

dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\build-output\
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\test-output\

如果 build/test 通过，请汇总：
- 修改了哪些文件
- 实现了哪些行为
- 新增/更新了哪些测试
- build/test 结果
- 是否建议进入阶段 7.2

注意：
- Get-Content 读取中文文档时使用 -Encoding UTF8。
- 不要回滚用户或其他 agent 的改动。
- 不要把阶段 7.2/7.3 的真实删除逻辑提前做进来。