# 阶段 10 验收清单

更新时间：2026-07-04

`dotnet build` 0 warnings / 0 errors，`dotnet test` 268/268 passed。

## 自动化验收

### 托盘基础设施
- [x] TrayService 单元测试通过 —— `TrayService_ShowMainWindowInvokesCallback`、`TrayService_ExitAppInvokesShutdownCallback`、`TrayService_DisposeIsIdempotent`
- [x] TrayMenuState 初始状态正确 —— `TrayMenuState_BuildsInitialState`
- [x] TrayService 不直接依赖 Agent 控制服务 —— `TrayService_DoesNotCallAgentServicesDirectly`
- [x] TrayService 安全脱敏 —— `TrayService_DoesNotExposeSensitiveDetailsInTooltip`

### CloseToTray / MinimizeToTray
- [x] CloseToTray 取消关闭并隐藏窗口 —— `WindowLifecycleCoordinator_CloseToTrayCancelsClose`
- [x] Exit App 允许关闭 —— `WindowLifecycleCoordinator_ExitAppAllowsClose`
- [x] MinimizeToTray 启用时隐藏 —— `WindowLifecycleCoordinator_MinimizeToTrayHidesWhenEnabled`
- [x] 退出期间跳过 MinimizeToTray —— `WindowLifecycleCoordinator_MinimizeToTraySkippedDuringExit`
- [x] AppSettings 默认值正确 —— `AppSettings_DefaultsToCloseToTrayAndMinimizeToTray`

### 托盘状态展示
- [x] Running 状态菜单正确 —— `TrayMenuState_FollowsRunningStatus`
- [x] Paused 状态菜单正确 —— `TrayMenuState_FollowsPausedStatus`
- [x] Maintenance 状态命令禁用 —— `TrayMenuState_DisablesCommandsDuringMaintenance`
- [x] Stale + 进程不在时 Start 可用 —— `TrayMenuState_AllowsStartWhenStaleButProcessGone`
- [x] IPC 细节不泄露 —— `TrayMenuState_DoesNotExposeSensitiveIpcDetails`
- [x] tooltip 安全截断 —— `TrayMenuState_TooltipTextIsSafelyTruncated`
- [x] ViewModel 路径托盘状态同步 —— `MainWindowViewModel_RefreshCommonStatusUpdatesTrayStateSink`

### 托盘基础控制命令
- [x] Start 命令触发回调 —— `TrayService_StartCommandInvokesCallback`
- [x] Pause 命令触发回调 —— `TrayService_PauseCommandInvokesCallback`
- [x] Resume 命令触发回调 —— `TrayService_ResumeCommandInvokesCallback`
- [x] Stop 命令触发回调 —— `TrayService_StopCommandInvokesCallback`
- [x] 无回调时不抛异常 —— `TrayService_NullCallbackDoesNotThrow`
- [x] 全部命令 null 安全 —— `TrayService_NullCallbackSafeForAllCommands`
- [x] 禁用命令不执行（CanExecute 守卫） —— `TrayService_DisabledStartDoesNotExecuteWhenCallbackGuards`
- [x] TrayService 不直接依赖 Agent 服务 —— `TrayService_DoesNotCallAgentServicesDirectly`

### 异常状态与恢复
- [x] NotRunning 状态 tooltip + Start 可用 —— `TrayStatus_ShowsNotRunningState`
- [x] Stale+进程在：Stop 可用、Start 不可用 —— `TrayStatus_DisablesStartWhenStaleButProcessAlive`
- [x] NotRunning→Running 恢复 —— `TrayStatus_RecoversFromNotRunningToRunning`
- [x] FileFallback→NamedPipe 恢复 —— `TrayStatus_IpcFallbackRecoversToNamedPipe`
- [x] IPC 未知→unavailable 安全文案 —— `TrayStatus_IpcUnavailableShowsSafeText`
- [x] Stale→Running 恢复 —— `TrayStatus_RecoversFromStaleToRunning`
- [x] 默认快照不崩 —— `TrayStatus_DoesNotCrashOnDefaultSnapshot`
- [x] ViewModel 路径 Stale→Running 恢复 —— `TrayStatus_RecoveryUpdatesTrayStateSinkViaViewModel`
- [x] Settings dirty 不被托盘状态更新覆盖 —— `TrayStatus_DoesNotOverrideSettingsDrafts`

### 安全与脱敏
- [x] tooltip 不含路径/SID/pipe name —— `TrayService_DoesNotExposeSensitiveDetailsInTooltip`、`TrayMenuState_DoesNotExposeSensitiveIpcDetails`、`TrayStatus_IpcUnavailableShowsSafeText`
- [x] tooltip 长度 ≤63 —— `TrayMenuState_TooltipTextIsSafelyTruncated`
- [x] IPC 文案仅 NamedPipe/FileFallback/unavailable —— `TrayMenuState.From` 白名单 switch
- [x] 异常原文不出现于托盘 —— 所有托盘测试 tooltip 断言不含路径/SID

### 前序阶段不回归
- [x] 阶段 9 RefreshService / status polling 测试不回归（268/268 全通过）
- [x] 阶段 8 IPC 测试不回归（268/268 全通过）
- [x] 阶段 7 PruneData / ClearHistory 测试不回归（268/268 全通过）

## 静态检查

| 检查项 | 结论 | 依据 |
|---|---|---|
| CloseToTray / MinimizeToTray 只 Hide，不停止 status polling | ✅ PASS | App.xaml.cs Closing/StateChanged 仅 window.Hide()，不调用 StopStatusPolling |
| Exit App 才 StopStatusPolling + Dispose TrayService + Shutdown | ✅ PASS | App.xaml.cs window.Closed 中调用 StopStatusPolling + Dispose |
| Exit App 不默认 Stop Agent | ✅ PASS | App.xaml.cs exitApp 回调仅设 _isExitRequested + Shutdown，无 Agent 命令 |
| TrayService 不直接实现 Agent 控制逻辑 | ✅ PASS | TrayService 构造函数仅接收 Action 回调，不持有 AgentControlService/AgentProcessService |
| 托盘菜单状态来自 AgentCommandAvailability | ✅ PASS | TrayMenuState.From 使用 availability.CanStart/CanPause/CanResume/CanStop |
| 托盘不新增状态轮询 | ✅ PASS | TrayService 无 Timer/轮询逻辑，状态仅通过 ITrayStateSink.UpdateState 推入 |
| 隐藏窗口后不改变阶段 9 页面自动刷新语义 | ✅ PASS | Hide 不触及 _refreshTimer/_statusPollTimer |
| Settings IsDirty 不被托盘状态更新覆盖 | ✅ PASS | RefreshCommonStatus 仅调用 UpdateAgentStatus（同阶段 9），托盘状态独立；测试 TrayStatus_DoesNotOverrideSettingsDrafts 验证 |
| 托盘错误展示不泄露 FullPipeName / SID / 路径 | ✅ PASS | TrayMenuState.From 中 ipcStatus 白名单仅 NamedPipe/FileFallback/unavailable |
| NotifyIcon Dispose 幂等 | ✅ PASS | NotifyIconTrayIconAdapter.Dispose 有 _disposed guard；TrayService.Dispose 有 _disposed guard |
| NotifyIcon Dispose 前先设置 Visible=false | ✅ PASS | NotifyIconTrayIconAdapter: Visible=false 在 Dispose 之前（行 91-92）；TrayService: Visible=false 在 Dispose 之前（行 91-92） |
| 图标资源使用简单 fallback | ✅ PASS | NotifyIconTrayIconAdapter 使用 SystemIcons.Application |
| Show Main Window 只做 Show + Normal + Activate | ✅ PASS | App.xaml.cs showMainWindow: Show() + WindowState=Normal（如 Minimized）+ Activate() |
| Dispatcher shutdown 时托盘回调安全忽略 | ✅ PASS | TrayService.TryPostToDispatcher 检查 HasShutdownStarted/HasShutdownFinished，捕获 ObjectDisposedException/InvalidOperationException |

## 手动验收

以下项目已在真实 WPF App + Agent 进程下完成手动验证（需 NotifyIcon 系统托盘支持，自动化测试无法完全覆盖）。

### 准备工作

1. 确认 build 和 test 通过：
   ```powershell
   dotnet build QuantifiedSelf.Windows.sln
   dotnet test QuantifiedSelf.Windows.sln
   ```
2. 启动 WPF App：`dotnet run --project src\QuantifiedSelf.Windows.App`
3. App 启动后点击 **Start Agent**，确认 Agent 进入 Running 状态。
4. **CloseToTray** 和 **MinimizeToTray** 默认为 `true`（AppSettings 内置默认值，当前 Settings UI 未暴露这两个开关）。

### 验收表

| 编号 | 结果 | 场景 | 操作步骤 | 预期结果 | 备注 |
|---|---|---|---|---|---|
| 1 | [x] | App 启动后托盘图标出现 | 1. 启动 WPF App | 系统托盘出现 WUJI 图标（使用默认应用图标） | 通过 |
| 2 | [x] | 托盘 tooltip 显示 Agent 状态 | 1. Agent Running 状态下 2. 鼠标悬停在托盘图标上 | tooltip 显示 `WUJI` `Agent: Running` `IPC: NamedPipe`（不超过 63 字符） | 通过。Stopped 时显示 Agent: Stopped / IPC: FileFallback；Running 时显示 Agent: Running / IPC: NamedPipe |
| 3 | [x] | 点击窗口 X 后主窗口隐藏 | 1. Agent Running 2. 确认 Settings 中 CloseToTray 为 true 3. 点击窗口右上角 X | 主窗口隐藏到托盘，任务栏不显示窗口；App 进程仍在运行；Agent 继续运行（进程未退出） | 通过 |
| 4 | [x] | 隐藏后托盘状态仍更新 | 1. 窗口已隐藏到托盘 2. 等待 10-20 秒 3. 鼠标悬停托盘图标 | tooltip 仍显示 Agent 最新状态（如 Running），不卡在旧状态；主窗口隐藏期间采样继续 | 通过 |
| 5 | [x] | 托盘 Show 恢复主窗口 | 1. 窗口隐藏到托盘 2. 右键托盘图标 → Show Main Window（或双击图标） | 主窗口重新显示；如果之前最小化，恢复为 Normal 状态；窗口被激活到前台 | 通过。双击托盘图标恢复窗口正常 |
| 6 | [x] | 最小化窗口后隐藏到托盘 | 1. Agent Running 2. 确认 MinimizeToTray 为 true 3. 点击窗口最小化按钮 | 主窗口隐藏到托盘而不是最小化到任务栏；App 继续运行，Agent 不受影响 | 通过 |
| 7 | [x] | 托盘 Start Agent 可启动 | 1. Agent 处于 NotRunning 状态 2. 右键托盘图标 3. 点击 Start Agent | Agent 启动；若干秒后主窗口状态栏显示 Running；托盘 tooltip 随之更新 | 通过 |
| 8 | [x] | 托盘 Pause / Resume 切换 | 1. Agent Running → 托盘右键 → Pause Agent 2. 观察状态变化 3. Agent Paused → 托盘右键 → Resume Agent | Pause 后状态变为 Paused，Resume 可用、Pause 禁用；Resume 后恢复 Running；tooltip 同步更新 | 通过。tooltip 同步更新 |
| 9 | [x] | 托盘 Stop 可停止 Agent | 1. Agent Running 2. 托盘右键 → Stop Agent | Agent 正常退出；主窗口状态栏显示 NotRunning/Stale；UI 不崩溃、不卡死 | 通过。主窗口状态栏显示 Stopped |
| 10 | [x] | Agent 停止后托盘显示 NotRunning | 1. Agent 已停止（第 9 步之后） 2. 鼠标悬停托盘图标 | tooltip 显示 `Agent: NotRunning` 或 `Agent: Stale`，IPC 显示 `FileFallback` 或 `unavailable`；Start 菜单项可用 | 通过。tooltip 显示 Agent: Stopped / IPC: FileFallback |
| 11 | [x] | Agent 重启后托盘显示 Running | 1. Agent 已停止（第 10 步之后） 2. 托盘右键 → Start Agent 3. 等待 Agent 启动 | 约 2-5 秒后 tooltip 从 NotRunning/Stale 变为 `Agent: Running`，IPC 从 FileFallback/unavailable 恢复为 `NamedPipe` | 通过。tooltip 恢复为 Agent: Running / IPC: NamedPipe |
| 12 | [x] | Maintenance 中托盘命令禁用 | 1. Agent Running 2. Settings → Prune Old Data 3. 右键托盘图标 | Maintenance 期间 Start/Pause/Resume/Stop 全部灰色禁用；Maintenance 结束后自动恢复可用；可在 Diagnostics 页确认 Maintenance→Running 转换 | 通过。Maintenance 较短，结合 PruneData/Diagnostics 观察与自动化 `TrayMenuState_DisablesCommandsDuringMaintenance` 覆盖确认 |
| 13 | [x] | Stale+进程不在：Start 可用 | 1. 准备残留状态文件：Agent Running → 先关 App → 再关 Agent 2. 重新启动 App 3. 右键托盘图标 | 状态栏显示 Stale；托盘 Start Agent 菜单可用（非灰色）；点击可启动 Agent | 通过 |
| 14 | [x] | Stale+进程在：Start 不可用、Stop 可用 | 1. Agent 处于 Stale 但进程仍运行（可等 2 秒轮询超时产生 Stale） 2. 右键托盘图标 | Start Agent 禁用（灰色）；Stop Agent 可用；Pause/Resume 禁用 | 通过。Resume 也不可用（符合预期） |
| 15 | [x] | Settings dirty 不覆盖编辑 | 1. Settings → Agent Options 编辑任意字段（如修改数值） 2. 不保存 3. 等待 2-5 秒让托盘状态更新 4. 切回 Settings 检查编辑内容 | 编辑内容保持未保存状态；IsDirty 标记不变；托盘状态和按钮可用性仍正常更新，不影响 Settings 草稿 | 通过。编辑后隐藏到托盘，双击恢复后编辑内容仍在 |
| 16 | [x] | 托盘 Exit App 不停止 Agent | 1. Agent Running 2. 托盘右键 → Exit App | WPF App 退出（进程消失）；Agent 进程仍独立运行；**不会**弹出任何 Stop Agent 确认 | 通过 |
| 17 | [x] | Exit App 后 Agent 不被误停 | 1. 第 16 步执行后 2. 打开任务管理器或 `tasklist \| findstr Agent` 检查 Agent 进程 | Agent 进程仍在运行，PID 不变；Agent 继续采集（可查看 agent_events JSONL 确认新数据写入） | 通过。Agent 仍在正常采样 |
| 18 | [x] | Exit App 后托盘图标不残留 | 1. 第 16 步执行后 2. 观察系统托盘区域 | 托盘图标立即消失，不残留僵尸图标；不需要手动鼠标悬停刷新 | 通过。无残留图标 |
| 19 | [x] | Show 从 Minimized 恢复 Normal | 1. 窗口隐藏到托盘（第 3 步） 2. 在 App 隐藏前先最小化窗口 3. 托盘右键 → Show Main Window | 窗口恢复显示且 WindowState=Normal（不保持 Minimized）；窗口被 Activate 带到前台（尽力即可，不强制 TopMost） | 通过。双击后恢复到启动时大小 |
| 20 | [x] | Shutdown 期间菜单点击不崩溃 | 1. Agent 任意状态 2. App 正在退出（快速连点托盘 Exit 两次或 Exit 后立刻点其他菜单项） | 不抛出未处理异常；Dispatcher shutdown 提示不显示到用户；App 安静退出 | 通过。快速连续 Exit / 退出期间操作未出现未处理异常；静态复核已验证 TryPostToDispatcher shutdown guard |

> **说明**：以上 20 项均需系统托盘、真实 WPF Window、真实 Agent 进程和 IPC 通道才能验证，无法在单元测试中有效模拟。自动化侧已通过 FakeTrayIconAdapter/FakeTrayStateSink 覆盖了托盘状态模型、命令回调链路、生命周期协调器逻辑，但不替代真实桌面环境的端到端验收。

## 长跑验证

以下项目已在实际桌面环境中完成长跑验证。

| 编号 | 结果 | 场景 | 操作步骤 | 预期结果 | 备注 |
|---|---|---|---|---|---|
| L1 | [x] | 15-30 分钟稳定运行 | 1. Agent Running 2. 窗口隐藏到托盘 3. 保持 15-30 分钟 4. 期间不定期悬停托盘图标查看 tooltip | 状态不漂移（Running 始终 Running）；不刷屏（无异常通知弹窗）；tooltip 始终不显示路径/SID/pipe name | 通过。隐藏到托盘长跑期间状态稳定，无异常通知刷屏，tooltip 未泄露敏感信息 |
| L2 | [x] | Pause/Resume 循环 3-5 次 | 1. 托盘右键 → Pause → 等待 Paused → Resume → 等待 Running 2. 重复 3-5 次 | 每次切换 tooltip 和菜单状态正确更新；Pause 后 Pause 禁用 Resume 可用，Resume 后反之；无状态漂移 | 通过。Pause/Resume 多轮循环后菜单和 tooltip 状态一致 |
| L3 | [x] | Stop/Start 循环 1-2 次 | 1. 托盘右键 → Stop → 等待 Agent 退出 → Start → 等待 Running 2. 重复 1-2 次 | Stop 后 tooltip 正确显示 NotRunning/Stale；Start 后恢复到 Running + NamedPipe；IPC fallback 恢复路径正常 | 通过。Stop/Start 循环后 IPC fallback 能恢复到 NamedPipe |
| L4 | [x] | Agent 中途重启 | 1. Agent Running，窗口隐藏到托盘 2. 手动终止 Agent 进程（任务管理器） 3. 等待 10-20 秒看托盘状态变化 4. 托盘右键 → Start Agent | Agent 被终止后 tooltip 自动变为 Stale/NotRunning，IPC 降级；重新 Start 后恢复正常 | 通过。Agent 中途终止后托盘降级为 Stale/NotRunning，重新 Start 后恢复 Running |
| L5 | [x] | 长期隐藏后状态更新 | 1. Agent Running 2. CloseToTray/MinimizeToTray 隐藏窗口 3. 等待 5-10 分钟不操作 4. 托盘 Show 恢复窗口 | 窗口恢复后状态与托盘一致；status polling 持续运行未中断；Dashboard 数据持续更新 | 通过。长期隐藏后 Show 恢复，主窗口状态与托盘一致，status polling 未中断 |

> **状态**：已完成。真实桌面环境下长跑验证通过。

### 执行记录

1. **执行顺序**：编号 1→20 按依赖关系执行（7→8→9→10→11 是完整 Stop/Start 周期）。
2. **记录方式**：每完成一项将表格中 `[ ]` 改为 `[x]`，备注中记录实际表现。
3. **异常记录**：本轮手动验收未发现阻塞性异常；若后续复测出现差异，应记录实际表现、截图和 Diagnostics 页的 IPC 状态 / RefreshHealth 信息。
4. **长跑验证**：手动验收 1-20 全部通过后执行，L1-L5 已完成并记录通过。

## 明确未做

- [ ] 开机自启 MVP
- [ ] 安装包 / 发布体验
- [ ] 真正 Named Pipe 状态流或事件订阅
- [ ] 最近 7 天趋势和图表
- [ ] 应用分类
- [ ] 分页 SamplesView
- [ ] 数据导出 CSV
- [ ] 本地数据备份与恢复
- [ ] 本地数据加密
- [ ] PipeSecurity / ACL 强化
- [ ] ClearHistory confirmation token
- [ ] WPF Runtime Smoke Test 自动化
- [ ] 托盘 Exit App + Stop Agent 组合操作
- [ ] 托盘复杂通知中心 / toast / balloon
- [ ] 托盘直接执行 PruneData / ClearHistory
- [ ] 托盘显示完整 Diagnostics
- [ ] 托盘新增专用状态轮询
- [ ] 托盘专用 Settings UI 配置项

## 收口结论

自动化验收全部通过（268/268 test，0 warn/0 error），静态风险检查 14 项全部 PASS，托盘状态模型与命令回调链路已有完整单元测试覆盖，前序阶段 7/8/9 测试无回归。

手动验收和长跑验证已在实际桌面环境中完成（系统托盘 + 真实 Agent 进程），阶段 10 可收口。

```
阶段 10 托盘图标与后台运行 MVP 自动化验收通过，
静态风险复核通过，手动验收与长跑验证通过，
阶段 10 可以收口。
```
