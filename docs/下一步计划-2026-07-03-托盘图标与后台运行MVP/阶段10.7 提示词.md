请在仓库 `C:\Users\WangZhenchi\work\WUJI\Win` 中实施阶段 10.7。

请先阅读并严格遵循这些文档，中文文档用 UTF-8 读取：

1. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP.md`
2. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\README.md`
3. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\07-阶段10.7-手动验收长跑验证与收口.md`
4. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\阶段10.1-完成说明-2026-07-03.md`
5. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\阶段10.2-完成说明-2026-07-03.md`
6. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\阶段10.3-完成说明-2026-07-03.md`
7. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\阶段10.4-完成说明-2026-07-03.md`
8. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\阶段10.5-完成说明-2026-07-03.md`
9. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\阶段10.6-完成说明-2026-07-03.md`
10. `docs\下一步计划-2026-07-01-Agent状态流订阅RefreshService优化MVP\阶段9-完成说明-2026-07-03.md`
11. `docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8-完成说明-2026-07-01.md`
12. `docs\下一步计划-2026-06-24-PruneDataClearHistory数据清理MVP\阶段7-验收清单-2026-06-25.md`

任务目标：

实施“阶段 10.7：手动验收、长跑验证与阶段 10 收口”。

本阶段不是新增业务功能阶段，而是完成阶段 10 的自动化验收复核、必要补测、手动验收清单、长跑验证和收口文档。请保持改动克制，不要借收口阶段扩展新的托盘功能或后续候补功能。

尤其注意本阶段不做：

- 不做开机自启 MVP。
- 不做安装包 / 发布体验。
- 不做真正 Named Pipe 状态流或事件订阅。
- 不做最近 7 天趋势和图表。
- 不做应用分类。
- 不做分页 SamplesView。
- 不做数据导出 CSV。
- 不做本地数据备份与恢复。
- 不做本地数据加密。
- 不做 PipeSecurity / ACL 强化。
- 不做 ClearHistory confirmation token。
- 不做 WPF Runtime Smoke Test 自动化。
- 不做托盘 Exit App + Stop Agent 组合操作。
- 不新增托盘状态轮询。
- 不改 Named Pipe IPC 协议。
- 不改 AgentStateMachine 业务语义。
- 不改 PruneData / ClearHistory 规则。
- 不改 ClearHistory 的 `CLEAR` 二次确认。
- 不回退阶段 9.6 的 Stale + IsRunning=false 时 Start 可用修复。
- 不改阶段 10.1-10.6 已实现的托盘语义，除非发现明确 bug 且测试证明修复。

需要完成的工作：

1. 自动化验收复核。

请确认并在验收清单中记录以下自动化覆盖：

```text
dotnet build 0 warning / 0 error
dotnet test 全部通过
TrayService 单元测试通过
CloseToTray / MinimizeToTray 测试通过
托盘菜单状态与 AgentCommandAvailability 一致
托盘命令复用现有控制服务
阶段 9 RefreshService / status polling 测试不回归
阶段 8 IPC 测试不回归
阶段 7 PruneData / ClearHistory 测试不回归
```

如果发现某一项没有测试支撑，请优先补少量低风险测试；不要为了补测大规模重构。

如果某项只能手动验证，或补自动化测试需要引入真实 WPF / NotifyIcon / Agent 进程依赖，则不要硬补复杂测试；在验收清单中标为手动验证项，并说明原因。

2. 断连 / 异常场景复核。

阶段 10.6 已覆盖的异常场景请确认测试存在或手动验证：

```text
Agent NotRunning
tooltip 显示 NotRunning
Start 可用

Stale + IsRunning=false
Start 可用，Stop 禁用

Stale + IsRunning=true
Stop 可用，Start 禁用

IPC unavailable / fallback
安全文案可见，不泄露 full pipe name / SID / 路径
恢复后重新显示 NamedPipe

Maintenance
控制命令禁用
结束后恢复

状态恢复
NotRunning / Stale -> Running / Paused
MainWindow 和托盘状态不漂移
```

已有自动化覆盖就写入验收清单并标注对应测试名；缺口只补最关键、最容易自动化、不会拖慢测试的场景。

如果某项只能手动验证，或补自动化测试需要引入真实 WPF / NotifyIcon / Agent 进程依赖，则不要硬补复杂测试；在验收清单中标为手动验证项，并说明原因。

3. 风险复核。

请做静态复核并在完成说明中写明结论：

```text
CloseToTray / MinimizeToTray 是否只 Hide，不停止 status polling。
Exit App 是否才 StopStatusPolling + Dispose TrayService + Shutdown。
Exit App 是否不默认 Stop Agent，不发送任何 Agent 命令。
TrayService 是否不直接实现 Agent 控制逻辑。
托盘菜单状态是否来自 AgentCommandAvailability。
托盘是否不新增状态轮询。
隐藏窗口后是否不改变阶段 9 页面自动刷新语义。
Settings IsDirty 是否不被托盘状态更新覆盖。
托盘错误展示是否不泄露 FullPipeName / SID / 路径。
NotifyIcon Dispose 是否幂等，不残留托盘图标。
NotifyIcon Dispose 前是否先设置 Visible=false。
图标资源是否使用简单 fallback，不因 .ico 资产阻塞托盘生命周期。
Show Main Window 是否只做 Show + WindowState=Normal + Activate。
Dispatcher shutdown / window released 时托盘回调是否安全忽略。
```

4. 生成阶段 10 验收清单。

新增文件：

```text
docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\阶段10-验收清单-YYYY-MM-DD.md
```

格式可参考：

- `docs\下一步计划-2026-06-24-PruneDataClearHistory数据清理MVP\阶段7-验收清单-2026-06-25.md`
- `docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8-验收清单-2026-07-01.md`
- `docs\下一步计划-2026-07-01-Agent状态流订阅RefreshService优化MVP\阶段9-验收清单-YYYY-MM-DD.md`（如果存在）

验收清单建议结构：

```text
# 阶段 10 验收清单

更新时间：YYYY-MM-DD

`dotnet build` 0 warnings / 0 errors，`dotnet test` N/N passed。

## 自动化验收
### 托盘基础设施
### CloseToTray / MinimizeToTray
### 托盘状态展示
### 托盘基础控制命令
### 异常状态与恢复
### 安全与脱敏
### 前序阶段不回归

## 静态检查

## 手动验收

## 长跑验证

## 明确未做

## 收口结论
```

自动化验收项可以根据实际测试结果打 `[x]`，并尽量标注测试名。

手动验收项如果没有实际执行，请保留 `[ ]`，不要打勾。建议至少包含：

```text
1. App 启动后托盘图标出现。
2. 托盘 tooltip 显示 Agent 状态。
3. 点击窗口 X 后主窗口隐藏，App 不退出，Agent 不停止。
4. 隐藏到托盘后 status polling 继续运行，托盘状态仍更新。
5. 托盘 Show 恢复主窗口。
6. 最小化窗口后隐藏到托盘。
7. 托盘 Start Agent 可启动 Agent。
8. 托盘 Pause / Resume 可切换状态。
9. 托盘 Stop 可停止 Agent，UI 不崩。
10. Agent 停止后托盘显示 NotRunning / fallback。
11. Agent 重启后托盘显示 Running / IPC connected。
12. Maintenance 中托盘控制命令禁用。
13. Stale + IsRunning=false 时托盘 Start 可用。
14. Stale + IsRunning=true 时托盘 Start 不可用、Stop 可用。
15. Settings dirty 时托盘状态更新不覆盖编辑内容。
16. 托盘 Exit App 退出 WPF App，但不默认停止 Agent。
17. Exit App 后 Agent 进程仍按退出前状态存在，不被托盘 Exit 误停。
18. Exit App 后托盘图标不残留。
19. Show Main Window 从 Minimized 恢复为 Normal 并尽力 Activate，不使用复杂前台窗口 hack。
20. Exit / Shutdown 期间点击托盘菜单不会因 Dispatcher 已 shutdown 或窗口已释放而崩溃。
```

长跑验证如果没有实际执行，也保留 `[ ]`，并填写 `Pending` / `Not Run`：

```text
运行 15-30 分钟，托盘状态不漂移、不刷屏、不泄露敏感信息。
Pause / Resume 循环 3-5 次。
Stop / Start 循环 1-2 次。
中途关闭 / 重启 Agent，托盘状态自动恢复。
CloseToTray / MinimizeToTray 后等待 5-10 分钟，状态仍更新。
```

如果无法实际进行 15-30 分钟人工长跑，不要模拟或假称完成；只生成待执行清单，并保持未执行项未勾选。

5. 生成阶段 10 完成说明。

新增文件：

```text
docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\阶段10-完成说明-YYYY-MM-DD.md
```

建议包含：

- 阶段 10 总目标
- 10.1 到 10.7 各阶段完成摘要
- 最终托盘语义：

```text
CloseToTray / MinimizeToTray: 只 Hide，不停止 status polling，不 Dispose TrayService，不 Shutdown。
Exit App: StopStatusPolling + Dispose TrayService + Shutdown，不默认 Stop Agent。
托盘状态: 复用阶段 9 AgentStatusSnapshot / AgentCommandAvailability。
托盘命令: Start / Pause / Resume / Stop 复用现有控制服务。
```

- 安全与隐私复核
- 自动化验证结果
- 手动验收待办或已验收情况
- 长跑验证待办或已验证情况
- 明确未做事项
- 阶段 10 是否满足收口条件：是 / 否 / 部分满足，并列出依据
- 后续候补建议（只列出候选方向，不自行决定进入下一阶段）

6. 如需补测试，注意边界。

可改动文件：

```text
tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs
必要时小幅调整 App / Agent 代码以修复真实缺陷
```

不要为了文档收口修改核心控制语义。若发现严重问题，请修最小闭环并在完成说明里记录。

建议重点查看这些文件：

```text
src\QuantifiedSelf.Windows.App\Services\TrayService.cs
src\QuantifiedSelf.Windows.App\Services\TrayMenuState.cs
src\QuantifiedSelf.Windows.App\Services\AgentCommandAvailability.cs
src\QuantifiedSelf.Windows.App\Services\AgentStatusService.cs
src\QuantifiedSelf.Windows.App\Services\AgentIpcStatusService.cs
src\QuantifiedSelf.Windows.App\Services\RefreshService.cs
src\QuantifiedSelf.Windows.App\Services\AgentProcessService.cs
src\QuantifiedSelf.Windows.App\Services\AgentControlService.cs
src\QuantifiedSelf.Windows.App\ViewModels\MainWindowViewModel.cs
src\QuantifiedSelf.Windows.App\App.xaml.cs
src\QuantifiedSelf.Windows.App\MainWindow.xaml.cs
tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs
```

请完成必要修改后运行：

```powershell
dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\build-output\
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\test-output\
```

请不要并行运行 build 和 test，避免 obj 文件锁。

完成后清理临时输出：

```powershell
$root = (Resolve-Path '.').Path
Get-ChildItem -Path src,tests -Recurse -Directory -Filter .codex | ForEach-Object {
    $full = $_.FullName
    if (-not $full.StartsWith($root, [System.StringComparison]::OrdinalIgnoreCase)) { throw "Refusing to remove outside workspace: $full" }
    Remove-Item -LiteralPath $full -Recurse -Force
}
```

完成说明和验收清单中请写入真实 build/test 数字。

如果阶段 10.7 补了测试，需要写明测试总数变化，例如 `260/260 passed`，以及新增 / 修改测试名。

注意：

- `Get-Content` 读取中文文档时使用 `-Encoding UTF8`。
- 不要回滚用户或其他 agent 的改动。
- 手工编辑文件请使用 `apply_patch`。
- 不要把未执行的手动验收项标成通过。
- 不要把未执行的长跑验证项标成通过。
- 不要把后续候补功能提前做进来。
- 完成说明中不需要自行判断是否进入下一阶段；只写事实、验证结果、未做事项、风险与遗留问题。

补充要求：

```text
1. 完成说明中不要写“建议进入下一阶段”；改为记录“阶段 10 是否满足收口条件：是 / 否 / 部分满足”，并列出依据。
2. 如果某项只能手动验证，或补自动化测试需要引入真实 WPF / NotifyIcon / Agent 进程依赖，则不要硬补复杂测试；在验收清单中标为手动验证项，并说明原因。
3. 完成说明请分开列出：已自动化验证、已手动验证、待人工执行、未执行原因。
4. 如果阶段 10.7 补了测试，需要写明测试总数变化和新增 / 修改测试名。
5. 如果无法实际进行 15-30 分钟人工长跑，不要模拟或假称完成；只生成待执行清单，并保持未执行项未勾选。
```
