# 阶段 10.6 完成说明（2026-07-03）

Build: `0 warnings / 0 errors` | Test: `268 / 268 passed`（259 原有 + 9 新增）

## 第一次审核修复（2026-07-03）

提示词逐条复核发现 2 个缺口，已补全（build 0/0, 0 warnings, test 266/266）：

1. 缺 Stale→Running 恢复测试 —— 新增 `TrayStatus_RecoversFromStaleToRunning`
2. 缺 Agent 退出不崩测试 —— 新增 `TrayStatus_DoesNotCrashOnDefaultSnapshot`

## 第二次审核修复（2026-07-03）

审核发现测试未走 ViewModel 真实路径 + 缺 Settings dirty 验证，已修复（build 0/0, 0 warnings, test 268/268）：

1. 补 ViewModel 真实路径测试 —— 新增 `TrayStatus_RecoveryUpdatesTrayStateSinkViaViewModel`：连续 apply Stale→Running，FakeTrayStateSink 的 LastState 从 CanStart=true 恢复到 CanPause/CanStop=true
2. 补 Settings dirty 测试 —— 新增 `TrayStatus_DoesNotOverrideSettingsDrafts`：设置 Settings 草稿后 apply 托盘状态，草稿不被覆盖、IsDirty 保持 true

## 阶段目标回顾

验证托盘在 NotRunning / Stale / IPC fallback / Maintenance 等异常状态下不崩、文案安全、能正确恢复。不新增通知功能。

## 修改的文件

### 测试

- tests/QuantifiedSelf.Windows.Tests/DataFlowTests.cs
  —— 新增 9 个托盘异常状态与恢复测试

## 异常状态展示（TrayMenuState.From 已覆盖）

| 状态 | Tooltip | 可用命令 |
|---|---|---|
| NotRunning | `NotRunning` | Start 可用 |
| Running | `Running` + IPC | Pause/Stop 可用 |
| Paused | `Paused` + IPC | Resume/Stop 可用 |
| Stale+进程在 | `Stale` | Stop 可用 |
| Stale+进程不在 | `Stale` | Start 可用 |
| Maintenance | `Maintenance` | 全部禁用 |
| IPC fallback | `FileFallback` | 按 Agent 状态 |
| IPC unavailable | `unavailable` | 按 Agent 状态 |

## 通知策略

本阶段不实现 toast / balloon 通知。异常状态仅通过 tooltip 和菜单可用性变化展示，不刷屏。

## 新增测试

- TrayStatus_ShowsNotRunningState —— NotRunning 时 tooltip + Start 可用
- TrayStatus_DisablesStartWhenStaleButProcessAlive —— Stale+进程在：Stop 可用、Start 不可用
- TrayStatus_RecoversFromNotRunningToRunning —— NotRunning→Running 状态恢复
- TrayStatus_IpcFallbackRecoversToNamedPipe —— FileFallback→NamedPipe 恢复
- TrayStatus_IpcUnavailableShowsSafeText —— IPC 未知→unavailable，不含路径/SID
- TrayStatus_RecoversFromStaleToRunning —— Stale+无进程→Start 可用，恢复后 Pause/Stop 可用
- TrayStatus_DoesNotCrashOnDefaultSnapshot —— 默认快照不崩，CanStart=true
- TrayStatus_RecoveryUpdatesTrayStateSinkViaViewModel —— ViewModel 路径：连续 apply Stale→Running，sink LastState 正确切换
- TrayStatus_DoesNotOverrideSettingsDrafts —— Settings 草稿不被托盘状态更新覆盖

## 收口

阶段 10.6 可以收口。异常状态覆盖完整，ViewModel 真实路径 + Settings dirty 均已验证。

## 明确没有做的事

- 未做复杂通知中心
- 未做持久化通知历史
- 未写 agent_events
- 未实现 toast / balloon 通知
- 未新增托盘专用状态轮询
- 未改阶段 9 的 2 秒 status polling interval
- 未改阶段 10.5 命令执行链路
