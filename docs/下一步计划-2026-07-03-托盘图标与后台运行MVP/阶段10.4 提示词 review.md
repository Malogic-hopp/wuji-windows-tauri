可行，这份阶段 10.4 提示词**可以交给 agent 执行**。它的定位很准确：只做托盘 tooltip 与菜单可用性绑定，让托盘跟随阶段 9 的 `AgentStatusSnapshot / AgentCommandAvailability`；不提前执行托盘 Start / Pause / Resume / Stop，也不新增托盘专用轮询。这个边界和阶段 10.4 文档一致。

---

# 总体评价

这份提示词已经比较成熟。

它抓住了阶段 10.4 的核心风险：**托盘状态不能和 MainWindow / Settings 漂移**。所以你要求：

```text id="vxlyaz"
托盘状态复用阶段 9 的 status polling
托盘菜单状态来源于 AgentCommandAvailability
TrayService 只渲染 TrayMenuState
不直接读 runtime_state / health_state
不直接查询 SQLite
不新增托盘状态轮询
```

这些约束都很关键。

---

# 我认为写得好的地方

## 1. 先做 `TrayMenuState` 纯模型是对的

你明确要求：

```text id="95qykc"
TrayMenuState 是纯模型，不依赖 NotifyIcon
TrayMenuState.From(...) 使用 AgentCommandAvailability
```

这非常好。托盘 UI 本身难测，先把状态推导做成纯模型，可以稳定覆盖 Running / Paused / Maintenance / Stale 等状态。

---

## 2. 没有提前做命令执行

你写得很清楚：

```text id="oh1v1j"
在 Show / Exit 之外新增 Start / Pause / Resume / Stop 菜单项，
但本阶段只绑定可用性，不绑定执行命令。
```

这是正确边界。10.4 只让菜单“看起来对”，10.5 再让菜单“点了能执行”。

---

## 3. 安全展示要求完整

你明确禁止 tooltip / menu 显示：

```text id="w7d6hx"
FullPipeName
SID
本机路径
SQL
异常原文
原始 JSON
```

并要求 tooltip 截断，这很必要。托盘 tooltip 是常驻信息，不应该暴露诊断细节。

---

# 建议补强 1：不要让 disabled 托盘菜单项触发回调

虽然 10.4 不执行 Agent 命令，但如果提前创建 Start/Pause/Resume/Stop 菜单项，建议加一句：

```text id="l2cqzu"
本阶段 Start / Pause / Resume / Stop 菜单项可以存在但不绑定执行回调；即使被点击，也不得调用任何 Agent 命令或 command bridge。
```

这样能防止 agent 给菜单项加了空壳后顺手接 command。

---

# 建议补强 2：`TrayMenuState` 不要直接依赖 `AgentIpcStatusService`

你现在写的是 `TrayMenuState.From(status, availability, ipcStatus)`，这很好。建议再明确：

```text id="ecfp11"
TrayMenuState.From 接收安全 IPC 展示文本或轻量 snapshot，不直接依赖 AgentIpcStatusService 实例。
```

这样 `TrayMenuState` 仍然是纯模型，不会变成服务组合器。

---

# 建议补强 3：状态更新路径要避免重复跨线程调用

你已经要求通过 Dispatcher。建议补一句：

```text id="rj27yl"
MainWindowViewModel 应只在已应用最新状态后更新 TrayMenuState；被 latest-wins 拒绝的旧状态不得刷新托盘菜单。
```

否则可能出现 MainWindow 拒绝了旧状态，但托盘仍被旧状态覆盖。

这是阶段 9 latest-wins 在托盘上的延伸。

---

# 建议补强 4：IPC 状态文案来源要安全且稳定

建议明确：

```text id="96nn82"
IpcStatusText 优先复用 Diagnostics 已有的 safe IPC status / source 文案；不要为了托盘重新暴露 DisplayPipeName 或 LastIpcError 原文。
```

托盘不需要显示 pipe name，只要显示 NamedPipe / FileFallback / unavailable 这类短句即可。

---

# 建议补强 5：tooltip 截断不能破坏核心状态

你要求 63 字符以内，这很好。建议再加：

```text id="ryb7ar"
截断时优先保留应用名和 Agent 状态；IPC 状态可简化为 IPC: NamedPipe / FileFallback / unavailable。
```

避免 tooltip 被截断后只剩半截错误文本。

---

# 建议补强 6：如果 10.2 adapter 没有菜单项 key，需要先补 key

10.4 要更新菜单项 Enabled。建议补一句：

```text id="w9l6xx"
TrayService / adapter 应使用稳定菜单项标识更新 Start/Pause/Resume/Stop/Show/Exit；不要依赖菜单项显示文本查找。
```

否则后续改文案会破坏菜单状态更新。

---

# 可直接追加到提示词里的补充段

建议你把下面这段加到“注意”或“状态更新路径”后面：

```text id="2d3j07"
补充要求：

1. 本阶段 Start / Pause / Resume / Stop 菜单项可以存在但不绑定执行回调；即使被点击，也不得调用任何 Agent 命令、AgentControlService、AgentProcessService 或 command bridge。
2. TrayMenuState.From 接收安全 IPC 展示文本或轻量 snapshot，不直接依赖 AgentIpcStatusService 实例，保持纯模型。
3. MainWindowViewModel 只在状态结果通过 latest-wins 并已应用到 MainWindow 后更新 TrayMenuState；被拒绝的旧状态不得刷新托盘菜单。
4. IpcStatusText 优先复用已有 safe IPC status/source 文案，不显示 DisplayPipeName、FullPipeName、SID 或 LastIpcError 原文。
5. Tooltip 截断时优先保留应用名和 Agent 状态，IPC 状态使用短句，例如 IPC: NamedPipe / FileFallback / unavailable。
6. TrayService / adapter 应使用稳定菜单项标识更新 Start/Pause/Resume/Stop/Show/Exit，不要依赖菜单项显示文本查找。
```

---

# 最终结论

这份阶段 10.4 提示词**可行，可以执行**。

我建议只补上三点后直接给 agent：

```text id="nj42bf"
1. 10.4 菜单项只绑定可用性，不绑定执行回调。
2. 托盘更新必须走 latest-wins 后的已应用状态。
3. IPC 状态只用安全短句，不暴露 pipe / SID / error 原文。
```

其余内容已经足够清楚。完成 10.4 后，再进入 10.5 接入托盘 Start / Pause / Resume / Stop 命令执行。
