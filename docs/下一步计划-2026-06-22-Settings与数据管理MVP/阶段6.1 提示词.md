你现在在仓库：

C:\Users\WangZhenchi\work\WUJI\Win

请实施阶段 6.1：Settings 读取与展示。

先阅读这些文档，注意中文文档用 UTF-8 读取：

C:\Users\WangZhenchi\work\WUJI\Win\docs\下一步计划-2026-06-22-Settings与数据管理MVP.md
C:\Users\WangZhenchi\work\WUJI\Win\docs\下一步计划-2026-06-22-Settings与数据管理MVP\README.md
C:\Users\WangZhenchi\work\WUJI\Win\docs\下一步计划-2026-06-22-Settings与数据管理MVP\01-阶段6.1-Settings读取与展示.md

阶段 6.1 的目标是只做 read-only Settings 页面，不做编辑、不做保存、不做 ReloadConfig。

需要重点查看现有代码：

C:\Users\WangZhenchi\work\WUJI\Win\src\QuantifiedSelf.Windows.App\Services\SettingsService.cs
C:\Users\WangZhenchi\work\WUJI\Win\src\QuantifiedSelf.Windows.Core\Options\AppSettings.cs
C:\Users\WangZhenchi\work\WUJI\Win\src\QuantifiedSelf.Windows.Core\Options\WindowsAgentOptions.cs
C:\Users\WangZhenchi\work\WUJI\Win\src\QuantifiedSelf.Windows.App\ViewModels\MainWindowViewModel.cs
C:\Users\WangZhenchi\work\WUJI\Win\src\QuantifiedSelf.Windows.App\App.xaml.cs
C:\Users\WangZhenchi\work\WUJI\Win\src\QuantifiedSelf.Windows.App\MainWindow.xaml
C:\Users\WangZhenchi\work\WUJI\Win\tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs

请按现有架构和阶段 5 的做法实现：

1. 新增独立的 `SettingsView.xaml` / `SettingsView.xaml.cs`。
2. 新增 `SettingsViewModel`。
3. Settings 页面展示 App Settings 和 Agent Options 的只读信息。
4. 展示配置文件路径、运行时目录、日志目录等有用路径信息。
5. 加载失败时不要把原始异常消息直接显示到 UI，复用 `DiagnosticMessageSanitizer` 做安全文本。
6. `MainWindow.xaml` 中 Settings Tab 改为承载独立 `SettingsView`，不要继续膨胀 MainWindow。
7. `MainWindowViewModel` 接入 `SettingsViewModel`。
8. 全局 Refresh 语义保持阶段 5.5 的规则：先刷新公共 Agent 状态，再只刷新当前页面。如果当前页是 Settings，则刷新 Settings 数据；非 Settings 页不要顺手刷新 Settings。
9. 保持导航顺序不变：
   Dashboard / Apps / Sessions / Samples / Diagnostics / Settings

测试要求：

1. 补充 SettingsViewModel 加载 App Settings 的测试。
2. 补充 SettingsViewModel 加载 Agent Options 的测试。
3. 补充配置缺失或空配置时使用安全默认值/空态的测试。
4. 补充加载失败时 UI 状态文本不泄露本机路径的测试。
5. 如修改 MainWindowViewModel Refresh 分流，补充或更新相关测试。

验证命令优先使用隔离输出，避免正在运行的 Agent 锁住默认 bin：

dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\build-output\
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\test-output\

完成后清理临时输出：

Get-ChildItem -Path src,tests -Recurse -Directory -Filter .codex | Remove-Item -Recurse -Force

注意事项：

- 不要实现阶段 6.2 之后的编辑、保存、备份、Restore Backup、ReloadConfig。
- 不要实现 PruneData / ClearHistory，这些已经后移到阶段 7。
- 不要新增托盘、主题之类假入口。
- 如果发现工作区已有未提交改动，不要回滚，和现有改动协作。
- 手工编辑文件请使用 apply_patch。
- 完成后用中文说明改动文件、测试结果、以及阶段 6.1 是否可以收口进入 6.2。