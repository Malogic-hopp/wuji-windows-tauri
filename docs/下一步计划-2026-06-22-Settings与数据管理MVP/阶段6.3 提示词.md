你现在在仓库：

C:\Users\WangZhenchi\work\WUJI\Win

请实施阶段 6.3：AgentOptionsValidator 与 Agent Options 编辑。

先阅读这些文档，注意中文文档用 UTF-8 读取：

C:\Users\WangZhenchi\work\WUJI\Win\docs\下一步计划-2026-06-22-Settings与数据管理MVP.md
C:\Users\WangZhenchi\work\WUJI\Win\docs\下一步计划-2026-06-22-Settings与数据管理MVP\README.md
C:\Users\WangZhenchi\work\WUJI\Win\docs\下一步计划-2026-06-22-Settings与数据管理MVP\03-阶段6.3-AgentOptionsValidator与编辑.md

当前阶段 6.1 / 6.2 已完成：
- Settings 已是独立 UserControl + SettingsViewModel
- App Settings 可以编辑保存
- Agent Options 目前仍只读
- MainWindow refresh 语义已保持“只刷新当前页”
- App Settings 保存失败/加载失败已走 DiagnosticMessageSanitizer
- build/test 上一轮结果：66/66 passed

阶段 6.3 目标：

1. 增加共享 `AgentOptionsValidator`。
2. Settings 页面支持编辑 Agent Options。
3. WPF 和 Agent ReloadConfig 后续能共用同一套校验规则。
4. 本阶段只做编辑模型和校验，不做保存 Agent Options 到文件，不做 `.bak`、Restore Backup、ReloadConfig。这些留到 6.4 / 6.5。

请重点查看现有代码：

C:\Users\WangZhenchi\work\WUJI\Win\src\QuantifiedSelf.Windows.Core\Options\WindowsAgentOptions.cs
C:\Users\WangZhenchi\work\WUJI\Win\src\QuantifiedSelf.Windows.App\ViewModels\SettingsViewModel.cs
C:\Users\WangZhenchi\work\WUJI\Win\src\QuantifiedSelf.Windows.App\Views\SettingsView.xaml
C:\Users\WangZhenchi\work\WUJI\Win\src\QuantifiedSelf.Windows.Agent\State\AgentStateMachine.cs
C:\Users\WangZhenchi\work\WUJI\Win\src\QuantifiedSelf.Windows.App\Services\SettingsService.cs
C:\Users\WangZhenchi\work\WUJI\Win\tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs

实施要求：

1. 在共享层新增 `AgentOptionsValidator`，建议位置：
   `src\QuantifiedSelf.Windows.Core\Options\AgentOptionsValidator.cs`

2. Validator 不依赖 WPF，不依赖 Agent，不做文件 IO，只接收 `WindowsAgentOptions` 并返回结构化校验结果。

3. 校验结果建议包含：
   - `IsValid`
   - 错误列表
   - 可供 UI 展示的安全错误文本
   具体形状可以按项目现有风格决定。

4. Validator 至少覆盖这些字段：
   - `samplingIntervalSeconds`
   - `idleThresholdSeconds`
   - `heartbeatIntervalSeconds`
   - `staleThresholdSeconds`
   - `retentionDays`
   - `enableJsonlJournal`
   - `enableAgentEventJournal`
   - `enableSessionMerge`
   - `maskWindowTitles`
   - `excludedProcesses`
   - `excludedTitlePatterns`

5. 数值校验建议按阶段文档来，不确定时采用保守范围，并在代码里集中定义：
   - samplingIntervalSeconds：建议 1..3600
   - idleThresholdSeconds：建议 5..86400
   - heartbeatIntervalSeconds：建议 1..3600
   - staleThresholdSeconds：建议 5..86400，且最好 >= heartbeatIntervalSeconds
   - retentionDays：建议 1..3650
   如果文档已有更具体范围，以文档为准。

6. 隐私规则校验：
   - `excludedProcesses` 允许用户输入 `notepad` 或 `notepad.exe`
   - 保存/预览模型中应归一化为不带 `.exe` 的进程名
   - 大小写比较不敏感
   - 拒绝路径输入，例如 `C:\Windows\System32\notepad.exe`
   - 拒绝空项
   - 去重
   - `excludedTitlePatterns` 去除空项、去重
   - 不要把真实窗口标题引入 UI 或测试数据

7. `SettingsViewModel` 增加 Agent Options 可编辑字段，但本阶段不要保存到文件。
   建议新增：
   - 可编辑 text/bool/list 属性
   - Validate Agent Options 命令
   - Reset Agent Options Editor 命令，恢复为当前已加载的 AgentOptions
   - 归一化预览文本，尤其是 excludedProcesses / excludedTitlePatterns

8. `SettingsView.xaml` 中 Agent Options 区域从只读展示改成编辑表单：
   - 数值字段用 TextBox
   - bool 字段用 CheckBox
   - excludedProcesses / excludedTitlePatterns 用多行 TextBox
   - 增加 Validate Agent Options 按钮
   - 增加 Reset Agent Options 按钮
   - 显示校验状态和归一化预览

9. 本阶段不要实现：
   - Save Agent Options
   - Backup
   - Restore Backup
   - ReloadConfig
   - PruneData / ClearHistory
   - 托盘/主题假入口

10. 注意阶段 6.2 已实现 App Settings 保存，不能回退它。
11. 保存 App Settings 时仍然只保存 App Settings，不要顺手保存 Agent Options。
12. 加载失败、校验失败文本不要包含敏感本机路径。校验文本应来自字段名和值类型，不拼异常原文。

测试要求：

请补充或更新测试，但本轮不需要你执行 build/test。

建议新增测试：

1. `AgentOptionsValidator_AcceptsDefaultOptions`
2. `AgentOptionsValidator_RejectsInvalidNumericRanges`
3. `AgentOptionsValidator_RejectsProcessPaths`
4. `AgentOptionsValidator_NormalizesExcludedProcesses`
5. `AgentOptionsValidator_DeduplicatesPrivacyRules`
6. `SettingsViewModel_ValidatesAgentOptionsEditor`
7. `SettingsViewModel_ResetAgentOptionsEditorRestoresLoadedValues`
8. `SettingsViewModel_SaveAppSettingsDoesNotSaveAgentOptions`

如果你发现文档和现有代码字段名不一致，以 `WindowsAgentOptions` 的真实字段为准，并在完成说明里指出。

注意事项：

- 如果发现工作区已有未提交改动，不要回滚，和现有改动协作。
- 手工编辑文件请使用 apply_patch。
- 完成后用中文说明改动文件、测试覆盖点，以及阶段 6.3 是否建议进入编译测试审核。