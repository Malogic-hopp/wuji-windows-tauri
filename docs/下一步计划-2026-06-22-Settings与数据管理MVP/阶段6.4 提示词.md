你现在在仓库：

C:\Users\WangZhenchi\work\WUJI\Win

请实施阶段 6.4：Save / Backup / Restore 配置文件链路。

先阅读这些文档，注意中文文档用 UTF-8 读取：

C:\Users\WangZhenchi\work\WUJI\Win\docs\下一步计划-2026-06-22-Settings与数据管理MVP.md
C:\Users\WangZhenchi\work\WUJI\Win\docs\下一步计划-2026-06-22-Settings与数据管理MVP\README.md
C:\Users\WangZhenchi\work\WUJI\Win\docs\下一步计划-2026-06-22-Settings与数据管理MVP\04-阶段6.4-SaveBackupRestore配置文件链路.md

当前阶段状态：

- 6.1：Settings 独立只读页已完成。
- 6.2：App Settings 可编辑保存已完成。
- 6.3：`AgentOptionsValidator` 已新增，Agent Options 编辑草稿、Validate、Reset 已完成。
- 当前验证结果：build 0 warning / 0 error，test 75/75 passed。
- `retentionDays` UI 已提示：阶段 6 只保存配置，不立即清理历史数据，实际清理在阶段 7。

阶段 6.4 目标：

让 Settings 页面可以安全保存 `windows-agent.json`，并提供备份与恢复链路。

这阶段只做文件保存、备份、恢复，不做 ReloadConfig，不向 Agent 发送命令，不做数据清理。

请重点查看现有代码：

C:\Users\WangZhenchi\work\WUJI\Win\src\QuantifiedSelf.Windows.App\ViewModels\SettingsViewModel.cs
C:\Users\WangZhenchi\work\WUJI\Win\src\QuantifiedSelf.Windows.App\Views\SettingsView.xaml
C:\Users\WangZhenchi\work\WUJI\Win\src\QuantifiedSelf.Windows.App\Services\SettingsService.cs
C:\Users\WangZhenchi\work\WUJI\Win\src\QuantifiedSelf.Windows.Core\Options\AgentOptionsValidator.cs
C:\Users\WangZhenchi\work\WUJI\Win\src\QuantifiedSelf.Windows.Core\Options\WindowsAgentOptions.cs
C:\Users\WangZhenchi\work\WUJI\Win\src\QuantifiedSelf.Windows.Infrastructure\Settings\WindowsAgentOptionsStore.cs
C:\Users\WangZhenchi\work\WUJI\Win\tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs

实施要求：

1. 在 App 层或 Infrastructure 层新增一个小型配置文件保存服务，或扩展 `SettingsService`，用于安全保存 `windows-agent.json`。
2. 保存 Agent Options 必须先通过共享 `AgentOptionsValidator`。
3. 保存前如果已有 `windows-agent.json`，创建 `.bak` 备份。
4. 写入必须采用临时文件 + 原子替换/移动策略，避免中途失败破坏原文件。
5. 保存失败时：
   - 不破坏旧 `windows-agent.json`
   - UI 显示安全错误文本
   - 不暴露本机敏感路径
6. 新增 Restore Backup 功能：
   - 只恢复 `windows-agent.json.bak` 到 `windows-agent.json`
   - 不自动 ReloadConfig
   - 恢复后 UI 要明确提示“已恢复文件，但运行中的 Agent 还未应用；需后续 ReloadConfig 或下次启动生效”
7. 如果没有 `.bak`，Restore Backup 应给出清晰状态，不崩溃。
8. 保存成功后：
   - 更新 `AgentOptions`
   - 更新编辑器展示
   - 更新归一化预览
   - 状态文本说明“已保存到文件，但运行中的 Agent 还未应用；需后续 ReloadConfig 或下次启动生效”
9. 保持 App Settings 保存逻辑不回退。
10. 不要新增 `windows-agent.pending.json`，本阶段不采用 pending 机制。
11. 不要实现 ReloadConfig，这留到 6.5。
12. 不要实现 PruneData / ClearHistory，这留到阶段 7。
13. 不要新增托盘、主题假入口。
14. 注意 `UseMockCapture` 和 `IdleSummaryIntervalMinutes` 当前不是编辑项，保存 Agent Options 时应保留已加载值，不要丢字段。

UI 要求：

1. 在 Agent Options 区域增加：
   - Save Agent Options
   - Restore Backup
2. 保留已有：
   - Validate Agent Options
   - Reset Agent Options
3. 明确显示 Agent Options 保存状态。
4. 保存/恢复状态必须说明：
   - 保存只是写配置文件
   - 运行中的 Agent 尚未应用
   - 后续需要 ReloadConfig 或下次启动
5. 不要在 UI 中出现 ReloadConfig 按钮。

测试要求：

请补充或更新测试，但本轮不需要你执行 build/test。

建议新增测试：

1. `SettingsViewModel_SavesAgentOptionsWithBackup`
2. `SettingsViewModel_RejectsInvalidAgentOptionsBeforeSave`
3. `SettingsViewModel_SaveAgentOptionsFailureDoesNotCorruptExistingFile`
4. `SettingsViewModel_RestoresAgentOptionsBackup`
5. `SettingsViewModel_RestoreBackupWhenMissingShowsSafeStatus`
6. `SettingsViewModel_SaveAgentOptionsPreservesNonEditableFields`
7. `SettingsViewModel_SaveAgentOptionsDoesNotReloadAgent`
8. 如新增独立保存服务，补充该服务的原子保存/备份测试

注意事项：

- 如果发现工作区已有未提交改动，不要回滚，和现有改动协作。
- 手工编辑文件请使用 apply_patch。
- 完成后用中文说明改动文件、测试覆盖点，以及阶段 6.4 是否建议进入编译测试审核。