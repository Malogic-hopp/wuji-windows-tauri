请在仓库 `C:\Users\WangZhenchi\work\WUJI\Win-ui-tauri` 中实施阶段 4.1。

请先用 UTF-8 完整阅读并严格遵循：

1. 根目录 `AGENTS.md`
2. `docs\rebuild\下一步计划-2026-07-23-Rebuild-v0.1第二轮审核第四阶段整改\README.md`
3. 同目录 `00-第四阶段代码审核报告.md`
4. 同目录 `01-阶段4.1-Settings恢复协议与双槽备份一致性.md`
5. `docs\rebuild\09-Tauri-Rust-Rebuild-v0.1实施基线.md`

任务目标：只实施“Settings 恢复协议与双槽备份一致性”，关闭审核 P1-01。

开始前记录：

```powershell
git status --short
git diff --stat
git rev-parse --short HEAD
```

必须完成：

1. 启动时读取 SQLite 最大 settings revision/digest。
2. 对账同时考虑 DB metadata、settings 文件、双槽候选。
3. DB revision > 0 时禁止回退默认 revision 0。
4. 文件/备份必须与 DB revision/digest 匹配，或是合法严格前滚。
5. 无法恢复 DB 当前 revision 时阻止 Capture，IPC 保持在线并设置安全错误。
6. 让 Settings apply + 双槽备份具备 crash-consistent 顺序；备份失败不得返回无条件成功。
7. 修正 `unrecoverable_last_known_good_blocks_capture` 的错误断言。
8. 增加阶段文档列出的全部确定性测试。

实现时优先复用：

- `Writer::latest_settings_revision_digest()`
- `settings_store.rs`
- `settings_backup.rs`
- 现有 SafeError/ErrorSource 体系

不要：

- 接入或重构 CaptureCoordinator；
- 改 Barrier/Lock/Sleep/effectivity；
- 扩大到 UI、打包或 soak；
- 操作旧 WUJI/WUJI-Dev 数据库；
- commit 或 push。

完成后运行：

```powershell
cd rebuild
cargo fmt --all -- --check
cargo check --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
cargo test -p wuji-storage
cargo test -p wuji-rebuild-agent --lib
git diff --check
```

然后在计划目录新增：

`阶段4.1-完成说明-YYYY-MM-DD.md`

完成说明必须包含 P1-01 根因、恢复决策表、崩溃窗口处理、修改文件、测试名称、
首次/最终结果、明确未做事项、`git status --short`，以及是否建议进入 4.2。

即使测试通过，也只能写“阶段 4.1 完成，等待复审”，不要开始 4.2。

