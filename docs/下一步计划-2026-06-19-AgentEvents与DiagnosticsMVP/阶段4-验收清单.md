# 阶段 4 验收清单：Agent Events 与 Diagnostics MVP

## 当前自动化结论

- [x] `dotnet build QuantifiedSelf.Windows.sln` 通过，0 warning / 0 error。
- [x] `dotnet test QuantifiedSelf.Windows.sln` 通过。
- [x] `agent_events` 表存在，并包含必要索引。
- [x] Recent Events / Recent Errors 使用 SQLite 查询，不依赖 JSONL。
- [x] Recent Events / Recent Errors 查询排序稳定：`event_time_utc DESC, id DESC`。
- [x] JSONL 文件按日期写入：`logs/agent_events_YYYYMMDD.jsonl`。
- [x] AgentEventWriter 写 SQLite 失败时不影响 JSONL 和主流程。
- [x] AgentEventWriter 写 JSONL 失败时不影响 SQLite 和主流程。
- [x] 旧库缺少 `agent_events` 表时，Diagnostics 查询安全返回空列表。
- [x] `CommandInvalidJson` 支持空 `request_id`，并写入 `error_code = CommandInvalidJson`。
- [x] 坏 `agent_control.json` 会被隔离为 `.bad`。
- [x] 命令事件包含 `commandSource = FileFallback`。
- [x] `PrivacyFiltered` 不写原始窗口标题。
- [x] `CaptureFailed` 带稳定 `errorCode`，并按失败阶段区分。
- [x] Windows 路径和 UNC 路径不会进入事件 payload / health state。
- [x] `PrivacyFiltered` 按 `ruleType + processName` 限流。
- [x] `CaptureFailed` 按 `errorCode` 限流。
- [x] Session 变化通过 `SessionStarted` / `SessionClosed` 表达，`ProcessChanged` 写入 `SessionClosed.closeReason`。
- [x] 普通采样不产生 `SampleCaptured` / `Heartbeat` 类事件。
- [x] `AgentEventPayloadSanitizer` 会移除未列入白名单的字段。
- [x] Diagnostics 页面已展示 Recent Events、Recent Errors、事件 SQLite 写入状态、事件 JSONL 写入状态、当前 JSONL 路径、最近写入错误和打开日志目录入口。

## 需要真实环境手动验收

- [x] Agent 启动后，Diagnostics 能看到 `AgentStarted`。
- [x] Pause / Resume / Stop 后，Diagnostics 能看到对应事件。
- [x] 控制命令链路能看到 `CommandDetected` / `CommandAccepted` / `CommandCompleted`。
- [x] 命令失败时，Diagnostics 能看到 `CommandFailed` 和 `errorCode`。
- [x] 手动制造坏 `agent_control.json` 后，Diagnostics 能看到 `CommandInvalidJson`，且原始 JSON 内容不出现在事件里。
- [x] 命中标题隐私规则时，Diagnostics 只显示泛化原因，不显示真实窗口标题。
- [x] 命中进程隐私规则时，不写 foreground sample，且事件不过量刷屏。
- [x] 手动触发或模拟采样异常时，Diagnostics 能看到 `CaptureFailed` 和阶段 `errorCode`，不显示敏感路径。
- [x] 当前 JSONL 路径可以定位到当天日志文件。
- [x] 打开日志目录按钮可用。
- [x] Diagnostics 刷新旧库或空表时不会报错。
- [x] 30 分钟正常运行后，`agent_events` 数量显著小于 `foreground_samples`。
- [x] 30 分钟正常运行后，未出现 `SampleCaptured` / `Heartbeat` 高频事件。
- [x] 30 分钟正常运行后，Diagnostics 页面仍能快速刷新和滚动。

## 已完成手动验收记录

```text
2026-06-21：
依次点击 Start / Pause / Resume / Stop。
Diagnostics Recent Events 中确认：
- AgentStarted 可见。
- Pause 对应 CommandDetected / CommandAccepted / SessionClosed / AgentPaused / CommandCompleted。
- Resume 对应 CommandDetected / CommandAccepted / AgentResumed / CommandCompleted。
- Stop 对应 CommandDetected / CommandAccepted / SessionClosed / AgentStopped / CommandCompleted。
同秒事件按 newest-first 显示，符合 `event_time_utc DESC, id DESC` 的稳定排序。

2026-06-21：
执行 manual-commandfailed-check.ps1 两次。
Diagnostics Recent Errors 中确认：
- 两个不同 requestId 均出现 CommandFailed。
- event_level = Warning。
- errorCode = UnsupportedCommand。
- 同 requestId 可在 Recent Events 中看到 CommandDetected。
- 未出现对应 CommandAccepted / CommandCompleted，符合不支持命令的语义。

2026-06-21：
执行 manual-commandinvalidjson-check.ps1。
Diagnostics Recent Errors 中确认：
- CommandInvalidJson 可见。
- event_level = Warning。
- errorCode = CommandInvalidJson。
- UI 未显示 RequestId，符合 request_id 为空的预期。
- 事件 message 为泛化文案 `Control command JSON invalid`，未展示原始坏 JSON。

2026-06-21：
执行 manual-privacy-title-check.ps1。
Diagnostics Recent Events 中确认：
- PrivacyFiltered 可见。
- message 为泛化文案 `Sample filtered by privacy`。
- UI 中未出现测试标题 `WUJI_TITLE_PRIVACY_CHECK`。
DB Browser 查询 agent_events 确认：
- payload.ruleType = Title。
- payload.processName = Notepad。
- payload.privacyReason = Excluded by title privacy rule。
- payload 中未出现测试窗口标题。

2026-06-21：
执行 manual-privacy-process-check.ps1。
Diagnostics Recent Events 中确认：
- PrivacyFiltered 可见。
- message 为泛化文案 `Sample filtered by privacy`。
- 同一短时间窗口内最多出现 5 条 PrivacyFiltered，符合当前 5 分钟 5 条限流策略。
DB Browser 查询 agent_events 确认：
- payload.ruleType = Process。
- payload.processName = Notepad。
- payload.privacyReason = Excluded by process privacy rule。

2026-06-21：
执行 manual-privacy-process-timed-check.ps1。
严格时间窗：
- start = 2026-06-21T15:54:55.3031366Z。
- end = 2026-06-21T15:55:07.3129457Z。
DB Browser 查询确认：
- `foreground_samples` 中 Notepad 样本数量 = 0。
- `agent_events` 中 PrivacyFiltered 数量 = 5。
- PrivacyFiltered 时间为 2026-06-21T15:54:55 到 2026-06-21T15:55:03，符合 5 分钟 5 条限流策略。

2026-06-22：
采样异常验收采用自动化模拟，不强制真实 UI 手动触发。
原因：
- 当前 Win32 采样实现会把多种前台窗口读取失败降级为 `Unknown` sample，不容易稳定触发真实 `CaptureFailed`。
- 为真实 Agent 增加故障注入开关会扩大产品代码面，本阶段不引入。
自动化覆盖：
- `AgentStateMachine_WritesCaptureFailedEvents_AndRateLimitsThem` 确认 `CaptureFailed` 会写入，并带阶段 `errorCode = ForegroundWindowUnavailable`。
- `AgentStateMachine_SanitizesPathLikeCaptureFailures` 确认带 `C:\Users\...` 和 `\\server\share\...` 的异常消息不会进入事件 payload / health state。

2026-06-22：
Diagnostics 中当前 JSONL 路径和打开日志目录入口验收通过。
确认 Open Logs Folder 可打开：
`C:\Users\WangZhenchi\AppData\Local\WUJI\WindowsAgent\logs`
目录中存在：
- `agent_events_20260621.jsonl`
- `agent_events_20260620.jsonl`

2026-06-22：
Diagnostics 刷新旧库或空表采用自动化验收。
自动化覆盖：
- `DiagnosticsQueryService_ReturnsEmptyListsWhenAgentEventsTableIsEmpty` 确认 `agent_events` 表存在但为空时返回空列表。
- `AgentEventRepository_ReturnsEmptyListsWhenAgentEventsTableIsMissing` 确认旧库缺少 `agent_events` 表时返回空列表，不抛异常。

2026-06-22：
执行 `check-30min-event-volume.py` 查询真实库 30 分钟窗口。
窗口：
- start = 2026-06-21T15:45:56.714861Z。
- end = 2026-06-21T16:15:56.714861Z。
结果：
- `foreground_samples` = 469。
- `agent_events` = 95。
- `agent_events / foreground_samples` = 0.203。
- `SampleCaptured` / `Heartbeat` 事件数 = 0。
事件类型分布：
- `SessionClosed` = 41。
- `SessionStarted` = 41。
- `PrivacyFiltered` = 5。
- `CommandAccepted` = 2。
- `CommandCompleted` = 2。
- `CommandDetected` = 2。
- `ConfigReloaded` = 2。

2026-06-22：
30 分钟正常运行后，Diagnostics UI 体验验收通过。
确认：
- Refresh 响应快速。
- Recent Events / Recent Errors 区域可以流畅滚动。
```

## 长跑记录模板

```text
验证时间：
Agent 运行时长：
foreground_samples 数量：
app_sessions 数量：
agent_events 数量：
Recent Events 是否正常：
Recent Errors 是否正常：
是否出现事件刷屏：
是否出现敏感标题或路径：
备注：
```

## 后续待办边界

- `SamplesView`：进入下一阶段候补。
- `AppsView`：进入下一阶段候补。
- `SessionsView`：进入下一阶段候补。
- `Settings` 编辑能力：继续后置，不并入本阶段。
- `PruneData` / `ClearHistory`：继续后置。
- Named Pipe / gRPC：继续后置，当前命令事件只要求兼容 `commandSource`。
- 托盘、开机自启、安装包、Windows Service：继续后置。

## 口径确认

- 当前命令 payload 正式允许 `requestedBy`、`requestedAtUtc`、`waitForCompletion`、`timeoutMilliseconds`。这些字段不包含窗口标题、路径、命令行或原始 JSON，用于诊断命令来源、请求时间和等待语义；总计划文档已同步加入命令事件 payload 白名单。
