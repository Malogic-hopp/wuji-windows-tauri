请在仓库 `C:\Users\WangZhenchi\work\WUJI\Win` 中实施阶段 11.6。

请先阅读并严格遵循这些文档，中文文档用 UTF-8 读取：

1. `docs\下一步计划-2026-07-04-开机自启MVP.md`
2. `docs\下一步计划-2026-07-04-开机自启MVP\README.md`
3. `docs\下一步计划-2026-07-04-开机自启MVP\06-阶段11.6-手动验收长跑验证与收口.md`
4. `docs\下一步计划-2026-07-04-开机自启MVP\阶段11.1-完成说明-2026-07-04.md`
5. `docs\下一步计划-2026-07-04-开机自启MVP\阶段11.2-完成说明-2026-07-05.md`
6. `docs\下一步计划-2026-07-04-开机自启MVP\阶段11.3-完成说明-2026-07-06.md`
7. `docs\下一步计划-2026-07-04-开机自启MVP\阶段11.4-完成说明-YYYY-MM-DD.md`（如已存在）
8. `docs\下一步计划-2026-07-04-开机自启MVP\阶段11.5-完成说明-YYYY-MM-DD.md`（如已存在）

任务目标：

完成“阶段 11.6：手动验收、长跑验证与收口”。

本阶段是阶段 11 的收尾阶段，不新增功能，核心工作是：

- 运行全量自动化验收，确认 build/test 通过且前期阶段无回归。
- 输出一份《阶段11-验收清单》供人工逐项勾选。
- 输出一份《阶段11-完成说明》总结实现范围、风险复核结论与后续建议。
- 如果发现阶段 11.1–11.5 遗留的明显小 bug（例如文案错误、资源未释放、测试不稳定），可以只做最小修复，并记录到完成说明。

尤其注意本阶段不做：

- 不做安装包、Windows Service、管理员权限自启、计划任务。
- 不做 single-instance 或多实例守卫。
- 不做新的 Named Pipe 协议、状态流订阅、趋势图、应用分类、CSV 导出。
- 不做数据加密、本地备份/恢复、PipeSecurity/ACL。
- 不改阶段 10 CloseToTray / MinimizeToTray / Exit App / 托盘命令语义。
- 不改阶段 9 RefreshService / status polling 语义。
- 不改阶段 8 IPC 协议。
- 不改阶段 7 PruneData / ClearHistory 规则。
- 不新增 Settings UI 功能或 Diagnostics 区域。
- 不新增 WPF Runtime Smoke Test（只输出是否建议后续做）。

执行步骤：

1. 运行自动化验收

使用命令：

```powershell
dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex/build-output/
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex/test-output/
```

要求：

```text
build 0 warning / 0 error
test 全部通过
阶段 11.1 / 11.2 / 11.3 / 11.4 / 11.5 的测试不回归
阶段 10 托盘测试不回归
阶段 9 RefreshService / status polling 测试不回归
阶段 8 IPC 测试不回归
阶段 7 PruneData / ClearHistory 测试不回归
```

如果测试失败，先判断是否属于阶段 11 引入的问题：

- 如果是，且是小范围修复，可以修改。
- 如果不是，记录到完成说明的“已知问题 / 不修复原因”中，不扩大修改范围。
- 如果 build/test 未通过，仍可生成问题记录文档，但不得生成“阶段 11 已完成”式收口结论；完成说明应标记为“未收口 / 待修复”，并列出阻塞项。

2. 生成《阶段11-验收清单》文档

路径：

```text
docs\下一步计划-2026-07-04-开机自启MVP\阶段11-验收清单-YYYY-MM-DD.md
```

`YYYY-MM-DD` 使用执行当天日期；如果执行环境无法确定日期，使用 `2026-07-06`。

内容至少包含：

```text
1. 自动化验收结果（build / test / 回归情况）
2. 手动验收 21 项清单，每项留空 [ ] 供人工勾选
3. 长跑验证 6 项清单，每项留空 [ ] 供人工勾选
4. 验收环境信息（Windows 版本、是否域账户、是否管理员运行、.NET 版本等）
   注意：不得记录用户名、用户目录、机器名、SID、完整项目路径或完整 Run Key command。
5. 备注与发现的问题
```

手动验收 21 项必须保留原文，不要精简。默认必须全部使用 `[ ]`，除非 agent 在真实 Windows 环境中实际执行并能记录具体结果；不得根据自动化测试推断为 `[x]`。

```text
1. 默认安装/启动后 StartAppOnWindowsLogin=false。
2. Settings 中开启 Start WUJI on Windows login 后保存成功。
3. HKCU Run Key 中出现 WUJI value。
4. Run Key command 指向当前 App，并包含 --from-autostart --start-hidden。
5. 关闭 Start WUJI on Windows login 后保存成功。
6. HKCU Run Key 中 WUJI value 被删除。
7. 开启自启后模拟登录启动参数运行 App，主窗口不弹出，托盘图标出现。
8. 自启隐藏后托盘 Show 可恢复主窗口。
9. 自启隐藏后 status polling 继续。
10. AutoStartAgentWhenAppStarts=false 时，自启 App 不启动 Agent。
11. AutoStartAgentWhenAppStarts=true 时，自启 App 启动 Agent。
12. 手动启动 App 仍显示主窗口。
13. 注册表 command 被手动改坏后，Settings / Diagnostics 显示 Mismatch / Needs repair。
14. Save 修复 mismatch 后，状态恢复 Enabled。
15. 注册失败或读失败时显示安全错误，不泄露完整路径或异常原文。
16. Exit App 后托盘图标不残留，Agent 不被误停。
17. CloseToTray / MinimizeToTray 行为不回归。
18. 托盘 Start / Pause / Resume / Stop 不回归。
19. Settings dirty 时等待 status polling，不覆盖编辑草稿。
20. 重启 App 后 Settings 显示的自启状态与 OS 注册状态一致。
21. 自启实例存在时再次手动启动 App，观察是否出现双托盘图标、双窗口或状态漂移；如出现，记录为后续前置风险或最小修复项。
```

重复启动行为请在验收清单中单独预留结论分类：

```text
重复启动结果：
[ ] 未执行
[ ] 无双托盘 / 无双窗口 / 无状态漂移
[ ] 出现双托盘
[ ] 出现双窗口
[ ] 出现状态漂移
处理建议：single-instance 是否提升为下一阶段前置项
```

长跑验证 6 项必须保留原文。默认必须全部使用 `[ ]`，除非 agent 在真实 Windows 环境中实际执行并能记录具体结果；不得根据自动化测试推断为 `[x]`。

```text
1. 启用自启，使用 --from-autostart --start-hidden 启动，隐藏到托盘运行 15-30 分钟。
2. AutoStartAgentWhenAppStarts=true 场景下观察 Agent Running 稳定。
3. AutoStartAgentWhenAppStarts=false 场景下观察 App 常驻但 Agent 不自动启动。
4. Pause / Resume / Stop / Start 从托盘循环 3-5 次。
5. Exit App 后 Agent 不被误停，托盘图标不残留。
6. 再次手动启动 App，主窗口正常显示。
```

3. 风险复核

对照 `06-阶段11.6-手动验收长跑验证与收口.md` 中“风险复核”的 13 项，逐条确认并记录结论：

```text
1. StartAppOnWindowsLogin 与 AutoStartAgentWhenAppStarts 语义独立。
2. HKCU Run Key 注册 / 删除幂等。
3. 禁用自启后 WUJI value 不残留。
4. StartupCommandBuilder 不会注册 dotnet.exe / dll / 空路径。
5. Run Key command 比较使用规范化解析。
6. Settings 保存失败时不显示假启用。
7. Settings dirty 不覆盖 StartAppOnWindowsLogin 草稿，不触发注册表写入。
8. AutoStartHidden 不通过 Window.Closing / CloseToTray 模拟隐藏。
9. AutoStartHidden 不弹阻塞式 MessageBox。
10. Diagnostics 不泄露完整路径 / SID / command / 注册表异常原文。
11. 阶段 10 托盘 CloseToTray / MinimizeToTray / Exit App 不回归。
12. 阶段 8 IPC、阶段 9 RefreshService、阶段 7 数据清理不回归。
13. 重复启动行为已手动验证并记录。
```

4. 生成《阶段11-完成说明》文档

路径：

```text
docs\下一步计划-2026-07-04-开机自启MVP\阶段11-完成说明-YYYY-MM-DD.md
```

`YYYY-MM-DD` 使用执行当天日期；如果执行环境无法确定日期，使用 `2026-07-06`。

完成说明至少包含：

```text
1. 阶段目标
2. 阶段 11.1–11.5 关键实现摘要（引用各子阶段完成说明）
3. 自动化验收结果（build / test / 回归情况）
4. 手动验收结论（21 项是否建议通过，未通过项及原因）
5. 长跑验证结论（6 项是否建议通过，未通过项及原因）
6. 风险复核结论（13 项逐条结论）
7. 修改文件清单（本阶段只修小 bug 时填写，无则写“无”）
8. 已知问题 / 建议后续前置项
9. 后续候补建议排序
10. 收口结论
```

收口结论使用模板：

```text
阶段 11 开机自启 MVP 已完成。

WUJI 支持当前用户登录 Windows 后自动启动；
StartAppOnWindowsLogin 与 AutoStartAgentWhenAppStarts 语义独立；
自启注册使用 HKCU Run Key；
自启启动默认进入托盘，不弹主窗口；
Settings 可启用/禁用并显示真实注册状态；
Diagnostics 可解释 Enabled / Disabled / Mismatch / Error；
阶段 10 托盘、阶段 9 RefreshService、阶段 8 IPC、阶段 7 数据清理测试无回归。
```

完成说明中请分开写：

```text
自动化验收：已完成 / 未完成
人工验收：待人工确认 / 部分完成 / 已完成
长跑验证：待人工确认 / 部分完成 / 已完成
```

如果只有 build/test 通过，但手动验收和长跑未执行，收口结论应写：

```text
阶段 11 已完成代码实现与自动化验收，手动验收和长跑验证待人工确认。
```

如果实际验收未全部通过，不得使用“已完成”措辞，应改为：

```text
阶段 11 开机自启 MVP 已完成代码实现，部分手动/长跑验收项待人工确认。
```

如果 build/test 未通过，不得生成“阶段 11 已完成”式收口结论；完成说明应标记为“未收口 / 待修复”。

5. 后续候补建议排序

在《阶段11-完成说明》中给出后续候补建议，参考 `06-阶段11.6-手动验收长跑验证与收口.md` 中的“后续候补”，但建议按以下优先级呈现：

```text
高优先级：
1. 安装包 / 发布体验
2. 轻量 single-instance + second launch show existing window
3. 正式应用图标资源
4. WPF Runtime Smoke Test 自动化

功能增强：
5. 最近 7 天趋势和图表
6. 应用分类
7. 分页 SamplesView
8. 数据导出 CSV
9. 本地数据备份与恢复
10. 本地数据加密

架构深化：
11. 真正 Named Pipe 状态流或事件订阅
12. PipeSecurity / ACL 强化
13. ClearHistory confirmation token
14. 管理员权限重启 Agent（按需）
15. Startup 文件夹快捷方式替代方案
```

如果第 21 项手动验收（重复启动）已观察到双托盘/双窗口，建议把 single-instance 提升到最优先。

安全与边界要求：

```text
本阶段不引入新功能，只输出文档和做小 bug 修复。
不得修改 HKLM / 计划任务 / Windows Service / 安装包相关代码。
不得在文档或代码中记录完整用户路径、SID、注册表异常原文。
如果手动修改注册表做验证，必须在完成说明中提醒“仅用于开发验证，普通用户不要手动改”。
手动验收改坏 HKCU Run Key 后，验收清单必须包含恢复步骤：通过 Settings 保存修复，或关闭自启删除 WUJI value，确保验收结束后不残留错误注册项。
```

验收标准：

- build 0 warning / 0 error。
- test 全部通过。
- 《阶段11-验收清单》文档已落盘。
- 《阶段11-完成说明》文档已落盘。
- 风险复核 13 项有逐条结论。
- 后续候补建议已排序。
- 不引入阶段 11 范围外的新功能。
- 阶段 10 / 9 / 8 / 7 无回归。

运行命令：

```powershell
dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex/build-output/
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex/test-output/
```

请不要并行运行 build 和 test，避免 obj 文件锁。

如果 build/test 产生 `.codex` 临时输出，请在确认结果后清理，清理前必须确认路径位于当前 workspace 内。

建议清理方式：

```powershell
$root = (Resolve-Path '.').Path
Get-ChildItem -Path src,tests -Recurse -Directory -Filter .codex | ForEach-Object {
    $full = $_.FullName
    if (-not $full.StartsWith($root, [System.StringComparison]::OrdinalIgnoreCase)) { throw "Refusing to remove outside workspace: $full" }
    Remove-Item -LiteralPath $full -Recurse -Force
}
```

注意：

- `Get-Content` 读取中文文档时使用 `-Encoding UTF8`。
- 不要回滚用户或其他 agent 的改动。
- 手工编辑文件请使用 `apply_patch`。
- 手动验收和长跑验证需要真实 Windows 环境，agent 只能输出清单和结论模板，无法替代人工勾选；完成说明中要明确标注哪些项是“待人工确认”。
- 不要把后续阶段的功能提前做进来。
