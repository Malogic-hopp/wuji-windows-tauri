请在仓库 `C:\Users\WangZhenchi\work\WUJI\Win` 中实施阶段 11.5。

请先阅读并严格遵循这些文档，中文文档用 UTF-8 读取：

1. `docs\下一步计划-2026-07-04-开机自启MVP.md`
2. `docs\下一步计划-2026-07-04-开机自启MVP\README.md`
3. `docs\下一步计划-2026-07-04-开机自启MVP\05-阶段11.5-Diagnostics自启状态与错误展示.md`
4. `docs\下一步计划-2026-07-04-开机自启MVP\阶段11.1-完成说明-2026-07-04.md`
5. `docs\下一步计划-2026-07-04-开机自启MVP\阶段11.2-完成说明-2026-07-05.md`
6. `docs\下一步计划-2026-07-04-开机自启MVP\阶段11.3-完成说明-2026-07-06.md`
7. `docs\下一步计划-2026-07-04-开机自启MVP\阶段11.4-完成说明-YYYY-MM-DD.md`（如已存在）

任务目标：

实现“阶段 11.5：Diagnostics 自启状态与错误展示”。

本阶段在阶段 11.4 的登录自启启动到托盘基础上，让 Diagnostics 能解释登录自启当前是否启用、是否 mismatch、最近是否出现注册错误。本阶段只新增只读展示，不修改 Settings UI 的保存逻辑，不新增托盘功能，不改变启动流程。

尤其注意本阶段不做：

- 不把自启状态写入 `agent_events`。
- 不写 JSONL。
- 不做通知中心。
- 不显示完整注册表命令。
- 不改 RefreshService 状态轮询语义。
- 不改 StartupRegistrationService。
- 不改 App 启动流程。
- 不改阶段 11.3 Settings 保存同步逻辑。
- 不做手动验收 / 收口（阶段 11.6）。
- 不做 single-instance。
- 不做安装包、Windows Service、管理员权限自启、计划任务。
- 不改阶段 10 CloseToTray / MinimizeToTray / Exit App / 托盘命令语义。
- 不改阶段 8 IPC 协议。
- 不改 AgentStateMachine 业务语义。
- 不改 PruneData / ClearHistory 规则。
- 不改 ClearHistory 的 `CLEAR` 二次确认。
- 不回退阶段 9.6 的 Stale + IsRunning=false 时 Start 可用修复。

补充要求：

```text
1. LaunchMode 应来自 App 启动时已解析并保存的 StartupLaunchOptions / provider；Diagnostics 不应在刷新时反复调用 Environment.GetCommandLineArgs() 并重新解析。
2. 自启状态只在 Diagnostics 页面刷新、Settings 显式刷新或 App 启动初始化时读取；不要接入 2 秒 status polling 的每次 tick。
3. Diagnostics 刷新自启状态不得修改 SettingsViewModel 的 StartAppOnWindowsLogin 草稿值，也不得触发 Settings IsDirty。
4. 建议新增 StartupRegistrationDisplayModel / StartupRegistrationStatusFormatter 等纯模型，将 Enabled / Disabled / Mismatch / Error / UnsupportedInCurrentLaunchMode 转换为安全展示文案；Diagnostics 和 Settings 复用同一转换。
5. UnsupportedInCurrentLaunchMode 应显示安全摘要，例如 Registration unavailable in current launch mode，不显示当前进程路径。
6. 如果阶段 11.3 已经在 Settings 中展示 StartupRegistrationStatusText，本阶段只复用或微调安全文案，不重构 Settings UI。
```

需要实现的范围：

1. Diagnostics 展示登录自启状态。

建议修改：

```text
src\QuantifiedSelf.Windows.App\ViewModels\MainWindowViewModel.cs
src\QuantifiedSelf.Windows.App\Views\MainWindow.xaml
src\QuantifiedSelf.Windows.App\Services\DiagnosticsDataService.cs（如存在）
```

要求：

```text
在 Diagnostics 页面新增 Login startup 区域，显示：
    Enabled / Disabled / Mismatch / Error / UnsupportedInCurrentLaunchMode
显示 Launch mode：
    Manual / AutoStart
显示 Startup registration 安全摘要：
    Registered to current app
    Registered command needs repair
    Not registered
    Registration unavailable
    Registration unavailable in current launch mode
    Registered command points to a different app location
    Required startup arguments are missing
显示 Last startup registration error：
    安全短句，不包含完整路径、完整 command、注册表异常原文、SID、用户目录、stack trace。
```

2. Settings 状态文本保留。

建议修改：

```text
src\QuantifiedSelf.Windows.App\ViewModels\SettingsViewModel.cs
src\QuantifiedSelf.Windows.App\Views\SettingsView.xaml
```

要求：

```text
Settings 中可保留简短状态文本，例如：
    Startup registration status: Enabled
状态文本是 OS 真实注册状态的只读反映，不参与 IsDirty。
状态文案安全，不泄露完整路径、完整 command、SID、用户目录。
```

3. 数据来源。

要求：

```text
复用阶段 11.2 的 StartupRegistrationService.GetStatusAsync()。
复用阶段 11.1 的 StartupLaunchOptions 获取当前 LaunchMode；LaunchMode 应来自 App 启动时已解析并保存的实例，不在 Diagnostics 刷新时反复解析命令行。
不要在 Diagnostics 中直接读取注册表或解析 Run Key command。
不要在 Diagnostics 中直接写 `agent_events` 或 JSONL。
自启状态读取只在 Diagnostics 页面刷新、Settings 显式刷新或 App 启动初始化时触发；不要接入 2 秒 status polling。
Diagnostics 刷新自启状态不得修改 SettingsViewModel 的 StartAppOnWindowsLogin 草稿值，也不得触发 Settings IsDirty。
```

4. 安全要求。

Diagnostics / Settings 不得展示：

```text
完整 exe 路径
完整 Run Key command
SID
用户名目录
注册表异常原文
stack trace
完整本机路径
```

要求：

```text
错误文案复用现有安全短句或 DiagnosticMessageSanitizer。
建议新增 StartupRegistrationDisplayModel / StartupRegistrationStatusFormatter 等纯模型，把 Enabled / Disabled / Mismatch / Error / UnsupportedInCurrentLaunchMode 统一转换为安全展示文案。
Diagnostics 和 Settings 复用同一 formatter，避免两处文案不一致。
不在展示层拼接路径或命令。
```

5. 与现有 Diagnostics 的衔接。

要求：

```text
新增自启状态展示不影响 RefreshHealth / IPC / Recent Events 等现有 Diagnostics 区域。
不新增高频日志或轮询。
自启状态只在 Diagnostics 页面刷新、Settings 显式刷新或 App 启动初始化时读取；不要接入 2 秒 status polling 的每次 tick。
如果已有 Diagnostics 刷新机制，复用该机制；不要为自启状态单独新增 timer。
```

6. 可测试性要求。

单元测试不要依赖真实 Windows 注册表，不要启动真实 WPF App。

建议新增测试：

```text
Diagnostics_ShowsLoginStartupEnabled
Diagnostics_ShowsLoginStartupDisabled
Diagnostics_ShowsLoginStartupMismatch
Diagnostics_ShowsLoginStartupError
Diagnostics_RedactsStartupRegistrationError
Diagnostics_ShowsLaunchModeManual
Diagnostics_ShowsLaunchModeAutoStart
Diagnostics_KeepsRefreshHealthVisible
Diagnostics_DoesNotExposeStartupCommandPath
Diagnostics_DoesNotExposeStartupCommandFullText
SettingsViewModel_ShowsSafeStartupRegistrationStatusText
```

测试要求：

```text
测试不要写真实注册表。
测试不要启动真实 WPF App。
测试不要依赖系统托盘。
使用 fake StartupRegistrationService 模拟 GetStatusAsync 返回各种状态。
使用 fake StartupLaunchOptions 或注入 provider 模拟 Manual / AutoStart launch mode。
验证 Diagnostics 显示 Enabled / Disabled / Mismatch / Error / UnsupportedInCurrentLaunchMode 文案。
验证错误展示不包含完整路径、完整 command、S-1-5-、用户名目录、HKCU\Software 原始路径、异常类型名、stack trace。
验证 Launch mode 显示 Manual / AutoStart。
验证 RefreshHealth / IPC / Recent Events 区域不受影响。
```

建议重点查看这些文件：

```text
src\QuantifiedSelf.Windows.App\ViewModels\MainWindowViewModel.cs
src\QuantifiedSelf.Windows.App\ViewModels\SettingsViewModel.cs
src\QuantifiedSelf.Windows.App\Views\MainWindow.xaml
src\QuantifiedSelf.Windows.App\Views\SettingsView.xaml
src\QuantifiedSelf.Windows.App\Services\DiagnosticsDataService.cs
src\QuantifiedSelf.Windows.App\Services\StartupRegistrationService.cs
src\QuantifiedSelf.Windows.App\Services\StartupRegistrationStatus.cs
src\QuantifiedSelf.Windows.App\Services\StartupLaunchOptions.cs
src\QuantifiedSelf.Windows.App\Services\RefreshHealthSnapshot.cs
tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs
```

验收标准：

- build 0 warning / 0 error。
- test 全部通过。
- Diagnostics 显示登录自启 Enabled / Disabled / Mismatch / Error。
- Mismatch 能显示安全修复提示（Needs repair）。
- Error 能显示安全错误。
- LaunchMode 能显示 Manual / AutoStart。
- 不影响 RefreshHealth / IPC / Recent Events 展示。
- 不新增高频日志或轮询。
- 不泄露完整路径、SID、注册表异常原文或完整 command。
- 不改 RefreshService 状态轮询语义。
- 不改 StartupRegistrationService。
- 不改 App 启动流程。
- 阶段 11.1 / 11.2 / 11.3 / 11.4 测试不回归。
- 阶段 10 托盘语义不回归。

请完成代码和测试修改后运行：

```powershell
dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex/build-output/
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex/test-output/
```

请不要并行运行 build 和 test，避免 obj 文件锁。

如果 build/test 产生 `.codex` 临时输出，请在确认结果后清理，清理前必须确认路径位于当前 workspace 内。

建议清理方式：

```powershell
$root = (Resolve-Path '.').Path
Get-ChildItem -Path src,tests -Recurse -Directory -Filter .codex | ForEach-Object {
    $full = $_.FullName
    if (-not $full.StartsWith($root, [System.StringComparison]::OrdinalIgnoreCase)) { throw "Refusing to remove outside workspace: $full" }
    Remove-Item -LiteralPath $full -Recurse -Force
}
```

完成后请把完成情况落盘为完成说明文档：

```text
docs\下一步计划-2026-07-04-开机自启MVP\阶段11.5-完成说明-YYYY-MM-DD.md
```

`YYYY-MM-DD` 使用执行当天日期；如果执行环境无法确定日期，使用 `2026-07-06`。

完成说明至少包含：

```text
1. 阶段目标
2. 修改文件清单
3. Diagnostics 自启状态区域说明
4. Settings 简短状态文本说明
5. 数据来源与更新机制说明
6. 安全脱敏说明
7. 新增 / 更新测试
8. build / test 结果
9. 明确未做事项
10. 风险与后续注意点
```

其中“明确未做事项”必须特别确认：

```text
未把自启状态写入 agent_events
未写 JSONL
未做通知中心
未显示完整注册表命令
未改 RefreshService 状态轮询语义
未改 StartupRegistrationService
未改 App 启动流程
未改 Settings 保存同步逻辑
未做手动验收 / 收口（阶段 11.6）
未做 single-instance
未做安装包 / Windows Service / 管理员权限自启 / 计划任务
未改阶段 10 托盘语义
未改阶段 8 IPC 协议
未改 AgentStateMachine 业务语义
未改 PruneData / ClearHistory 规则
```

注意：

- `Get-Content` 读取中文文档时使用 `-Encoding UTF8`。
- 不要回滚用户或其他 agent 的改动。
- 手工编辑文件请使用 `apply_patch`。
- 完成说明中不需要自行判断是否进入下一阶段。
- 不要把阶段 11.6 的手动验收 / 收口提前做进来。
