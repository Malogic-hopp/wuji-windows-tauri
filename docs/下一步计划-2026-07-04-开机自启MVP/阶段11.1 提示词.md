请在仓库 `C:\Users\WangZhenchi\work\WUJI\Win` 中实施阶段 11.1。

请先阅读并严格遵循这些文档，中文文档用 UTF-8 读取：

1. `docs\下一步计划-2026-07-04-开机自启MVP.md`
2. `docs\下一步计划-2026-07-04-开机自启MVP\README.md`
3. `docs\下一步计划-2026-07-04-开机自启MVP\01-阶段11.1-AppSettings与启动参数基础.md`
4. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\阶段10-完成说明-2026-07-04.md`

任务目标：

实现“阶段 11.1：AppSettings 与启动参数基础”。

本阶段是开机自启 MVP 的第一步，只新增登录自启的配置字段和启动参数解析基础，但不真正写注册表、不改 Settings UI、不改变当前 App 启动显示行为。本阶段要把配置模型和启动参数解析先补齐，避免一开始就把注册表、Settings UI 和 WPF 启动流程混在一起。

尤其注意本阶段不做：

- 不写 HKCU Run Key（阶段 11.2）。
- 不改 Settings UI（阶段 11.3）。
- 不实现自启到托盘（阶段 11.4）。
- 不做 single-instance。
- 不做 Diagnostics 自启状态展示（阶段 11.5）。
- 不做手动验收 / 收口（阶段 11.6）。
- 不做安装包、Windows Service、管理员权限自启、计划任务。
- 不改阶段 10 CloseToTray / MinimizeToTray / Exit App / 托盘命令语义。
- 不改阶段 8 IPC 协议。
- 不改 AgentStateMachine 业务语义。
- 不改 PruneData / ClearHistory 规则。
- 不改 ClearHistory 的 `CLEAR` 二次确认。
- 不回退阶段 9.6 的 Stale + IsRunning=false 时 Start 可用修复。

补充要求：

```text
1. StartAppOnWindowsLogin 应保持普通 System.Text.Json 属性序列化兼容，不需要自定义 converter；旧配置缺字段时默认 false，新保存配置应能稳定 round-trip。
2. 只有 --from-autostart 才将 LaunchMode 设为 AutoStart；单独 --start-hidden 只表示 StartHidden=true，但 Mode 仍为 Manual。
3. 启动参数解析建议大小写不敏感，例如 --FROM-AUTOSTART 与 --from-autostart 等价；RawArgs 如保留则保留原值。
4. RawArgs 仅用于测试或内部调试，本阶段不要把完整 RawArgs 展示到 UI、Diagnostics 或日志。
5. App.xaml.cs 中解析得到的 StartupLaunchOptions 本阶段可以只保存在私有字段，或仅用于完成代码路径验证；不得传入 TrayService / MainWindowViewModel 改变行为。
6. SettingsViewModel 保存 StartAppOnWindowsLogin 时只写 AppSettings store，不调用任何 StartupRegistrationService，不访问注册表。
```

需要实现的范围：

1. AppSettings 新增 `StartAppOnWindowsLogin`。

建议修改：

```text
src\QuantifiedSelf.Windows.Core\Options\AppSettings.cs
```

要求：

```text
新增 bool StartAppOnWindowsLogin，默认 false。
现有 AutoStartAgentWhenAppStarts 语义不变。
旧 app-settings.json 缺少 StartAppOnWindowsLogin 时可正常读取，默认 false。
保存 AppSettings 时保留 StartAppOnWindowsLogin。
```

2. 新增启动参数解析器。

建议新增：

```text
src\QuantifiedSelf.Windows.App\Services\StartupLaunchOptions.cs
```

`StartupLaunchOptions` 至少包含：

```text
LaunchMode Mode  // Manual 或 AutoStart
bool StartHidden
bool FromAutostart
string[] RawArgs（可选，用于调试/日志）
```

要求：

```text
支持识别 --from-autostart。
支持识别 --start-hidden。
未知参数不崩溃，安全忽略。
手动启动（无 --from-autostart）默认 LaunchMode=Manual，StartHidden=false。
自启启动（有 --from-autostart）默认 LaunchMode=AutoStart，FromAutostart=true。
只有 --from-autostart 才将 LaunchMode 设为 AutoStart；单独 --start-hidden 只表示 StartHidden=true，但 Mode 仍为 Manual。
同时提供 --from-autostart 和 --start-hidden 时 StartHidden=true。
启动参数解析大小写不敏感，例如 --FROM-AUTOSTART 与 --from-autostart 等价。
参数解析应可单元测试，不依赖真实 WPF App 启动。
```

3. 与 App.xaml.cs 的衔接（只解析，不改变行为）。

建议修改：

```text
src\QuantifiedSelf.Windows.App\App.xaml.cs
```

要求：

```text
在 App 启动早期解析 Environment.GetCommandLineArgs() 并生成 StartupLaunchOptions。
但本阶段不根据 StartupLaunchOptions 改变窗口显示行为。
手动启动仍显示主窗口，保持阶段 10 现状。
自启参数启动也仍显示主窗口，阶段 11.4 再做隐藏。
不要将 StartupLaunchOptions 传给 TrayService 或 MainWindowViewModel 改变现有语义。
```

4. SettingsViewModel 预支持（只读/保存，不改 UI）。

建议修改：

```text
src\QuantifiedSelf.Windows.App\ViewModels\SettingsViewModel.cs
```

要求：

```text
加载 AppSettings 时读取 StartAppOnWindowsLogin。
保存 AppSettings 时把 StartAppOnWindowsLogin 写回 AppSettings store。
SettingsViewModel 保存 StartAppOnWindowsLogin 时只写配置 store，不调用任何 StartupRegistrationService，不访问注册表。
本阶段不在 Settings UI 上新增开关控件（阶段 11.3 做）。
可以通过代码级测试验证保存/读取往返。
```

5. 可测试性要求。

单元测试不要依赖真实 Windows 注册表，不要启动真实 WPF App。

建议新增测试：

```text
AppSettings_DefaultsStartAppOnWindowsLoginToFalse
AppSettingsStore_ReadOldSettingsDefaultsLoginStartupFalse
AppSettingsStore_RoundTripPreservesStartAppOnWindowsLogin
SettingsViewModel_LoadsStartAppOnWindowsLogin
SettingsViewModel_SavesStartAppOnWindowsLogin
StartupLaunchOptions_ParsesAutostartHiddenArgs
StartupLaunchOptions_IgnoresUnknownArgs
StartupLaunchOptions_DefaultsToManualVisible
StartupLaunchOptions_ParsesOnlyStartHiddenWithoutFromAutostart
StartupLaunchOptions_ParsesEmptyArgsAsManual
```

测试要求：

```text
测试不要写真实注册表。
测试不要启动真实 WPF App。
测试不要依赖系统托盘。
AppSettings 测试验证默认值和旧配置兼容性。
StartupLaunchOptions 测试覆盖手动启动、自启隐藏、未知参数等场景。
SettingsViewModel 测试验证 StartAppOnWindowsLogin 的加载和保存往返，验证保存时未访问注册表，但不验证 UI 控件存在。
```

建议重点查看这些文件：

```text
src\QuantifiedSelf.Windows.Core\Options\AppSettings.cs
src\QuantifiedSelf.Windows.Core\Options\AppSettingsRepository.cs
src\QuantifiedSelf.Windows.Infrastructure\Settings\AppSettingsStore.cs
src\QuantifiedSelf.Windows.App\App.xaml.cs
src\QuantifiedSelf.Windows.App\ViewModels\SettingsViewModel.cs
tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs
```

验收标准：

- build 0 warning / 0 error。
- test 全部通过。
- 旧 `app-settings.json` 缺少 `StartAppOnWindowsLogin` 时可正常读取，默认 false。
- 保存 AppSettings 时保留 `StartAppOnWindowsLogin`。
- `AutoStartAgentWhenAppStarts` 现有测试不回归。
- 启动参数解析测试通过。
- 不写注册表。
- 不改 Settings UI。
- 不改变 App 启动显示主窗口的现状。
- 自启参数启动不改变现有行为（隐藏到托盘在阶段 11.4 实现）。

请完成代码和测试修改后运行：

```powershell
dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\build-output\
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\test-output\
```

请不要并行运行 build 和 test，避免 obj 文件锁。

如果 build/test 产生 `.codex` 临时输出，请在确认结果后清理，清理前必须确认路径位于当前 workspace 内。

建议清理方式：

```powershell
$root = (Resolve-Path '.').Path
Get-ChildItem -Path src,tests -Recurse -Directory -Filter .codex | ForEach-Object {
    $full = $_.FullName
    if (-not $full.StartsWith($root, [System.StringComparison]::OrdinalIgnoreCase)) { throw "Refusing to remove outside workspace: $full" }
    Remove-Item -LiteralPath $full -Recurse -Force
}
```

完成后请把完成情况落盘为完成说明文档：

```text
docs\下一步计划-2026-07-04-开机自启MVP\阶段11.1-完成说明-YYYY-MM-DD.md
```

`YYYY-MM-DD` 使用执行当天日期；如果执行环境无法确定日期，使用 `2026-07-04`。

完成说明至少包含：

```text
1. 阶段目标
2. 修改文件清单
3. AppSettings 变更说明
4. StartupLaunchOptions 设计说明
5. App.xaml.cs 解析衔接说明
6. SettingsViewModel 预支持说明
7. 新增 / 更新测试
8. build / test 结果
9. 明确未做事项
10. 风险与后续注意点
```

其中“明确未做事项”必须特别确认：

```text
未写 HKCU Run Key（阶段 11.2）
未改 Settings UI（阶段 11.3）
未实现自启到托盘（阶段 11.4）
未做 Diagnostics 自启状态展示（阶段 11.5）
未做手动验收 / 收口（阶段 11.6）
未做 single-instance
未做安装包 / Windows Service / 管理员权限自启 / 计划任务
未改阶段 10 托盘语义
未改阶段 8 IPC 协议
未改 AgentStateMachine 业务语义
未改 PruneData / ClearHistory 规则
```

注意：

- `Get-Content` 读取中文文档时使用 `-Encoding UTF8`。
- 不要回滚用户或其他 agent 的改动。
- 手工编辑文件请使用 `apply_patch`。
- 完成说明中不需要自行判断是否进入下一阶段。
- 不要把阶段 11.2 的 HKCU Run Key 写入提前做进来。
- 不要把阶段 11.3 的 Settings UI 开关提前做进来。
- 不要把阶段 11.4 的自启隐藏到托盘提前做进来。
