# 阶段 11 完成说明（2026-07-06）

Build: `0 warnings / 0 errors` | Test: `356 / 356 passed`

## 阶段目标

让 WUJI 支持当前用户登录 Windows 后自动启动到托盘，并复用已有 `AutoStartAgentWhenAppStarts` 决定是否随 App 启动 Agent。

## 阶段 11.1–11.5 关键实现摘要

| 阶段 | 内容 | 测试增长 | 完成说明 |
|---|---|---|---|
| 11.1 | AppSettings 新增 `StartAppOnWindowsLogin`，`StartupLaunchOptions` 参数解析 | +13 | `阶段11.1-完成说明-2026-07-04.md` |
| 11.2 | `IStartupRegistry` / `RegistryStartupRegistry` / `StartupCommandBuilder` / `StartupRegistrationService` — HKCU Run Key 读写 | +30 | `阶段11.2-完成说明-2026-07-05.md` |
| 11.3 | Settings UI 独立开关 + 保存同步注册/取消注册 + Mismatch/Disabled 修复 | +11 | `阶段11.3-完成说明-2026-07-06.md` |
| 11.4 | `WindowStartupPolicy` + `--from-autostart --start-hidden` 隐藏启动 + `InitializeAsync` 增强（idempotent + AutoStartAgent） | +10 | `阶段11.4-完成说明-2026-07-06.md` |
| 11.5 | Diagnostics 自启状态只读展示 + `StartupRegistrationDisplayModel` 安全文案 | +18 | `阶段11.5-完成说明-2026-07-06.md` |
| 11.6 | 验收清单 + 完成说明（本文档） | +6 | — |

阶段 11 全阶段测试增长：281 → 356（+75）。

**最终测试分布：**

| 阶段 | 测试数 |
|---|---|
| 11.1 AppSettings + 启动参数 | 13 |
| 11.2 StartupRegistrationService + Run Key | 30 |
| 11.3 Settings 开关 + 保存同步 | 11 |
| 11.4 自启启动到托盘 | 10 |
| 11.5 Diagnostics 自启展示 | 18 |
| 11.6 修复 + 补强测试 | 6 |
| 前序（7-10）回归 | 268 |
| **合计** | **356** |

## 自动化验收结果

```text
dotnet build QuantifiedSelf.Windows.sln --no-restore
    0 warnings / 0 errors

dotnet test QuantifiedSelf.Windows.sln --no-restore
    356 / 356 passed
```

- 阶段 11.1 StartupLaunchOptions 测试：13 个 ✅
- 阶段 11.2 StartupRegistrationService 测试：30 个 ✅
- 阶段 11.3 Settings 自启开关测试：11 个 ✅
- 阶段 11.4 自启启动策略测试：10 个 ✅
- 阶段 11.5 Diagnostics 展示测试：18 个 ✅
- 阶段 10 托盘测试不回归 ✅
- 阶段 9 RefreshService / status polling 不回归 ✅
- 阶段 8 IPC 不回归 ✅
- 阶段 7 PruneData / ClearHistory 不回归 ✅

## 手动验收结论

**已完成**（21/21）。全部 21 项在真实 Windows 桌面环境（系统托盘 + Agent 进程 + IPC 通道）中执行通过。

关键验收项：
- HKCU Run Key 注册/删除正常，command 格式正确
- 自启参数启动主窗口不弹出，托盘 Show 可恢复
- AutoStartAgentWhenAppStarts true/false 行为正确
- Mismatch 修复路径（Settings Save → RegisterAsync）正常
- Exit App 不停止 Agent，托盘图标不残留
- 重复启动无双托盘/双窗口/状态漂移
- 第 15 项（注册失败安全错误）由自动化测试覆盖，手动未强制复现 HKCU 权限失败

详见 `阶段11-验收清单-2026-07-06.md`。

## 长跑验证结论

**已完成**（6/6）。自启隐藏运行 15-30 分钟，Agent 稳定、Pause/Resume/Stop/Start 循环正常、Exit App 无残留、再次手动启动正常。

## 风险复核

| 编号 | 检查项 | 结论 | 依据 |
|---|---|---|---|
| 1 | StartAppOnWindowsLogin 与 AutoStartAgentWhenAppStarts 语义独立 | ✅ PASS | Settings UI 两个独立 CheckBox + 各自说明文字；`TryBuildAppSettings` 分开保存 |
| 2 | HKCU Run Key 注册 / 删除幂等 | ✅ PASS | `RegisterIsIdempotent` / `UnregisterIsIdempotent` 测试 + 手动验收确认 |
| 3 | 禁用自启后 WUJI value 不残留 | ✅ PASS | `UnregisterAsync` → `DeleteValue("WUJI")` + 手动验收确认 Run Key 已删除 |
| 4 | StartupCommandBuilder 不会注册 dotnet.exe / dll / 空路径 | ✅ PASS | `RejectsDotnetHostPath` / `RejectsDllPath` / `RejectsEmptyProcessPath` / `RejectsNullProcessPath` 测试 |
| 5 | Run Key command 比较使用规范化解析 | ✅ PASS | token 精确匹配 + `NormalizeExePath` + 大小写不敏感 + 伪参数前缀拒绝 |
| 6 | Settings 保存失败时不显示假启用 | ✅ PASS | `RegisterAsync` 失败 → return before save → IsDirty 保留 → `SaveStatusText` 显示安全错误 |
| 7 | Settings dirty 不覆盖草稿，不触发注册表写入 | ✅ PASS | `RefreshStartupRegistrationStatusAsync` 只读 + 手动验收确认 |
| 8 | AutoStartHidden 不通过 Window.Closing / CloseToTray 模拟隐藏 | ✅ PASS | `WindowStartupPolicy` 直接决定 `window.Show()`；AutoStartHidden 路径不依赖 `Window.Closing` / `CloseToTray`。App 中 CloseToTray 的 `Cancel=true` / `Hide()` 逻辑仅保留用于手动关闭窗口场景 |
| 9 | AutoStartHidden 不弹阻塞式 MessageBox | ✅ PASS | `InitializeAsync` 为 fire-and-forget；异常写入安全状态文本 |
| 10 | Diagnostics 不泄露完整路径 / SID / command / 注册表异常原文 | ✅ PASS | `StartupRegistrationDisplayModel` 固定安全文案 + 手动验收确认 |
| 11 | 阶段 10 托盘 CloseToTray / MinimizeToTray / Exit App 不回归 | ✅ PASS | 356/356 全通过 + 手动验收确认 |
| 12 | 阶段 8 IPC、阶段 9 RefreshService、阶段 7 数据清理不回归 | ✅ PASS | 356/356 全通过 |
| 13 | 重复启动行为已手动验证并记录 | ✅ PASS | 手动验收确认：无双托盘/双窗口/状态漂移。single-instance 仍建议后续作为轻量 guard |

## 修改文件清单（本阶段 11.6）

无代码修改。新增两份文档：
- `阶段11-验收清单-2026-07-06.md`
- `阶段11-完成说明-2026-07-06.md`

## 已知问题 / 建议后续前置项

1. **无 single-instance 保护**：手动验收确认当前无重复启动异常（无双托盘/双窗口），但仍建议后续做轻量 named mutex guard 防止边界场景。
2. **无正式应用图标**：托盘图标使用 `SystemIcons.Application` fallback，视觉效果一般。
3. **`AutoStartAgentWasTriggered` 为内部观测标志**：不如 fake process service 调用计数硬，但作为阶段 11.4 的收口验证已够用。

## 后续候补建议排序

### 高优先级
1. 安装包 / 发布体验
2. 轻量 single-instance + second launch show existing window
3. 正式应用图标资源
4. WPF Runtime Smoke Test 自动化

### 功能增强
5. 最近 7 天趋势和图表
6. 应用分类
7. 分页 SamplesView
8. 数据导出 CSV
9. 本地数据备份与恢复
10. 本地数据加密

### 架构深化
11. 真正 Named Pipe 状态流或事件订阅
12. PipeSecurity / ACL 强化
13. ClearHistory confirmation token
14. 管理员权限重启 Agent（按需）
15. Startup 文件夹快捷方式替代方案

## 收口结论

阶段 11 开机自启 MVP 已完成。

- 自动化验收：**已完成**（356/356 passed，0/0）
- 手动验收：**已完成**（21/21）
- 长跑验证：**已完成**（6/6）

WUJI 支持当前用户登录 Windows 后自动启动；
StartAppOnWindowsLogin 与 AutoStartAgentWhenAppStarts 语义独立；
自启注册使用 HKCU Run Key；
自启启动默认进入托盘，不弹主窗口；
Settings 可启用/禁用并显示真实注册状态；
Diagnostics 可解释 Enabled / Disabled / Mismatch / Error；
阶段 10 托盘、阶段 9 RefreshService、阶段 8 IPC、阶段 7 数据清理测试无回归。

```
阶段 11 开机自启 MVP 正式收口。
```
