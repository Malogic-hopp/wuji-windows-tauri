请在仓库 C:\Users\WangZhenchi\work\WUJI\Win 中实施阶段 8.3。

请先阅读并严格遵循这些文档，中文文档用 UTF-8 读取：

1. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP.md
2. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\README.md
3. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\03-阶段8.3-WPF-AgentControlClient与Fallback策略.md
4. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8.1-完成说明-2026-07-01.md
5. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8.2-完成说明-2026-07-01.md

任务目标：

实现“阶段 8.3：WPF AgentControlClient 与 fallback 策略”。

本阶段只做 WPF/App 侧 Named Pipe client、IPC GetStatus 优先读取、失败 fallback 到既有 runtime_state / health_state 文件状态，并在 App 会话内记录 IPC 状态。不要迁移 Pause / Resume / Stop / ReloadConfig / PruneData / ClearHistory，不要删除现有 `agent_control.json` 文件 fallback。

尤其注意本阶段不做：

- 不迁移 Pause / Resume / Stop 到 IPC。
- 不迁移 ReloadConfig 到 IPC。
- 不迁移 PruneData / ClearHistory 到 IPC。
- 不改变 `AgentControlService` 对控制命令的文件投递主逻辑。
- 不删除或弱化 `agent_control.json` fallback。
- 不新增 IPC runtime 持久化文件。
- 不做 Diagnostics 完整 UI。
- 不做状态流订阅。
- 不引入 gRPC。
- 不做 PipeSecurity / ACL 扩展，除非已有代码自然支持且不会扩大范围。

需要实现的范围：

1. 新增 Infrastructure 层 IPC client。

建议文件：

- src\QuantifiedSelf.Windows.Infrastructure\Ipc\IAgentIpcClient.cs
- src\QuantifiedSelf.Windows.Infrastructure\Ipc\NamedPipeAgentControlClient.cs
- 如需要，可以新增轻量 options 类型，例如 `AgentIpcClientOptions`

职责：

- 使用阶段 8.1 的 `AgentPipeName` 生成当前用户 pipe name。
- 使用阶段 8.1 的 `NamedPipeProtocol` 读写 length-prefixed JSON。
- 连接阶段和请求阶段使用不同 timeout。
- 支持 `CancellationToken`。
- 对外返回 `AgentIpcResponse` 或抛出安全 IPC client 异常，但不要暴露完整 SID、完整 pipe name、路径、异常原文、原始 payload。
- 只实现通用 `SendAsync(AgentIpcRequest, ...)` 或 `PingAsync` / `GetStatusAsync` 这类低风险方法；不要在 client 内执行业务命令语义。

建议 timeout 默认值：

```text
ConnectTimeoutMilliseconds = 1000
RequestTimeoutMilliseconds = 5000
MaintenanceCommandTimeoutMilliseconds = 30000
```

本阶段只会用到 `ConnectTimeout` 和 `RequestTimeout`。`MaintenanceCommandTimeout` 可以先定义但不要接 PruneData / ClearHistory。

2. 新增 App 层 IPC 状态记录服务。

建议文件：

- src\QuantifiedSelf.Windows.App\Services\AgentIpcStatusService.cs

第一版只保存本次 App 会话内存状态，不落盘。

建议字段：

```text
lastIpcSuccessUtc
lastIpcError
lastFallbackUsedUtc
lastCommandSource
fullPipeName
displayPipeName
```

注意：

- `FullPipeName` 只用于内部连接，不要放进 UI 状态文本或错误文案。
- UI / 状态文本如果需要展示 pipe name，只展示 `DisplayPipeName`。
- `lastIpcError` 必须是安全文本，不要保存原始异常 message。
- `lastCommandSource` 建议使用字符串或轻量 enum：`NamedPipe` / `FileFallback` / `Unavailable`。

3. 调整 AgentStatusService：优先 IPC GetStatus，失败 fallback。

当前文件：

- src\QuantifiedSelf.Windows.App\Services\AgentStatusService.cs

要求：

- 构造函数可以新增可选 `IAgentIpcClient` / `AgentIpcStatusService` 依赖，注意保持现有测试构造成本可控。
- `GetStatusAsync` 优先尝试 IPC `GetStatus`。
- IPC 成功时用 IPC response 构造 `AgentStatusSnapshot`，并更新 `lastIpcSuccessUtc` / `lastCommandSource = NamedPipe`。
- IPC 连接失败、超时、协议错误或 Agent 未运行时，不抛到 UI；应 fallback 到现有 runtime_state / health_state 文件逻辑。
- fallback 时更新 `lastFallbackUsedUtc` / `lastCommandSource = FileFallback` / `lastIpcError`。
- fallback 后现有状态判断语义应保持不回归：NotRunning / Stale / Stopped / Running 等判断不要被破坏。
- IPC GetStatus 不应导致 `agent_control.json` 写入。

IPC 成功映射建议：

```text
AgentIpcResponse.Status.ActualState -> AgentStatusSnapshot.ActualState
Status.IsHealthy -> IsHealthy
Status.LastHeartbeatUtc -> LastHeartbeatText
Status.LastSampleUtc -> LastSampleText
Status.ProcessId -> ProcessText
Status.Version 可暂不展示，除非已有模型适合承载
```

如果 IPC response 缺少 `Status`、`Accepted=false`、`Completed=false` 或 errorCode 不为空，应视为 IPC 不可用并 fallback，不要把原始 response message 直接显示到 UI。

4. AgentControlService 本阶段只做最小接入或不接入。

当前文件：

- src\QuantifiedSelf.Windows.App\Services\AgentControlService.cs

本阶段目标是 WPF 侧 client 与 fallback 策略，控制命令迁移留到阶段 8.4 / 8.5。

允许做的事：

- 如果为了 `GetStatusAsync` 复用状态服务，可以保持现有 `AgentControlService.GetStatusAsync` 通过 `AgentStatusService.GetStatusAsync` 间接受益于 IPC。
- 可以注入 `AgentIpcStatusService` 仅用于记录 command source，但不要改变 Pause / Resume / Stop / ReloadConfig / PruneData / ClearHistory 的文件投递行为。

不允许做的事：

- 不要让 Pause / Resume / Stop 走 IPC。
- 不要让 ReloadConfig 走 IPC。
- 不要让 PruneData / ClearHistory 走 IPC。
- 不要在 request timeout 后对高风险命令做 fallback 重发，因为本阶段还没有迁移这些命令。

5. App 启动接线。

当前文件：

- src\QuantifiedSelf.Windows.App\App.xaml.cs

要求：

- 创建并注入 `AgentIpcStatusService`。
- 创建并注入 `NamedPipeAgentControlClient` 或 `IAgentIpcClient`。
- 保持现有服务初始化简单，不引入重型 DI 容器重构。
- 不影响 Settings / Diagnostics / Samples / Sessions / Apps 已有页面构造。

6. 安全文案要求。

IPC client 和 status service 中：

- 不显示完整 SID。
- 不显示完整 pipe name。
- 不显示 Windows 路径。
- 不显示原始异常 message。
- 不显示原始 JSON payload。
- timeout / unavailable 文案使用固定安全短句，例如：

```text
IPC unavailable; using file fallback.
IPC request timed out; using file fallback.
IPC protocol error; using file fallback.
```

如需使用现有脱敏器，优先复用：

- src\QuantifiedSelf.Windows.Core\Events\DiagnosticMessageSanitizer.cs

7. 不要破坏阶段 8.2 Agent server。

本阶段不应该修改 Agent server 的 Ping / GetStatus 语义，除非修复明显 bug。尤其不要重新引入：

- `Ping` 进入共享 `AgentCommandType`
- `DesiredState = Running` 硬编码
- unsupported command 回显原始 command
- server 异常热循环

建议重点查看这些现有文件：

- src\QuantifiedSelf.Windows.Core\Ipc\AgentIpcRequest.cs
- src\QuantifiedSelf.Windows.Core\Ipc\AgentIpcResponse.cs
- src\QuantifiedSelf.Windows.Core\Ipc\AgentIpcStatus.cs
- src\QuantifiedSelf.Windows.Core\Ipc\AgentPipeName.cs
- src\QuantifiedSelf.Windows.Infrastructure\Ipc\NamedPipeProtocol.cs
- src\QuantifiedSelf.Windows.Infrastructure\Ipc\NamedPipeAgentCommandServer.cs
- src\QuantifiedSelf.Windows.Agent\Services\AgentCommandServerHostedService.cs
- src\QuantifiedSelf.Windows.App\Services\AgentStatusService.cs
- src\QuantifiedSelf.Windows.App\Services\AgentControlService.cs
- src\QuantifiedSelf.Windows.App\App.xaml.cs
- src\QuantifiedSelf.Windows.App\Models\AgentStatusSnapshot.cs
- tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs

建议新增测试：

- NamedPipeAgentControlClient_PingReturnsPong
- NamedPipeAgentControlClient_GetStatusReturnsStatus
- NamedPipeAgentControlClient_TimesOutSafely
- NamedPipeAgentControlClient_UsesSeparateConnectAndRequestTimeouts
- AgentStatusService_UsesIpcStatusWhenAvailable
- AgentStatusService_FallsBackToRuntimeStateWhenIpcUnavailable
- AgentStatusService_FallsBackWhenIpcResponseIsFailed
- AgentStatusService_DoesNotWriteControlFileForIpcGetStatus
- AgentIpcStatusService_RecordsLastSuccessAndFallback
- AgentIpcStatusService_DoesNotExposeFullPipeNameInDisplayText
- AgentControlService_ExistingCommandsStillUseFileFallback

测试建议：

- IPC client 可以用真实 `NamedPipeServerStream` / `NamedPipeClientStream` 做少量 roundtrip，但必须使用短 timeout，避免测试挂住。
- timeout 测试要短，尽量用不可连接 pipe 或 fake stream / fake client，避免 5 秒级等待拖慢测试。
- `AgentStatusService` 优先 IPC / fallback 测试建议用 fake `IAgentIpcClient`，不要强依赖真实 Agent 进程。
- fallback 测试应覆盖 runtime_state / health_state 旧逻辑仍可用。
- 测试应断言 IPC GetStatus 不写 `agent_control.json`。
- 测试结束必须释放 pipe / cancel token，不能留下后台 task。

验收标准：

- App 在 Agent 运行且 pipe 可用时，能通过 IPC GetStatus 获取状态。
- Agent 未运行或 IPC 不可用时，App 不崩溃。
- IPC 不可用时仍能通过 runtime_state / health_state 显示状态。
- IPC 超时不会卡住 Refresh。
- App 会话内能记录 last IPC success / error / fallback used。
- App 不显示完整 SID / 完整 pipe name / 路径 / 异常原文。
- `AgentControlService` 的 Pause / Resume / Stop / ReloadConfig / PruneData / ClearHistory 仍走现有文件链路。
- `agent_control.json` fallback 没有被删除。
- 阶段 8.1 协议测试不回归。
- 阶段 8.2 Agent server 测试不回归。
- 阶段 7 PruneData / ClearHistory 测试不回归。

请完成代码和测试修改后运行：

```powershell
dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\build-output\
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\test-output\
```

请不要并行运行 build 和 test，避免 obj 文件锁。

完成后清理临时输出：

```powershell
Get-ChildItem -Path src,tests -Recurse -Directory -Filter .codex | Remove-Item -Recurse -Force
```

如果 build/test 通过，请写一份完成说明到：

```text
docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8.3-完成说明-2026-07-01.md
```

完成说明请包含：

- 修改了哪些文件
- WPF/App 侧 IPC client 如何连接和超时
- AgentStatusService 如何优先 IPC 并 fallback
- AgentIpcStatusService 记录了哪些内存状态
- 安全文案如何避免泄露完整 SID / pipe name / 路径 / 原始异常
- 新增/更新了哪些测试
- build/test 结果
- 明确说明没有迁移 Pause / Resume / Stop / ReloadConfig / PruneData / ClearHistory
- 明确说明没有删除 `agent_control.json` fallback
- 是否建议进入阶段 8.4

注意：

- Get-Content 读取中文文档时使用 `-Encoding UTF8`。
- 不要回滚用户或其他 agent 的改动。
- 手工编辑文件请使用 apply_patch。
- 不要把阶段 8.4 的基础控制命令迁移提前做进来。
- 不要把阶段 8.5 的维护命令迁移提前做进来。
- 不要把阶段 8.6 的 Diagnostics 完整 IPC 状态展示提前做进来。
