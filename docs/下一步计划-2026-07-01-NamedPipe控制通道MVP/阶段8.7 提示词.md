请在仓库 C:\Users\WangZhenchi\work\WUJI\Win 中实施阶段 8.7。

请先阅读并严格遵循这些文档，中文文档用 UTF-8 读取：

1. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP.md
2. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\README.md
3. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\07-阶段8.7-验收断连测试与收口.md
4. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8.1-完成说明-2026-07-01.md
5. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8.2-完成说明-2026-07-01.md
6. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8.3-完成说明-2026-07-01.md
7. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8.4-完成说明-2026-07-01.md
8. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8.5-完成说明-2026-07-01.md
9. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8.6-完成说明-2026-07-01.md

任务目标：

实施“阶段 8.7：验收、断连测试与收口”。

本阶段不是新增业务功能阶段，而是完成阶段 8 的自动化验收、必要补测、断连场景覆盖和收口文档。请保持改动克制，不要借收口阶段扩展新的 IPC 功能。

本阶段重点产出：

```text
docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8-验收清单-2026-07-01.md
docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8-完成说明-2026-07-01.md
```

如发现现有自动化测试缺少关键断连覆盖，可以补少量测试；如果只是手动验收项，不要伪造通过结果，请在清单中保留待人工勾选。

尤其注意本阶段不做：

- 不做阶段 9 状态流订阅。
- 不做托盘。
- 不做开机自启。
- 不做安装包。
- 不做趋势图、导出或复杂 IPC 日志页。
- 不新增 IPC runtime 持久化文件。
- 不改变命令投递语义。
- 不删除 `agent_control.json` fallback。
- 不改 PruneData / ClearHistory 删除规则。
- 不绕过 ClearHistory 的 `CLEAR` 二次确认。

需要完成的工作：

1. 自动化验收复核。

请确认并在验收清单中记录以下自动化覆盖：

```text
dotnet build 0 warning / 0 error
dotnet test 全部通过
IPC 协议模型 roundtrip
pipe name 安全规则
MaxPayloadBytes / invalid payload / truncated payload
Ping / GetStatus
Pause / Resume / Stop IPC
Stop 快速退出 / pipe broken / timeout 不误报失败
ReloadConfig IPC 成功 / 失败
PruneData / ClearHistory IPC
fallback 到 agent_control.json
requestId 轻量防重
IPC timeout 不重复副作用命令
Diagnostics IPC 状态展示
CommandFailed / IPC error 脱敏
AgentCommandServer 异常不拖垮采样
阶段 7 PruneData / ClearHistory 测试不回归
```

如果发现某一项没有测试支撑，请优先补少量低风险测试；不要为了补测大规模重构。

2. 断连场景补测或确认。

阶段文档要求覆盖：

```text
Agent 未运行
Pipe 不存在
Pipe 连接超时
Pipe request 超时
Pipe response 不是合法 JSON
协议版本不支持
命令执行失败
Maintenance 中拒绝重复命令
WPF 取消请求
Agent 处理中退出
```

请先查现有测试是否已覆盖。已有覆盖就写入验收清单并标注对应测试名；缺口只补最关键、最容易自动化、不会拖慢测试的场景。

建议重点补测候选：

- `AgentControlService` 遇到 `OperationCanceledException` 时不写 fallback，或按现有设计安全退出。
- `NamedPipeAgentControlClient` request timeout 不阻塞 UI。
- `AgentCommandServerHostedService` pipe/protocol 异常不导致 hosted service fault。
- `AgentProcessService` Stop timeout 后确认 Stopping/Stopped 时不写 stale fallback。
- Diagnostics 的 IPC error 不显示 SID / 路径。

如果某些场景只能手动验证（例如真实 Agent 处理中退出），请在手动验收清单中列出，不要假装自动化已覆盖。

3. 风险复核。

请做静态复核并在完成说明中写明结论：

```text
IPC 是否可能卡 UI
双通道是否可能重复执行命令
IPC 与 file fallback 语义是否一致
是否存在跨用户控制风险
是否破坏现有 agent_control.json 稳定链路
Diagnostics 是否泄露 FullPipeName / SID / 路径 / 原始异常 / 原始 JSON
AgentCommandServer 是否可能拖垮采样
Stop 是否会留下 stale agent_control.json
ClearHistory 是否仍需 CLEAR 二次确认
```

4. 生成阶段 8 验收清单。

新增文件：

```text
docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8-验收清单-2026-07-01.md
```

格式可参考：

- docs\下一步计划-2026-06-24-PruneDataClearHistory数据清理MVP\阶段7-验收清单-2026-06-25.md
- docs\下一步计划-2026-06-22-SamplesSessionsApps数据浏览MVP\阶段5.6-验收清单.md
- docs\下一步计划-2026-06-22-Settings与数据管理MVP\阶段6-验收清单-2026-06-24.md

验收清单建议结构：

```text
# 阶段 8 验收清单

更新时间：2026-07-01

`dotnet build` 0 warnings / 0 errors，`dotnet test` N/N passed。

## 自动化验收
### IPC 协议与安全
### Agent server 与基础命令
### WPF client 与 fallback
### 维护命令迁移
### Diagnostics IPC 状态展示
### 断连与异常场景
### 前序阶段不回归

## 静态检查

## 手动验收

## 长跑验证

## 明确未做

## 收口结论
```

自动化验收项可以根据实际测试结果打 `[x]`，并尽量标注测试名。

手动验收项如果没有实际执行，请保留 `[ ]`，不要打勾。建议列出：

```text
启动 Agent，Diagnostics 显示 IPC 可用。
Refresh 正常。
Pause 通过 IPC 进入 Paused。
Resume 通过 IPC 进入 Running。
ReloadConfig 通过 IPC 写 ConfigReloaded / CommandCompleted。
PruneData 通过 IPC 写 DataPruned。
ClearHistory 仍需输入 CLEAR，成功后 Paused。
停止 Agent 后 UI 不崩溃，Diagnostics 显示 IPC unavailable / fallback。
Agent 停止时 ReloadConfig 沿用“下次启动生效”语义。
模拟 IPC 失败时 fallback 到 agent_control.json。
Diagnostics 显示 fallback used 和安全 last IPC error。
Diagnostics 只显示 DisplayPipeName，不显示完整 SID。
重启 Agent 后 IPC 自动恢复。
```

长跑验证如果没有实际执行，也保留 `[ ]`：

```text
Agent Running 15-30 分钟，多次 Refresh。
Pause / Resume 循环 3-5 次。
ReloadConfig 1-2 次。
PruneData 后继续运行 10-15 分钟。
ClearHistory 后 Resume，再运行 5-10 分钟。
中途关闭 / 重启 Agent，WPF 不崩溃，IPC 恢复。
```

5. 生成阶段 8 完成说明。

新增文件：

```text
docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8-完成说明-2026-07-01.md
```

建议包含：

- 阶段 8 总目标
- 8.1 到 8.7 各阶段完成摘要
- 最终命令通道语义：

```text
Ping / GetStatus: IPC
Pause / Resume / Stop / ReloadConfig: IPC 优先，file fallback
PruneData / ClearHistory: IPC 优先，file fallback，维护语义保持
agent_control.json: 保留
```

- requestId 防重和 timeout 策略
- Diagnostics IPC 状态展示
- 安全与隐私复核
- 自动化验证结果
- 手动验收待办或已验收情况
- 明确未做事项
- 是否建议阶段 8 收口

6. 如需补测试，注意边界。

可改动文件：

- tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs
- 必要时小幅调整 App / Agent 代码以修复真实缺陷

不要为了文档收口修改核心控制语义。若发现严重问题，请修最小闭环并在完成说明里记录。

建议重点查看这些文件：

- src\QuantifiedSelf.Windows.Core\Ipc\AgentIpcRequest.cs
- src\QuantifiedSelf.Windows.Core\Ipc\AgentIpcResponse.cs
- src\QuantifiedSelf.Windows.Core\Ipc\AgentPipeName.cs
- src\QuantifiedSelf.Windows.Infrastructure\Ipc\NamedPipeProtocol.cs
- src\QuantifiedSelf.Windows.Infrastructure\Ipc\NamedPipeAgentControlClient.cs
- src\QuantifiedSelf.Windows.Agent\Services\AgentCommandServerHostedService.cs
- src\QuantifiedSelf.Windows.Agent\Services\ProcessedRequestCache.cs
- src\QuantifiedSelf.Windows.Agent\State\AgentStateMachine.cs
- src\QuantifiedSelf.Windows.App\Services\AgentStatusService.cs
- src\QuantifiedSelf.Windows.App\Services\AgentControlService.cs
- src\QuantifiedSelf.Windows.App\Services\AgentProcessService.cs
- src\QuantifiedSelf.Windows.App\Services\AgentIpcStatusService.cs
- src\QuantifiedSelf.Windows.App\ViewModels\MainWindowViewModel.cs
- src\QuantifiedSelf.Windows.App\ViewModels\SettingsViewModel.cs
- tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs

请完成必要修改后运行：

```powershell
dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\build-output\
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\test-output\
```

请不要并行运行 build 和 test，避免 obj 文件锁。

完成后清理临时输出：

```powershell
Get-ChildItem -Path src,tests -Recurse -Directory -Filter .codex | Remove-Item -Recurse -Force
```

完成说明和验收清单中请写入真实 build/test 数字。

注意：

- Get-Content 读取中文文档时使用 `-Encoding UTF8`。
- 不要回滚用户或其他 agent 的改动。
- 手工编辑文件请使用 apply_patch。
- 不要把未执行的手动验收项标成通过。
- 不要把阶段 9 的状态流订阅提前做进来。
