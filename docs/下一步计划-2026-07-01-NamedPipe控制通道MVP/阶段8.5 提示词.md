请在仓库 C:\Users\WangZhenchi\work\WUJI\Win 中实施阶段 8.5。

请先阅读并严格遵循这些文档，中文文档用 UTF-8 读取：

1. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP.md
2. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\README.md
3. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\05-阶段8.5-维护命令迁移到IPC.md
4. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8.1-完成说明-2026-07-01.md
5. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8.2-完成说明-2026-07-01.md
6. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8.3-完成说明-2026-07-01.md
7. docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8.4-完成说明-2026-07-01.md
8. docs\下一步计划-2026-06-24-PruneDataClearHistory数据清理MVP\阶段7-验收清单-2026-06-25.md
9. docs\下一步计划-2026-06-24-PruneDataClearHistory数据清理MVP\阶段7.6-完成说明-2026-06-25.md

任务目标：

实现“阶段 8.5：维护命令迁移到 IPC”。

本阶段只把阶段 7 已完成的数据维护命令迁移为 IPC 优先：

```text
PruneData
ClearHistory
```

必须保持阶段 7 的数据清理语义，不改变删除规则、不绕过 ClearHistory 二次确认、不删除 `agent_control.json` fallback。

尤其注意本阶段不做：

- 不改变 PruneData 的 cutoff / retentionDays / 删除范围。
- 不改变 ClearHistory 的 SQLite 事务规则、JSONL 删除规则和最终 Paused 语义。
- 不绕过 WPF 中 ClearHistory 的 `CLEAR` 二次确认。
- 不实现 confirmation token；只把它记录为后续增强。
- 不做清理进度条。
- 不做取消维护任务。
- 不做 VACUUM。
- 不做 Diagnostics 完整 IPC 状态展示，那是阶段 8.6。
- 不做断连手动验收清单，那是阶段 8.7。
- 不删除或弱化 `agent_control.json` fallback。
- 不把 WPF 改成直接删除 SQLite / JSONL。

需要实现的范围：

1. Agent server 支持维护命令。

当前相关文件：

- src\QuantifiedSelf.Windows.Agent\Services\AgentCommandServerHostedService.cs
- src\QuantifiedSelf.Windows.Agent\State\AgentStateMachine.cs
- src\QuantifiedSelf.Windows.Agent\Services\ProcessedRequestCache.cs

要求：

- 在 IPC dispatcher 中新增对以下 `AgentIpcRequest.Command` 字符串的支持：

```text
PruneData
ClearHistory
```

- 不要把 `Ping` 加回 `AgentCommandType`。
- 不要在 IPC server 里重新实现数据删除逻辑。
- 对 PruneData / ClearHistory 构造 `AgentControlCommand`，调用 `AgentStateMachine.ProcessCommandAsync`，复用阶段 7 的状态机语义。
- `AgentControlCommand.RequestId` 必须使用 IPC request 的 `RequestId`。
- `AgentControlCommand.RequestedBy` 使用 `request.RequestedBy`，为空时 fallback 到 `QuantifiedSelf.Windows.App`。
- `Reason` 使用安全固定文本，例如：

```text
IPC requested PruneData
IPC requested ClearHistory
```

- 不把完整 pipe name、SID、路径、原始异常 message、原始 JSON payload 拼进 message / payload。
- 将 `AgentCommandResult` 映射为 `AgentIpcResponse`：

```text
RequestId
Accepted
Completed
ActualState
Message
ErrorCode
StartedAtUtc
CompletedAtUtc
```

2. requestId 防重必须覆盖维护命令。

阶段 8.4 已引入 `ProcessedRequestCache`。本阶段要确认并补齐：

- PruneData / ClearHistory 通过 IPC 重复 requestId 时不能重复删除。
- 如果 IPC request 超时后 WPF 使用同一个 requestId 写 file fallback，Agent 端不能重复执行同一个有副作用维护命令。
- 重复命中返回安全结果：

```text
accepted = true
completed = true
errorCode = DuplicateRequest
message = Duplicate request ignored
actualState = 当前状态
```

- cache 仍然只做内存态，不落盘。
- 不要扩大 cache 为全局持久状态。

如果当前 file fallback 路径尚未进入同一个防重逻辑，需要评估并修复。阶段 8.5 的高风险点正是维护命令 response 超时后可能重复清理。

3. WPF AgentControlService 迁移 PruneData / ClearHistory 为 IPC 优先。

当前相关文件：

- src\QuantifiedSelf.Windows.App\Services\AgentControlService.cs
- src\QuantifiedSelf.Windows.App\Services\AgentIpcStatusService.cs
- src\QuantifiedSelf.Windows.App\ViewModels\SettingsViewModel.cs
- src\QuantifiedSelf.Windows.App\App.xaml.cs

要求：

- `PruneDataAsync` / `ClearHistoryAsync` 优先走 IPC。
- IPC request 使用同一个 requestId 贯穿 IPC 和 file fallback。
- IPC 成功时返回 IPC 映射出的 `AgentCommandResult`，并记录 `AgentIpcStatusService.RecordIpcSuccess()`。
- IPC 连接失败、pipe unavailable、connect timeout 等“未送达”场景 fallback 到现有 `agent_control.json` 写入逻辑，并记录 `RecordIpcFallback(...)`。
- IPC request timeout 需要谨慎处理：
  - 不要用新 requestId 盲目 fallback。
  - 推荐策略：用同一个 requestId fallback，并依赖 Agent 侧防重覆盖 IPC / file 两条路径。
  - 如果无法证明防重覆盖 file fallback，则 request timeout 时应返回安全超时结果，不重复投递维护命令。
- fallback 写入 `agent_control.json` 时必须保留同一个 `ipc-*` requestId。
- fallback 状态文本使用安全短句，不显示完整 pipe name、SID、路径或原始异常 message。

维护命令建议 timeout：

```text
MaintenanceCommandTimeoutMilliseconds = 30000
```

如果已有 `AgentIpcClientOptions.MaintenanceCommandTimeoutMilliseconds`，请复用；不要把基础命令的短 timeout 直接用于真实维护清理。

4. ClearHistory 二次确认必须保持 WPF 层语义。

当前相关文件：

- src\QuantifiedSelf.Windows.App\ViewModels\SettingsViewModel.cs
- src\QuantifiedSelf.Windows.App\Views\SettingsView.xaml

要求：

- 用户仍必须输入 `CLEAR` 后才能执行 ClearHistory。
- `ConfirmClearHistoryCommand` 的 can-execute 守卫不能被绕过。
- `ConfirmClearHistoryAsync` 方法体内的运行时守卫不能被移除。
- IPC 迁移只改变命令投递通道，不改变二次确认流程。
- Agent 端本阶段不强制检查 confirmation token。
- 在完成说明中明确记录：confirmation token 是后续增强，不属于本阶段。

5. 维护状态语义不能回归。

必须保持阶段 7 已验收语义：

```text
PruneData:
Running -> Maintenance -> Running
Paused  -> Maintenance -> Paused

ClearHistory:
Running -> Maintenance -> Paused
Paused  -> Maintenance -> Paused
```

要求：

- Maintenance 中重复 PruneData / ClearHistory 应被拒绝或安全返回，不得并发执行。
- Start / Pause / Resume / Stop 按钮在 Maintenance 中的既有守卫不能回归。
- `DataPruned` / `HistoryCleared` 事件仍在清理完成后写入。
- `CommandCompleted` / `CommandFailed` 仍按阶段 7 规则写入。
- `CommandFailed` message / payload 必须继续脱敏，不泄露路径。
- ClearHistory 后 `HistoryCleared` payload 的 `finalState` 仍应是 `Paused`，不要回退成 `Maintenance`。

6. 不要破坏阶段 8.1-8.4 既有行为。

必须保持：

- `Ping` IPC-only，不进入 `AgentCommandType`。
- `GetStatus.Status.DesiredState = null`，不要重新硬编码 Running。
- unsupported command 固定安全文案，不回显原始 command。
- server 异常后有 1 秒 delay，避免热循环。
- `NamedPipeAgentControlClient` 连接 timeout 和 request timeout 分离。
- `AgentStatusService.GetStatusAsync` 仍 IPC 优先，失败 fallback 到 runtime/health 文件。
- Pause / Resume / Stop / ReloadConfig 仍 IPC 优先且 file fallback 不回归。
- Stop 特殊语义不回归：IPC 发送/超时/pipe broken 后不能盲目留下 stale fallback。

建议重点查看这些文件：

- src\QuantifiedSelf.Windows.Core\Ipc\AgentIpcRequest.cs
- src\QuantifiedSelf.Windows.Core\Ipc\AgentIpcResponse.cs
- src\QuantifiedSelf.Windows.Core\Control\AgentControlCommand.cs
- src\QuantifiedSelf.Windows.Core\Control\AgentCommandResult.cs
- src\QuantifiedSelf.Windows.Core\Control\AgentCommandType.cs
- src\QuantifiedSelf.Windows.Core\Maintenance\PruneDataResult.cs
- src\QuantifiedSelf.Windows.Core\Maintenance\ClearHistoryResult.cs
- src\QuantifiedSelf.Windows.Agent\Services\AgentCommandServerHostedService.cs
- src\QuantifiedSelf.Windows.Agent\Services\ProcessedRequestCache.cs
- src\QuantifiedSelf.Windows.Agent\State\AgentStateMachine.cs
- src\QuantifiedSelf.Windows.Infrastructure\Database\DataMaintenanceService.cs
- src\QuantifiedSelf.Windows.App\Services\AgentControlService.cs
- src\QuantifiedSelf.Windows.App\Services\AgentIpcStatusService.cs
- src\QuantifiedSelf.Windows.App\ViewModels\SettingsViewModel.cs
- src\QuantifiedSelf.Windows.App\App.xaml.cs
- tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs

建议新增或更新测试：

- AgentCommandServer_PruneDataReturnsCompleted
- AgentCommandServer_ClearHistoryReturnsCompletedAndPaused
- AgentCommandServer_RejectsPruneDataDuringMaintenance
- AgentCommandServer_RejectsClearHistoryDuringMaintenance
- AgentCommandServer_DuplicatePruneDataRequestDoesNotDeleteTwice
- AgentCommandServer_DuplicateClearHistoryRequestDoesNotDeleteTwice
- AgentControlService_PruneDataUsesIpcWhenAvailable
- AgentControlService_ClearHistoryUsesIpcWhenAvailable
- AgentControlService_PruneDataFallsBackToFileWhenIpcUnavailable
- AgentControlService_ClearHistoryFallsBackToFileWhenIpcUnavailable
- AgentControlService_PruneDataTimeoutDoesNotUseNewRequestId
- AgentControlService_ClearHistoryTimeoutDoesNotUseNewRequestId
- SettingsViewModel_ClearHistoryStillRequiresConfirmationWithIpc
- SettingsViewModel_ClearHistoryCannotRunWhenConfirmationTextIsWrong
- SettingsViewModel_PruneDataUsesMaintenanceTimeout
- SettingsViewModel_ClearHistoryUsesMaintenanceTimeout

测试建议：

- Agent server 命令测试可直接调用 dispatcher，不必每个测试都开真实 pipe。
- WPF/App 侧 IPC 测试建议使用 fake `IAgentIpcClient`，避免真实 pipe 时序不稳定。
- 维护 timeout 测试必须短，不要真实等待 30 秒。
- fallback 测试要读取 `agent_control.json`，确认 requestId 与原 IPC requestId 一致。
- 防重测试要证明第二次相同 requestId 不会重复删除 SQLite rows / JSONL。
- ClearHistory 测试要证明二次确认仍在 ViewModel 层生效，不要直接绕过 UI 入口后宣称通过。
- 失败路径要断言安全 message，不包含 `C:\Users\...`、UNC 路径、完整 pipe name、SID 或原始 JSON。
- 测试结束必须释放 pipe / cancel token，不能留下后台 task。

验收标准：

- PruneData 通过 IPC 成功执行。
- ClearHistory 通过 IPC 成功执行。
- ClearHistory 仍无法绕过 WPF 的 `CLEAR` 二次确认。
- Maintenance 中重复 PruneData / ClearHistory 通过 IPC 被拒绝或安全返回。
- IPC 不可用时 PruneData / ClearHistory fallback 到 `agent_control.json` 后仍能被 Agent 文件通道处理。
- IPC request timeout 不会造成重复 PruneData / ClearHistory。
- fallback 使用同一个 `ipc-*` requestId。
- Diagnostics / agent_events 仍能看到 DataPruned / HistoryCleared / CommandCompleted / CommandFailed。
- DataPruned / HistoryCleared payload 不包含路径、标题、原始异常。
- ClearHistory 后 Agent 最终状态仍为 Paused。
- 阶段 7 数据清理测试不回归。
- 阶段 8.1-8.4 IPC 测试不回归。
- confirmation token 只作为后续增强记录，本阶段不实现。

请完成代码和测试修改后运行：

```powershell
dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\build-output\
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\test-output\
```

请不要并行运行 build 和 test，避免 obj 文件锁。

完成后清理临时输出：

```powershell
Get-ChildItem -Path src,tests -Recurse -Directory -Filter .codex | Remove-Item -Recurse -Force
```

如果 build/test 通过，请写一份完成说明到：

```text
docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8.5-完成说明-2026-07-01.md
```

完成说明请包含：

- 修改了哪些文件
- Agent server 如何把 PruneData / ClearHistory 映射到 `AgentStateMachine.ProcessCommandAsync`
- requestId 防重如何覆盖维护命令，是否覆盖 IPC / file fallback 两条路径
- WPF 侧 AgentControlService 如何 IPC 优先并 fallback
- PruneData / ClearHistory 使用了什么 timeout
- ClearHistory 二次确认如何保持 WPF 层语义
- Maintenance 状态和重复维护命令如何处理
- DataPruned / HistoryCleared / CommandCompleted / CommandFailed 事件是否保持阶段 7 语义
- 安全文案如何避免泄露完整 SID / pipe name / 路径 / 原始异常
- 新增/更新了哪些测试
- build/test 结果
- 明确说明没有实现 confirmation token
- 明确说明没有删除 `agent_control.json` fallback
- 明确说明没有做阶段 8.6 Diagnostics 完整展示
- 是否建议进入阶段 8.6

注意：

- Get-Content 读取中文文档时使用 `-Encoding UTF8`。
- 不要回滚用户或其他 agent 的改动。
- 手工编辑文件请使用 apply_patch。
- 不要把阶段 8.6 的 Diagnostics IPC 状态展示提前做进来。
- 不要把阶段 8.7 的断连验收清单提前做进来。
