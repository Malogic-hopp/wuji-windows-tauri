# 阶段 6 验收清单

更新时间：2026-06-24

## 自动化验收

- [x] `dotnet build` 通过，0 warning / 0 error。
- [x] `dotnet test` 通过，103/103。
- [x] `SettingsViewModel` 能读取 App Settings。 —— `SettingsViewModel_LoadsAppSettings`
- [x] `SettingsViewModel` 能读取 Agent Options。 —— `SettingsViewModel_LoadsAgentOptions`
- [x] 配置缺失时显示默认值，不报错。 —— `SettingsViewModel_ReturnsDefaultsWhenFilesMissing`
- [x] JSON 读取失败时显示安全错误文本（不泄露路径）。 —— `SettingsViewModel_RedactsLoadFailureAndShowsSafeStatusText`
- [x] `refreshIntervalSeconds` 校验 5..300，非法值拦截。 —— `SettingsViewModel_RejectsInvalidRefreshInterval`
- [x] `autoStartAgentWhenAppStarts` 可编辑保存。 —— `SettingsViewModel_SavesAppSettings`
- [x] 保存 App Settings 后 `AppSettingsSaved` 事件触发，`MainWindowViewModel` 应用新刷新间隔。 —— `MainWindowViewModel_AppliesRefreshIntervalAfterSettingsSave`
- [x] `lastSelectedPage` 只读展示，Settings 表单不提供编辑入口。 —— `SettingsViewModel_PreservesLastSelectedPageAsReadOnly`
- [x] `AgentOptionsValidator` 拦截无效间隔（sampling/idle/heartbeat/stale/retention）。 —— `AgentOptionsValidator_AcceptsDefaultOptions`, `_RejectsInvalidNumericRanges`
- [x] `staleThresholdSeconds` 必须大于 `heartbeatIntervalSeconds`。 —— `AgentOptionsValidator_RejectsStaleThresholdEqualToHeartbeat`
- [x] `excludedProcesses` 允许 `.exe` 输入，归一化去掉后缀。 —— `AgentOptionsValidator_NormalizesExcludedProcesses`
- [x] `excludedProcesses` 比较大小写不敏感。 —— `AgentOptionsValidator_DeduplicatesPrivacyRules`
- [x] `excludedProcesses` 拒绝路径输入。 —— `AgentOptionsValidator_RejectsProcessPaths`, `SettingsViewModel_RejectsPathLikeExcludedProcesses`
- [x] `excludedProcesses` 空行忽略、重复去重。 —— `AgentOptionsValidator_DeduplicatesPrivacyRules`
- [x] `excludedProcesses` 单条长度 ≤100，总数 ≤50。 —— `AgentOptionsValidator_RejectsOverlongExcludedProcesses`, `_RejectsExceedingMaxCount`
- [x] `excludedTitlePatterns` 空行忽略、重复去重。 —— `AgentOptionsValidator_DeduplicatesPrivacyRules`
- [x] `excludedTitlePatterns` 单条长度 ≤200，总数 ≤50。 —— `AgentOptionsValidator_RejectsOverlongExcludedTitlePatterns`, `_RejectsExceedingMaxCount`
- [x] Validate / Reset 按钮可用。 —— `SettingsViewModel_ValidatesAgentOptionsEditor`, `_ResetAgentOptionsEditorRestoresLoadedValues`
- [x] Save Agent Options 前走 `AgentOptionsValidator`，保存时归一化。 —— `SettingsViewModel_RejectsInvalidAgentOptionsBeforeSave`, `_SavesAgentOptionsWithBackup`
- [x] Save Agent Options 使用 `.tmp` + `.bak` 原子保存。 —— `WindowsAgentOptionsStore_WriteWithBackupAsync_*`
- [x] 保存失败不破坏正式配置。 —— `SettingsViewModel_SaveAgentOptionsFailureDoesNotCorruptExistingFile`
- [x] Save Agent Options 不自动 Reload。 —— `SettingsViewModel_SaveAgentOptionsDoesNotReloadAgent`
- [x] Save App Settings 不会顺手保存 Agent Options。 —— `SettingsViewModel_SaveAppSettingsDoesNotSaveAgentOptions`
- [x] Restore Backup 恢复文件成功，不自动 Reload。 —— `SettingsViewModel_RestoreBackupDoesNotReloadAutomatically`
- [x] `SaveAndReloadAgentOptionsAsync` 先校验、保存、再发 ReloadConfig。 —— `SettingsViewModel_SaveAndReloadSavesThenRequestsReload`
- [x] Agent Running/Paused 时 Reload 可用，NotRunning/Stopped 时不可用。 —— `SettingsViewModel_ReloadConfigDisabledWhenAgentNotRunning`
- [x] 控制文件读写和 malformed JSON 处理正常。 —— `AgentControlFileStore_RoundsTripStringEnums_AndFlagsMalformedFiles`
- [x] Agent ReloadConfig 使用共享 `AgentOptionsValidator`，校验失败或读取失败不替换当前 `_options`。 —— `AgentStateMachine_ReloadConfigRejectsInvalidOptionsAndKeepsCurrentOptions`, `_ReloadConfigReadFailureKeepsCurrentOptions`
- [x] ReloadConfig 成功后替换为 normalized options。 —— `AgentStateMachine_ReloadConfigUsesValidatorAndNormalizesOptions`
- [x] ReloadConfig 成功写 `ConfigReloaded` / `CommandCompleted`。 —— `AgentStateMachine_WritesConfigReloadedEvent`
- [x] ReloadConfig 失败写 `CommandFailed` + `errorCode`，错误文本不泄露路径。 —— `AgentStateMachine_ReloadConfigRejectsInvalidOptionsAndKeepsCurrentOptions`
- [x] `ConfigReloaded` 携带 `requestId`，与 `CommandCompleted` 可精确关联。 —— `SettingsViewModel_PollsUntilConfigReloadedObserved`
- [x] ReloadConfig 后 `excludedProcesses` 更新对后续采样生效。 —— `AgentStateMachine_ReloadConfigUsesValidatorAndNormalizesOptions`
- [x] ReloadConfig 后 `excludedTitlePatterns` 更新对后续采样生效。 —— `AgentStateMachine_ReloadConfigAppliesUpdatedExcludedTitlePatterns`
- [x] `PrivacyFiltered` payload 不含真实窗口标题，只含泛化 `privacyReason`。 —— `PrivacyFilter_ExcludesProcesses_AndMasksWindowTitles`, `AgentStateMachine_WritesPrivacyFilteredEvents_AndRateLimitsThem`, `AgentStateMachine_ReloadConfigAppliesUpdatedExcludedTitlePatterns`
- [x] `maskWindowTitles=true` 后 sample 不包含真实标题。 —— `PrivacyFilter_ExcludesProcesses_AndMasksWindowTitles`
- [x] `AgentStateMachine.InitializeAsync` 启动时走 `AgentOptionsValidator`，非法配置 fallback 到默认值。 —— `AgentStateMachine_InitializeAsync_FallsBackToDefaultsForInvalidConfig_UsesNormalizedForValidConfig`
- [x] ReloadConfig polling 成功/失败路径 UI 反馈正确。 —— `SettingsViewModel_PollsUntilConfigReloadedObserved`, `_PollsCommandFailedAndShowsError`
- [x] 服务层 `AgentControlService.ReloadConfigAsync` Agent NotRunning 时拒绝写 control file。 —— 静态检查 `IssueCommandAsync` 中 ReloadConfig 分支
- [x] 导航顺序为 `Dashboard / Apps / Sessions / Samples / Diagnostics / Settings`。
- [x] `LastSelectedPage` 映射正确，启动后导航到记录页。
- [x] 全局 Refresh 不覆盖 Settings 未保存编辑。 —— `IsDirty` 守卫

## 手动验收

- [x] Settings 页面可打开，无异常弹窗。
- [x] App Settings 区域显示 `refreshIntervalSeconds`、`autoStartAgentWhenAppStarts`、`lastSelectedPage (read-only)`。
- [x] `lastSelectedPage` 显示为只读文本，不是可编辑下拉框。
- [x] Agent Options 区域显示采集参数、隐私规则、操作按钮和归一化预览。
- [x] Local Data 区域显示 data root / config / database / logs / runtime 路径。
- [x] 修改 Settings 字段但不保存，等待自动刷新后输入内容不丢失。
- [x] 修改 `refreshIntervalSeconds` 保存后，自动刷新节奏与新间隔一致。
- [x] 编辑 `excludedProcesses`、`excludedTitlePatterns` 多行文本正常。
- [x] 输入 `notepad.exe` 后 Validate，归一化预览显示 `notepad`。
- [x] 输入路径形式进程规则后校验失败，提示不是路径。
- [x] Save and Reload 成功，UI 显示 "ReloadConfig succeeded"。
- [x] Diagnostics 能看到 `CommandDetected`、`ConfigReloaded`、`CommandCompleted` 事件。
- [x] `ConfigReloaded` 与 `CommandCompleted` 可通过同一 requestId 关联。
- [x] Agent NotRunning 时 Reload / Save and Reload 按钮不可用，文案提示下次启动生效。
- [x] NotRunning 时 Save Agent Options 仍可用，不宣称已应用。
- [x] Restore Backup 恢复文件成功，UI 提示仍需 ReloadConfig 或下次启动。
- [x] Restore 后不自动 Reload，手动 Reload 后 Agent 应用恢复配置。
- [x] 进程隐私规则（notepad）生效：Diagnostics 出现 `PrivacyFiltered`（`ruleType=Process`），foreground_samples / app_sessions 不新增 Notepad。
- [x] 标题隐私规则生效：命中标题的窗口不落库，payload 不含真实标题。
- [x] `maskWindowTitles=true` 后 Samples 不显示真实窗口标题。
- [x] Dashboard / Apps / Sessions / Samples / Diagnostics 各页面数据、筛选、状态显示正常。
- [x] Start / Pause / Resume / Stop 控制正常，状态变化正确。
- [x] 验收后恢复配置（如运行过手动脚本），最终 build / test 通过。

## 最终通过标准

- [x] Settings 页面能稳定查看 App Settings / Agent Options / Local Data
- [x] App Settings 保存和校验可用
- [x] Agent Options 保存、校验、Reset 可用
- [x] Save Agent Options 不误导用户认为 Agent 已应用
- [x] Save and Reload 能真实应用配置
- [x] Diagnostics 能看到 ReloadConfig 成功 / 失败证据
- [x] 隐私规则修改后对后续采样真实生效
- [x] Restore Backup 能恢复文件且不自动宣称生效
- [x] Dashboard / Apps / Sessions / Samples / Diagnostics 不回归
- [x] build / test 全部通过

一句话验收结论：

```
Settings 与配置应用 MVP 可以正式收口，进入阶段 7：PruneData / ClearHistory 数据清理 MVP。
```
