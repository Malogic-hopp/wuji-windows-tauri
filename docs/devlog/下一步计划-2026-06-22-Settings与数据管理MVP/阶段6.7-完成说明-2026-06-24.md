# 阶段 6.7 交付说明（2026-06-24）

Build: 0 warnings / 0 errors | Test: 103 / 103 passed

## 修改的文件

- src/QuantifiedSelf.Windows.App/Services/AgentControlService.cs
  —— D: ReloadConfig 时 Agent NotRunning 拒绝写 control file（服务层防御）

- src/QuantifiedSelf.Windows.Agent/State/AgentStateMachine.cs
  —— E: ConfigReloaded 事件携带 command.RequestId；WriteLifecycleEventAsync 新增 requestId 参数

- src/QuantifiedSelf.Windows.App/ViewModels/SettingsViewModel.cs
  —— E: PollForReloadConfigResultAsync 用 requestId 精确匹配 ConfigReloaded
  —— A: 可注入 poll 参数（ReloadConfigPollMaxAttempts / ReloadConfigPollDelay）
  —— B: LastSelectedPageText setter 去掉 IsDirty 标记

- src/QuantifiedSelf.Windows.App/Views/SettingsView.xaml
  —— B: lastSelectedPage 从 ComboBox 改为只读 TextBlock

- src/QuantifiedSelf.Windows.App/QuantifiedSelf.Windows.App.csproj
  —— A: InternalsVisibleTo 开放给测试项目

- tests/QuantifiedSelf.Windows.Tests/DataFlowTests.cs
  —— A: 新增 2 个 polling 测试（成功路径 + CommandFailed 路径）
  —— B: lastSelectedPage 测试调整为只读语义

- docs/下一步计划-2026-06-22-Settings与数据管理MVP/06-手动验收-隐私规则生效.ps1
  —— C: 探测顺序对齐 WindowsAgentPaths（-AgentRoot → QUANTIFIEDSELF_WINDOWS_AGENT_ROOT → D:\WUJI\WindowsAgent → %LOCALAPPDATA%）

- docs/下一步计划-2026-06-22-Settings与数据管理MVP/阶段6-完成说明-2026-06-24.md
  —— 新增：阶段 6 总完成说明文档

- docs/下一步计划-2026-06-22-Settings与数据管理MVP/README.md
  —— 更新：所有子阶段标记完成，链接到完成说明

## 审查项修复情况

A —— ReloadConfig polling 成功/失败路径测试未覆盖
  已修复。新增 SettingsViewModel_PollsUntilConfigReloadedObserved（验证 CommandCompleted + ConfigReloaded → UI 显示 success）和 SettingsViewModel_PollsCommandFailedAndShowsError（验证 CommandFailed + errorCode → UI 显示 failed + HasAgentOptionsReloadError=true）。poll 参数可注入，测试零延迟。

B —— lastSelectedPage 可编辑 vs 文档只读
  已修复。XAML 中 ComboBox 改为只读 TextBlock；LastSelectedPageText setter 移除 IsDirty 标记；TryBuildAppSettings 不再从编辑器读取 lastSelectedPage，改为保留已加载值。新增 SettingsViewModel_PreservesLastSelectedPageAsReadOnly 验证保存 App Settings 时不会改写 lastSelectedPage。

C —— 手动验收脚本 AgentRoot 探测补齐
  已修复。探测顺序：-AgentRoot 参数 → QUANTIFIEDSELF_WINDOWS_AGENT_ROOT 环境变量 → D:\WUJI\WindowsAgent（如果存在）→ %LOCALAPPDATA%\WUJI\WindowsAgent。与 WindowsAgentPaths 构造函数完全对齐。

D —— 服务层 NotRunning 防御是否处理
  已修复。AgentControlService.IssueCommandAsync 中，对 ReloadConfig 命令增加 Agent 状态检查：如果 Agent 不在运行中，返回 Accepted=false，不写 control file。范围限于 ReloadConfig，不触及 Pause/Resume/Stop 语义。

E —— ConfigReloaded 无 requestId 关联
  已修复。WriteLifecycleEventAsync 新增 requestId 可选参数；ConfigReloaded 事件携带 command.RequestId；PollForReloadConfigResultAsync 用 requestId 精确匹配 ConfigReloaded，不再依赖时间窗口。

## 新增/更新测试

- SettingsViewModel_PollsUntilConfigReloadedObserved
  polling 成功路径：getRecentAgentEventsAsync 返回 CommandCompleted + ConfigReloaded（均携带 requestId），UI 显示 "ReloadConfig succeeded"

- SettingsViewModel_PollsCommandFailedAndShowsError
  polling 失败路径：getRecentAgentEventsAsync 返回 CommandFailed（携带 requestId + errorCode），UI 显示 "ReloadConfig failed"，HasAgentOptionsReloadError=true

- SettingsViewModel_PreservesLastSelectedPageAsReadOnly
  验证 lastSelectedPage 为只读展示，保存 App Settings 时从加载值保留，不会被编辑器改写

## build/test 结果

  dotnet build QuantifiedSelf.Windows.sln --no-restore
  0 warnings / 0 errors

  dotnet test QuantifiedSelf.Windows.sln --no-restore
  103 / 103 passed

## 是否建议进入阶段 7

是。阶段 6 全部 6 个 Finding 已关闭，7 个子阶段均完成，自动化验证通过，手动验收脚本就绪。可以正式结束阶段 6 并进入阶段 7 PruneData / ClearHistory 数据清理 MVP。
