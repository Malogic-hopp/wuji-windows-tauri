你现在在仓库 C:\Users\WangZhenchi\work\WUJI\Win 工作。请实施阶段 6.6：隐私规则编辑与生效验收。

背景：
阶段 6.1-6.5 已完成：
- Settings 独立页面已存在。
- App Settings 可编辑保存。
- Agent Options 可编辑、本地校验、保存、备份、恢复。
- AgentOptionsValidator 已在 Core 层。
- Save and Reload / ReloadConfig 链路已接上。
- 当前 build 0 warning / 0 error，test 96/96 passed。
- 已修复 Settings 页全局 Refresh 覆盖未保存编辑的问题。
- 已修复 Agent 启动时 options 不走 validator 的问题。

请先阅读这些文档，注意中文编码用 UTF-8：
- docs\下一步计划-2026-06-22-Settings与数据管理MVP.md
- docs\下一步计划-2026-06-22-Settings与数据管理MVP\README.md
- docs\下一步计划-2026-06-22-Settings与数据管理MVP\06-阶段6.6-隐私规则编辑与生效验收.md
- docs\下一步计划-2026-06-22-Settings与数据管理MVP\阶段6.1-6.5代码审查报告-2026-06-23.md

重点代码：
- src\QuantifiedSelf.Windows.App\ViewModels\SettingsViewModel.cs
- src\QuantifiedSelf.Windows.App\Views\SettingsView.xaml
- src\QuantifiedSelf.Windows.Core\Options\AgentOptionsValidator.cs
- src\QuantifiedSelf.Windows.Core\Options\WindowsAgentOptions.cs
- src\QuantifiedSelf.Windows.Agent\State\AgentStateMachine.cs
- src\QuantifiedSelf.Windows.Agent\Services\ForegroundSamplePrivacyFilter.cs
- tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs

阶段 6.6 目标：
让 Settings 页中的隐私规则编辑形成完整闭环，并能证明 ReloadConfig 后对后续采样真实生效。

请实现或补齐以下内容：

1. 隐私规则编辑体验
- `excludedProcesses` 支持多行输入。
- `excludedTitlePatterns` 支持多行输入。
- `excludedProcesses` 允许用户输入 `notepad` / `notepad.exe`。
- 保存时归一化为不带 `.exe`。
- 比较时大小写不敏感。
- 路径输入必须被拒绝，例如 `C:\Windows\System32\notepad.exe`。
- 空行要忽略。
- 重复项要去重。
- UI 上应展示归一化预览，让用户能看到最终保存形态。
- 不要显示或保存窗口标题等敏感内容。
- 不要引入 PruneData / ClearHistory / pending 文件机制。

2. 生效链路
- Save Agent Options 只保存文件，不自动 Reload。
- Save and Reload 先校验、保存归一化配置，再发送 ReloadConfig。
- ReloadConfig 成功后，后续采样必须使用新隐私规则。
- 如果 Agent NotRunning，Reload 按钮不可用，状态文案说明“下次启动生效”。
- Restore Backup 只恢复文件，不自动 Reload；恢复后仍需 ReloadConfig 或下次启动。

3. 测试要求
请补充或完善测试，至少覆盖：
- SettingsViewModel：输入 `notepad.exe` 后 Validate/Save 预览和保存值为 `notepad`。
- SettingsViewModel：输入路径形式进程规则时校验失败，且不保存。
- SettingsViewModel：空行和重复隐私规则会被清理/去重。
- Save and Reload：保存归一化隐私规则后发送 ReloadConfig。
- AgentStateMachine：ReloadConfig 后 `excludedProcesses = ["notepad.exe"]` 能归一化为 `notepad`，后续 Notepad sample 被 PrivacyFiltered，不写入 foreground_samples。
- AgentStateMachine：ReloadConfig 后 `excludedTitlePatterns` 命中时，不写 foreground sample，事件 payload 只包含泛化 privacyReason，不包含真实窗口标题。
- 如果现有测试已经覆盖其中某些点，请不要重复堆测试；只补缺口。

4. 手动验收辅助
如阶段文档要求，请根据需要新增或更新手动验收脚本，放在：
docs\下一步计划-2026-06-22-Settings与数据管理MVP\
建议脚本可以辅助验证：
- 通过 Settings 保存 notepad 进程隐私规则后，ReloadConfig 生效。
- 打开 Notepad 后 Diagnostics 能看到 PrivacyFiltered。
- foreground_samples 不新增 Notepad 样本。
脚本不要破坏用户现有配置；如会修改配置，必须创建备份并提供 restore 脚本或自动恢复说明。

5. 编译和测试
这次请你执行：
dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\build-output\
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\test-output\

完成后清理测试输出：
Get-ChildItem -Path src,tests -Recurse -Directory -Filter .codex | Remove-Item -Recurse -Force

6. 审慎边界
- 不要重构无关页面。
- 不要改阶段 7 的数据清理功能。
- 不要把 lastSelectedPage 语义作为本阶段主任务，除非测试被它阻塞。
- 不要回滚用户或其他 agent 的已有修改。
- 保持代码风格和现有 MVVM / DataFlowTests 写法一致。

交付时请说明：
- 修改了哪些文件。
- 阶段 6.6 的验收点完成情况。
- 新增/更新了哪些测试。
- build/test 结果。
- 是否还有必须进入 6.7 前处理的问题。