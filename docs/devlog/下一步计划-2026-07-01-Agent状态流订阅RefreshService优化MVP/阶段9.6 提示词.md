请在仓库 `C:\Users\WangZhenchi\work\WUJI\Win` 中实施阶段 9.6。

请先阅读并严格遵循这些文档，中文文档用 UTF-8 读取：

1. `docs\下一步计划-2026-07-01-Agent状态流订阅RefreshService优化MVP.md`
2. `docs\下一步计划-2026-07-01-Agent状态流订阅RefreshService优化MVP\README.md`
3. `docs\下一步计划-2026-07-01-Agent状态流订阅RefreshService优化MVP\06-阶段9.6-断连重连Agent重启场景验收.md`
4. `docs\下一步计划-2026-07-01-Agent状态流订阅RefreshService优化MVP\阶段9.4-完成说明-2026-07-02.md`
5. `docs\下一步计划-2026-07-01-Agent状态流订阅RefreshService优化MVP\阶段9.5-完成说明-2026-07-02.md`
6. `docs\Agent Start按钮无响应问题记录-2026-07-02.md`
7. `docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8-完成说明-2026-07-01.md`

任务目标：

实现“阶段 9.6：断连 / 重连 / Agent 重启场景验收”。

本阶段重点不是新增大功能，而是围绕阶段 9.1-9.5 已建立的 RefreshService、2 秒 status polling、command availability、status/page refresh 解耦和 Diagnostics RefreshHealth，补齐断连、重连、Agent 重启、Maintenance、Settings dirty 和 Stale 状态的专项验证。

尤其注意本阶段不做：

- 不实现真正长连接状态流、subscribe 协议或 Named Pipe streaming。
- 不改变阶段 9.2 的 2 秒 status polling timer interval。
- 不改变阶段 9.4 的 status/page refresh gate 结构，除非发现明确 bug 且测试证明修复。
- 不重写阶段 9.3 的 command availability 架构。
- 不回退 `Stale + IsRunning=false` 时 Start Agent 可用的修复。
- 不改变 Start / Pause / Resume / Stop / ReloadConfig / PruneData / ClearHistory 的命令执行链路。
- 不改变 ClearHistory 的 `CLEAR` 二次确认流程。
- 不改 AgentStateMachine 的业务语义。
- 不改 Named Pipe IPC 协议。
- 不把 Samples / Sessions / Apps 数据查询搬到 IPC。
- 不改 SQLite 查询服务语义。
- 不新增托盘、自启、安装包、Windows Service。
- 不新增 refresh health 持久化文件。
- 不把 RefreshHealth 写入 agent_events / JSONL / SQLite。

需要实现的范围：

1. 补齐断连 / 重连自动化覆盖。

围绕现有可测入口补充自动化测试，不依赖真实 2 秒 timer，不用长时间 `Thread.Sleep` 或长 `Task.Delay`。

建议优先覆盖这些场景：

```text
Agent NotRunning:
    status polling fallback 可返回安全状态
    UI 不崩溃
    ReloadConfig 保留“下次启动生效”语义
    PruneData / ClearHistory 不可用

IPC unavailable:
    status refresh fallback 到 runtime_state / health_state
    RefreshHealth 记录安全错误或 fallback 信息
    Diagnostics 显示 safe error / fallback 状态
    不显示 FullPipeName / SID / 本机路径 / SQL / 原始 JSON

Agent restart:
    状态可以从 NotRunning / Stale 恢复到 Running
    IPC 状态可以从 unavailable / fallback 恢复到 NamedPipe
    按钮状态随同一份 AgentStatusSnapshot 恢复

Maintenance:
    状态轮询能看到 Maintenance
    MainWindow 控制按钮禁用
    Settings ReloadConfig / PruneData / ClearHistory 禁用
    结束后恢复 Running 或 Paused 的按钮状态

Settings dirty:
    配置字段不被 status polling 或 page refresh 覆盖
    Agent 状态仍更新
    Settings 内按钮可用性仍更新
```

2. 明确覆盖 Stale + 进程不存在的 Start 场景。

阶段 9.5 前后已经记录过实际问题：

```text
runtime_state.json / health_state.json 残留 Running
但 Agent 进程已经不存在
App 合成为 Stale + IsRunning=false
Start Agent 应可用
```

本阶段必须确认该修复不回归。

要求：

```text
Stale + IsRunning=false:
    CanStart = true
    CanReloadConfigNow = false
    PruneData / ClearHistory = false

Stale + IsRunning=true:
    CanStart = false
    CanStop = true
    Pause / Resume / ReloadConfig / PruneData / ClearHistory 按保守策略不可用
```

如现有测试已经覆盖，也要保留并在完成说明中列出；如覆盖不完整，请补测试。

3. 断连后 UI 不应被默认状态覆盖。

IPC 失败、fallback 失败、状态文件缺失或陈旧时：

```text
不得用 new AgentStatusSnapshot() 之类默认快照覆盖顶部真实状态。
不得错误清空页面列表。
不得让按钮进入和顶部状态不一致的漂移状态。
不得让 RefreshHealth.IsStatusRefreshing / IsPageRefreshing 残留为 true。
```

如果没有可靠新状态：

```text
保持上一份已应用状态；
显示安全错误；
Diagnostics RefreshHealth 说明 degraded / fallback / unavailable；
按钮可用性按上一份可靠状态或明确 NotRunning/Stale 状态计算。
```

具体策略请遵循现有 9.2-9.5 实现，不要大改；如发现现有实现有 bug，只做最小修复并加测试。

补充限制：

```text
如果测试暴露 bug，只允许在当前断连 / 重连 / 状态派发 / RefreshHealth 范围内做最小修复。
不得重构 RefreshService、AgentStatusService、AgentControlService 或 SettingsViewModel 的整体架构。
不得为了通过阶段 9.6 测试而扩大到阶段 9.7 长跑收口或后续托盘 / 自启能力。
```

4. 重连恢复必须 latest-wins。

断连恢复时常见竞态：

```text
旧 fallback / stale 结果晚于新的 IPC Running 结果返回。
```

要求确认：

```text
旧 sequence 结果不得覆盖新 sequence 结果。
状态从 fallback / stale 恢复到 IPC Running 后，Diagnostics 和按钮状态同步恢复。
完整刷新和 status polling 均遵守 latest-wins。
```

断连恢复到 Running 后，至少验证这些状态同步恢复：

```text
顶部 AgentStatusText / LastHeartbeatText。
MainWindow Start / Stop / Pause / Resume 控制按钮。
Settings ReloadConfig / PruneData / ClearHistory 可用性。
Diagnostics IPC / fallback / RefreshHealth 文案。
```

如果现有测试已覆盖，请保留；如果没有覆盖断连恢复路径，请补一个轻量测试。

5. Diagnostics 断连 / 重连展示。

阶段 9.5 已新增 RefreshHealth 展示。本阶段需要确认断连 / 重连场景下 Diagnostics 能解释状态来源：

```text
IPC unavailable / fallback used 可见。
DisplayPipeName 可见。
FullPipeName 不可见。
完整 SID 不可见。
Last IPC error 是安全短句。
RefreshHealth 能显示 status/page 最近成功、最近错误、skipped count。
重连成功后 IPC / RefreshHealth 展示能恢复。
```

不要为了展示这些信息主动触发额外 refresh 递归。

6. Maintenance 与数据清理链路验收。

围绕 PruneData / ClearHistory 已有阶段 7 和阶段 8 链路，确认阶段 9 的状态刷新不会破坏：

```text
PruneData 进入 Maintenance 后，MainWindow 和 Settings 控制 / 清理按钮禁用。
PruneData 完成后恢复 Running 或 Paused。
ClearHistory 输入 CLEAR 后执行，完成后 Paused。
Maintenance 中 ConfirmClearHistoryCommand 不可用，即使已输入 CLEAR 也不得发送命令。
```

本阶段不重写数据清理逻辑，只补断连 / 状态同步相关验证。

7. Settings dirty 验收。

必须保留：

```text
Settings IsDirty=true 时，不调用配置 LoadAsync 覆盖 App Settings / Agent Options 草稿。
status polling 仍更新顶部 Agent 状态。
Settings 内 ReloadConfig / PruneData / ClearHistory 可用性仍跟随 AgentStatusSnapshot。
Diagnostics RefreshHealth / IPC 状态仍可更新。
```

建议补或保留测试：

```text
MainWindowViewModel_StatusPollingUpdatesStatusWhenSettingsDirty
MainWindowViewModel_StatusPollingDoesNotLoadSettingsWhenDirty
SettingsViewModel_UpdateAgentStatusDoesNotReloadConfigDrafts
```

8. 手动验收清单或脚本。

本阶段可以更新或新增一份轻量手动验收文档，帮助后续阶段 9.7 长跑前执行断连 / 重连检查。

建议新增：

```text
docs\下一步计划-2026-07-01-Agent状态流订阅RefreshService优化MVP\阶段9.6-手动验收清单-YYYY-MM-DD.md
```

如已有更合适的阶段 9 验收清单雏形，也可以更新已有文件。

清单建议包含表格列：

```text
编号
场景
操作步骤
预期结果
结果
备注
```

至少包含：

```text
1. 启动 App + Agent，状态约 2 秒内更新为 Running。
2. Pause，顶部状态和按钮快速变为 Paused。
3. Resume，顶部状态和按钮快速变为 Running。
4. PruneData，Maintenance 中按钮禁用，完成后恢复。
5. ClearHistory，输入 CLEAR 后成功，完成后 Paused。
6. Stop Agent，UI 不崩溃，Diagnostics 显示 IPC unavailable / fallback。
7. Agent 停止但状态文件残留 Running 时，Start Agent 仍可用。
8. 重启 Agent 后，IPC 和状态自动恢复。
9. Settings 编辑未保存时等待自动刷新，编辑内容不被覆盖。
10. Apps / Sessions / Samples 慢刷新期间，顶部 Agent 状态不被拖慢。
```

手动清单不能替代能自动化覆盖的测试。凡是可用 fake service / fake status / fake IPC 构造的断连、重连、Stale、Maintenance、Settings dirty 场景，应优先写自动化测试。

手动清单可以先不全部打勾，由人工执行后再回填。清单创建后，未实际执行的项目结果列填写 `Pending` / `Not Run`，不要把未执行项写成 `Passed`。

建议重点查看这些现有文件：

- `src\QuantifiedSelf.Windows.App\Services\RefreshService.cs`
- `src\QuantifiedSelf.Windows.App\Services\RefreshHealthSnapshot.cs`
- `src\QuantifiedSelf.Windows.App\Services\RefreshResult.cs`
- `src\QuantifiedSelf.Windows.App\Services\AgentCommandAvailability.cs`
- `src\QuantifiedSelf.Windows.App\Services\AgentStatusService.cs`
- `src\QuantifiedSelf.Windows.App\Services\AgentIpcStatusService.cs`
- `src\QuantifiedSelf.Windows.App\ViewModels\MainWindowViewModel.cs`
- `src\QuantifiedSelf.Windows.App\ViewModels\SettingsViewModel.cs`
- `src\QuantifiedSelf.Windows.App\MainWindow.xaml`
- `tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs`

建议新增或调整测试：

- `AgentCommandAvailability_AllowsStartWhenStaleButProcessGone`
- `AgentCommandAvailability_HandlesStaleWithRunningProcessConservatively`
- `MainWindowViewModel_StatusPollRecoversFromStaleToRunning`
- `MainWindowViewModel_StatusPollDoesNotApplyOlderFallbackAfterIpcRecovery`
- `MainWindowViewModel_IpcUnavailableShowsSafeDiagnosticsAndFallback`
- `MainWindowViewModel_NotRunningDisablesDataCleanupButKeepsReloadNextStartSemantics`
- `MainWindowViewModel_MaintenanceDisablesMainAndSettingsCommands`
- `MainWindowViewModel_SettingsDirtyKeepsDraftsButUpdatesAgentStatus`
- `MainWindowViewModel_StatusFailureClearsRefreshingFlags`
- `MainWindowViewModel_RestartRestoresIpcStatusAndCommandAvailability`

测试要求：

- 不依赖真实 2 秒 timer；优先直接调用 `PerformStatusPollAsync`、`RefreshAsync` 或可测 service 方法。
- 用 fake IPC / fake status service / fake process info 构造断连和恢复。
- Agent restart 自动化测试优先用 fake status service 模拟 `NotRunning/Stale -> Running`，不要启动真实 Agent 进程，不要依赖真实 Named Pipe server。
- 用 TaskCompletionSource 控制异步竞态，不用长 sleep。
- 异步测试使用 `async Task` + `await`，不要用 `.GetAwaiter().GetResult()`。
- 错误文本测试需包含路径、FullPipeName、SID、SQL 或原始 JSON，断言展示文本不泄露原文。
- 任何 status/page refresh 异常、取消或跳过路径，都必须确保 `IsStatusRefreshing` / `IsPageRefreshing` 最终恢复 `false`；实现上建议使用 `finally` 或等价机制。
- 保持 build 0 warning。

验收标准：

- build 0 warning / 0 error。
- test 全部通过。
- Agent NotRunning / IPC unavailable / fallback 场景 UI 不崩溃。
- Stale + IsRunning=false 时 Start 可用。
- Stale + IsRunning=true 时 Start 不可用，Stop 可用。
- Agent restart 后状态和 IPC 展示可恢复。
- Maintenance 可见且按钮禁用。
- Settings IsDirty 不回归，草稿不被覆盖。
- Diagnostics 能解释 IPC / fallback / RefreshHealth，且错误安全脱敏。
- latest-wins 不回归，旧 fallback 不覆盖新 IPC Running。
- 阶段 9.2 status polling 测试不回归。
- 阶段 9.3 command availability 测试不回归。
- 阶段 9.4 status/page refresh 解耦测试不回归。
- 阶段 9.5 Diagnostics RefreshHealth 测试不回归。
- 阶段 8 IPC 测试不回归。
- 阶段 7 PruneData / ClearHistory 测试不回归。

请完成代码、测试和必要文档修改后运行：

```powershell
dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\build-output\
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\test-output\
```

如果 build/test 通过，请汇总：

- 修改了哪些文件
- 实现或修复了哪些行为
- 新增/更新了哪些测试
- 是否新增/更新了手动验收清单
- build/test 结果

同时请把完成情况落盘为完成说明文档：

```text
docs\下一步计划-2026-07-01-Agent状态流订阅RefreshService优化MVP\阶段9.6-完成说明-YYYY-MM-DD.md
```

`YYYY-MM-DD` 使用执行当天日期；如果执行环境无法确定日期，使用 `2026-07-02`。

完成说明至少包含：

```text
1. 阶段目标
2. 修改文件清单
3. 断连 / 重连 / Agent restart 验证说明
4. Stale + IsRunning=false Start 可用回归说明
5. Diagnostics IPC / fallback / RefreshHealth 展示说明
6. Maintenance 与 Settings dirty 验证说明
7. 新增 / 更新测试
8. 手动验收清单位置与待人工执行项
9. build / test 结果
10. 明确未做事项
11. 风险与遗留问题
```

其中“明确未做事项”需要特别确认：

```text
未实现真正长连接状态流 / subscribe 协议
未改变 2 秒 status polling interval
未改变 status/page refresh gate 架构，除非只做明确 bugfix
未改变 command availability 架构
未回退 Stale + IsRunning=false 可 Start 修复
未改变命令执行链路
未改变 ClearHistory 的 CLEAR 二次确认
未改变 IPC 协议
未把页面数据查询搬到 IPC
未写 refresh health 到 agent_events / JSONL / SQLite / runtime 文件
未做托盘、自启、安装包、Windows Service
```

注意：

- `Get-Content` 读取中文文档时使用 `-Encoding UTF8`。
- 不要回滚用户或其他 agent 的改动。
- 不要让完成说明自行判断是否进入下一阶段；只写事实、验证结果、未做事项、风险与遗留问题。
- 本阶段可以修复断连 / 重连验收中发现的小 bug，但不要借机大改刷新架构。
- 任何错误展示都不得泄露路径、SQL、FullPipeName、SID、窗口标题或原始 JSON。
