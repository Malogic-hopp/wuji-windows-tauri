可行，这份阶段 11.6 提示词**可以交给 agent 执行**。它的定位很准确：11.6 是阶段 11 的**收口阶段**，主要做自动化验收、风险复核、验收清单和完成说明，不新增功能、不替代人工做真实 Windows 手动验收。这个边界和阶段 11.6 文档一致。

---

# 总体评价

这份提示词已经比较成熟，尤其是这几点写得很好：

```text
1. 明确本阶段不新增功能。
2. 明确 build/test 全量回归。
3. 明确手动验收和长跑验证只输出清单，不能虚假打勾。
4. 明确风险复核 13 项逐条记录。
5. 明确如果实际验收未全部通过，不能直接写“已完成”。
6. 明确重复启动行为要记录，如果出现双托盘/双窗口，single-instance 提升优先级。
```

这些都符合阶段 11 收口阶段的要求。

---

# 我认为写得好的地方

## 1. 没有让 agent 假装完成手动验收

你最后写了：

```text
手动验收和长跑验证需要真实 Windows 环境，agent 只能输出清单和结论模板，无法替代人工勾选；完成说明中要明确标注哪些项是“待人工确认”。
```

这非常关键。11.6 的手动验收涉及 HKCU Run Key、系统托盘、真实 WPF App、Agent 进程、重复启动行为，这些不能只靠代码审查宣称通过。

---

## 2. 手动验收项完整

你保留了 21 项，覆盖了：

```text
StartAppOnWindowsLogin 默认值
Settings 开启/关闭自启
HKCU Run Key 写入/删除
自启参数启动隐藏到托盘
AutoStartAgentWhenAppStarts true/false 两种组合
手动启动不回归
Mismatch 修复
错误脱敏
Exit App 不误停 Agent
托盘命令不回归
Settings dirty 不覆盖草稿
重复启动行为
```

这个清单是完整的。

---

## 3. 风险复核抓住了关键风险

13 项风险复核里最关键的是：

```text
StartAppOnWindowsLogin 与 AutoStartAgentWhenAppStarts 语义独立
HKCU Run Key 注册/删除幂等
StartupCommandBuilder 不会注册 dotnet.exe / dll / 空路径
Settings 保存失败时不显示假启用
AutoStartHidden 不通过 CloseToTray 模拟隐藏
Diagnostics 不泄露完整路径 / SID / command / 异常原文
重复启动行为已验证并记录
```

这些正好对应阶段 11 的核心风险。

---

# 建议补强 1：验收清单里的手动项默认必须是 `[ ]`

你已经写了“供人工逐项勾选”，但建议更硬一点：

```text
手动验收 21 项和长跑验证 6 项默认必须全部使用 [ ]，除非 agent 在真实 Windows 环境中实际执行并能记录具体结果；不得根据自动化测试推断为 [x]。
```

这样能防止 agent 在文档中自动打勾。

---

# 建议补强 2：自动化验收和人工验收分开写结论

完成说明中建议明确分成：

```text
自动化验收：已完成 / 未完成
人工验收：待人工确认 / 部分完成 / 已完成
长跑验证：待人工确认 / 部分完成 / 已完成
```

并且收口结论根据实际情况选不同措辞。你已经写了如果未全部通过要改措辞，建议再更具体：

```text
如果只有 build/test 通过，但手动验收和长跑未执行，收口结论应写：
“阶段 11 已完成代码实现与自动化验收，手动验收和长跑验证待人工确认。”
```

---

# 建议补强 3：读取真实环境信息时不要泄露路径和用户名

提示词要求验收环境信息包括 Windows 版本、是否域账户、是否管理员、.NET 版本等。这个可以，但建议补一句：

```text
验收环境信息不得记录用户名、用户目录、机器名、SID、完整项目路径或完整 Run Key command。
```

否则阶段 11 的隐私边界可能在验收文档里被破坏。

---

# 建议补强 4：如果 build/test 失败，不要继续写“收口完成”

你已经说测试失败先判断是否属于阶段 11 问题。建议再加一句：

```text
如果 build/test 未通过，仍可生成问题记录文档，但不得生成“阶段 11 已完成”式收口结论；完成说明应标记为“未收口 / 待修复”。
```

这能避免测试失败但文档仍然收口。

---

# 建议补强 5：手动改注册表后的恢复责任要写清楚

手动验收第 13 项会故意改坏 Run Key。建议补一句：

```text
如果手动验收需要修改 HKCU Run Key，验收清单必须包含恢复步骤：通过 Settings 保存修复，或关闭自启删除 WUJI value，确保验收结束后不残留错误注册项。
```

这很重要，避免开发机器残留坏的 `WUJI` Run Key。

---

# 建议补强 6：重复启动行为不能只“观察”，还要记录结论分类

第 21 项很关键。建议让 agent 在验收清单里预留分类：

```text
重复启动结果：
[ ] 未执行
[ ] 无双托盘 / 无双窗口 / 无状态漂移
[ ] 出现双托盘
[ ] 出现双窗口
[ ] 出现状态漂移
处理建议：single-instance 是否提升为下一阶段前置项
```

这样你后续决策是否先做 single-instance 会更清楚。

---

# 可直接追加到提示词里的补充段

建议把下面这段加到“注意”或“生成验收清单”后面：

```text
补充要求：

1. 手动验收 21 项和长跑验证 6 项默认必须全部使用 [ ]，除非 agent 在真实 Windows 环境中实际执行并能记录具体结果；不得根据自动化测试推断为 [x]。
2. 完成说明中请分开写：自动化验收、人工验收、长跑验证。若只有 build/test 通过，结论应写“阶段 11 已完成代码实现与自动化验收，手动验收和长跑验证待人工确认”。
3. 验收环境信息不得记录用户名、用户目录、机器名、SID、完整项目路径或完整 Run Key command。
4. 如果 build/test 未通过，不得生成“阶段 11 已完成”式收口结论；完成说明应标记为“未收口 / 待修复”。
5. 如果手动验收需要修改 HKCU Run Key，验收清单必须包含恢复步骤：通过 Settings 保存修复，或关闭自启删除 WUJI value，确保验收结束后不残留错误注册项。
6. 重复启动行为请在验收清单中单独预留结论分类：未执行、无双托盘/双窗口、出现双托盘、出现双窗口、出现状态漂移；如异常出现，将 single-instance 提升为下一阶段前置项。
```

---

# 最终结论

这份阶段 11.6 提示词**可行，可以执行**。

我建议只补上三类内容后直接交给 agent：

```text
1. 未实际执行的手动验收 / 长跑验证必须保持未勾选。
2. build/test 未通过时不得写“阶段 11 已完成”。
3. 手动改 Run Key 的验收必须包含恢复步骤，避免残留坏注册项。
```

其余内容已经足够完整。执行完 11.6 后，就可以审核《阶段11-完成说明》和《阶段11-验收清单》，再决定下一步是“安装包 / 发布体验”还是先补“single-instance”。
