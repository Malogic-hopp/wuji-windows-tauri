# 阶段 11.5：Diagnostics 自启状态与错误展示

## 阶段目标

让 Diagnostics 能解释登录自启当前是否启用、是否 mismatch、最近是否出现注册错误。

## 背景

自启注册属于 OS 集成。用户需要能知道“当前 Settings 中的选择”和“Windows Run Key 的真实状态”是否一致，但 UI 不能泄露完整本机路径、完整 command 或注册表异常原文。

## 建议展示

```text
Login startup
    Enabled / Disabled / Mismatch / Error

Launch mode
    Manual / AutoStart

Startup registration
    Registered to current app / Needs repair / Not registered / Unavailable

Last startup registration error
    安全短句
```

Settings 中也可保留简短状态：

```text
Startup registration status: Enabled
```

安全摘要可以包括：

```text
Registered to current app
Registered command needs repair
Registration unavailable
Registered command points to a different app location
Required startup arguments are missing
```

## 安全要求

```text
不展示完整 exe 路径。
不展示完整 Run Key command。
不展示 SID。
不展示用户名目录。
不展示注册表异常原文。
不展示 stack trace。
不展示完整本机路径。
```

## 建议查看文件

```text
src/QuantifiedSelf.Windows.App/ViewModels/MainWindowViewModel.cs
src/QuantifiedSelf.Windows.App/ViewModels/SettingsViewModel.cs
src/QuantifiedSelf.Windows.App/Views/MainWindow.xaml
src/QuantifiedSelf.Windows.App/Services/DiagnosticsDataService.cs
src/QuantifiedSelf.Windows.App/Services/StartupRegistrationService.cs
tests/QuantifiedSelf.Windows.Tests/DataFlowTests.cs
```

## 建议测试

```text
Diagnostics_ShowsLoginStartupEnabled
Diagnostics_ShowsLoginStartupDisabled
Diagnostics_ShowsLoginStartupMismatch
Diagnostics_RedactsStartupRegistrationError
Diagnostics_ShowsLaunchModeManual
Diagnostics_ShowsLaunchModeAutoStart
Diagnostics_KeepsRefreshHealthVisible
Diagnostics_DoesNotExposeStartupCommandPath
```

## 验收标准

```text
build 0 warning / 0 error
test 全部通过
Diagnostics 显示登录自启 Enabled / Disabled
Mismatch 能显示安全修复提示
Error 能显示安全错误
LaunchMode 能显示 Manual / AutoStart
不影响 RefreshHealth / IPC / Recent Events
不新增高频日志
不泄露完整路径、SID、注册表异常原文或完整 command
```

## 不做什么

```text
不把自启状态写入 agent_events
不写 JSONL
不做通知中心
不显示完整注册表命令
不改 RefreshService 状态轮询语义
```
