请在仓库 `C:\Users\WangZhenchi\work\WUJI\Win` 中实施阶段 11.3。

请先阅读并严格遵循这些文档，中文文档用 UTF-8 读取：

1. `docs\下一步计划-2026-07-04-开机自启MVP.md`
2. `docs\下一步计划-2026-07-04-开机自启MVP\README.md`
3. `docs\下一步计划-2026-07-04-开机自启MVP\03-阶段11.3-Settings自启开关与保存同步.md`
4. `docs\下一步计划-2026-07-04-开机自启MVP\阶段11.1-完成说明-2026-07-04.md`
5. `docs\下一步计划-2026-07-04-开机自启MVP\阶段11.2-完成说明-2026-07-05.md`

任务目标：

实现“阶段 11.3：Settings 自启开关与保存同步”。

本阶段在阶段 11.2 的 StartupRegistrationService 与 HKCU Run Key 基础上，在 Settings 中暴露登录自启开关，并把保存操作同步到 StartupRegistrationService。本阶段要实现 Settings UI 上的独立开关、保存时的注册/取消注册同步、以及注册状态的安全展示。

尤其注意本阶段不做：

- 不实现自启启动到托盘（阶段 11.4）。
- 不做 Diagnostics 自启状态展示（阶段 11.5）。
- 不做手动验收 / 收口（阶段 11.6）。
- 不做 single-instance。
- 不直接从 Settings 启动或停止 Agent。
- 不改变 ReloadConfig。
- 不改变 PruneData / ClearHistory。
- 不新增管理员权限提示。
- 不改 IPC。
- 不改托盘命令。
- 不做安装包、Windows Service、管理员权限自启、计划任务。
- 不改阶段 10 CloseToTray / MinimizeToTray / Exit App / 托盘命令语义。
- 不改阶段 8 IPC 协议。
- 不改 AgentStateMachine 业务语义。
- 不改 PruneData / ClearHistory 规则。
- 不改 ClearHistory 的 `CLEAR` 二次确认。
- 不回退阶段 9.6 的 Stale + IsRunning=false 时 Start 可用修复。

补充要求：

```text
1. 如果 StartupRegistrationStatus 为 Mismatch，且用户保存 StartAppOnWindowsLogin=true，应调用 RegisterAsync() 修复为当前 App 的正确 Run Key command；修复成功后再保存 AppSettings=true。
2. 加载 Settings 时，StartAppOnWindowsLogin 草稿值来自 AppSettings；StartupRegistrationStatusText 来自 OS 状态。若 AppSettings=true 但 OS Disabled/Mismatch，应显示 Needs repair / Not registered 警告，但不要自动覆盖用户草稿。
3. RegisterAsync / UnregisterAsync 失败时，不保存 AppSettings 对应变更，不应把 IsDirty 清为 false；SaveStatusText 显示安全失败原因，允许用户重试或取消。
4. SettingsViewModel 优先依赖 IStartupRegistrationService 或等价接口；如果当前没有接口，也必须保证单元测试可用 fake service，不写真实注册表。
5. StartupRegistrationStatusText 是只读状态，更新它不得设置 Settings IsDirty=true。
6. 如果用户只修改 AutoStartAgentWhenAppStarts，而 StartAppOnWindowsLogin 未变，不应调用 RegisterAsync / UnregisterAsync。
7. Settings UI 文案必须明确区分“登录 Windows 后启动 WUJI App”和“WUJI App 启动后自动启动 Agent”，不要使用容易混淆的“开机自启 Agent”之类表述。
```

需要实现的范围：

1. Settings UI 新增独立开关。

建议修改：

```text
src\QuantifiedSelf.Windows.App\Views\SettingsView.xaml
src\QuantifiedSelf.Windows.App\ViewModels\SettingsViewModel.cs
```

要求：

```text
在 App Settings 区域新增 StartAppOnWindowsLogin 开关。
保持 AutoStartAgentWhenAppStarts 独立显示，不与新开关合并。
两个开关的文案必须明确区分，建议使用：
    登录 Windows 后启动 WUJI
    说明：登录后自动启动 WUJI App，之后可从托盘打开窗口。
    WUJI 启动后自动启动 Agent
    说明：WUJI App 启动后自动开始后台采集。
新增 StartupRegistrationStatusText 或等价的只读状态文本，显示 Enabled / Disabled / Mismatch / Error 等安全摘要。
StartupRegistrationStatusText 是只读状态，更新它不得设置 Settings IsDirty=true。
```

2. SettingsViewModel 保存同步注册状态。

建议修改：

```text
src\QuantifiedSelf.Windows.App\ViewModels\SettingsViewModel.cs
src\QuantifiedSelf.Windows.App\Services\SettingsService.cs（如存在）
```

要求：

```text
SettingsViewModel 优先依赖 IStartupRegistrationService 或等价接口；如当前未定义接口，则依赖 StartupRegistrationService 具体类，但必须保证单元测试可用 fake service，不写真实注册表。
用户开启 StartAppOnWindowsLogin 时（包括当前 OS 状态为 Mismatch 的修复场景）：
    先调用 StartupRegistrationService.RegisterAsync()。
    注册成功后再保存 AppSettings（StartAppOnWindowsLogin=true）。
    注册失败时显示安全错误，不保存 AppSettings，不清除 IsDirty，允许用户重试或取消。
用户关闭 StartAppOnWindowsLogin 时：
    先调用 StartupRegistrationService.UnregisterAsync()。
    取消注册成功后再保存 AppSettings（StartAppOnWindowsLogin=false）。
    取消注册失败时显示安全错误，不保存 AppSettings，不清除 IsDirty，允许用户重试或取消。
如果用户只修改 AutoStartAgentWhenAppStarts，而 StartAppOnWindowsLogin 未变：
    不应调用 RegisterAsync / UnregisterAsync。
如果现有保存流程难以严格事务化，至少必须保证：
    UI 开关值是用户草稿。
    StartupRegistrationStatus 反映 OS 真实注册状态。
    保存失败时 SaveStatusText 明确说明注册失败。
    下次 Refresh 能检测到 mismatch。
```

3. 注册状态展示。

要求：

```text
SettingsViewModel 暴露 StartupRegistrationStatusText，基于 StartupRegistrationService.GetStatusAsync()。
状态文案安全，不显示完整路径、完整 command、注册表异常原文、SID、用户目录。
示例文案：
    Login startup: Enabled
    Login startup: Disabled
    Login startup: Mismatch — registered command needs repair
    Login startup: Error — registration unavailable
AppSettings=true 但 OS Run Key 缺失时，显示 Needs repair / Not registered 警告，不得自动把 UI 草稿改成 false。
```

4. Settings dirty 规则。

要求：

```text
Settings IsDirty=true 时，status polling / diagnostics refresh 可以更新只读 StartupRegistrationStatusText。
Settings IsDirty=true 时，不得覆盖 StartAppOnWindowsLogin 草稿值。
Settings IsDirty=true 时，不得因为后台刷新触发注册表写入或删除。
Refresh 只能重新读取 OS 状态并更新只读提示，不能提交用户草稿。
```

5. 与阶段 11.1 / 11.2 的衔接。

要求：

```text
复用阶段 11.1 新增的 AppSettings.StartAppOnWindowsLogin。
复用阶段 11.2 新增的 StartupRegistrationService / StartupRegistrationStatus。
不要在 SettingsViewModel 中直接写注册表，必须经 StartupRegistrationService。
不要在 SettingsViewModel 中直接构建 Run Key command，必须经 StartupCommandBuilder。
```

6. 可测试性要求。

单元测试不要依赖真实 Windows 注册表，不要启动真实 WPF App。

建议新增测试：

```text
SettingsViewModel_LoadsStartAppOnWindowsLogin
SettingsViewModel_SavesLoginStartupAndRegistersRunKey
SettingsViewModel_DisablesLoginStartupAndUnregistersRunKey
SettingsViewModel_StartupRegistrationFailureShowsSafeError
SettingsViewModel_ShowsStartupMismatchStatus
SettingsViewModel_LoginStartupDirtyDraftNotOverwritten
SettingsViewModel_DirtyRefreshDoesNotWriteStartupRegistry
SettingsViewModel_ShowsWarningWhenAppSettingsEnabledButRunKeyMissing
SettingsViewModel_AutoStartAgentSettingRemainsIndependent
SettingsViewModel_StartAppOnWindowsLoginIndependentOfAgentAutoStart
```

测试要求：

```text
测试不要写真实注册表。
测试不要启动真实 WPF App。
测试不要依赖系统托盘。
使用 fake StartupRegistrationService 模拟 RegisterAsync / UnregisterAsync / GetStatusAsync 结果。
验证开启开关时调用 RegisterAsync，成功后保存 AppSettings=true。
验证 Mismatch 状态下保存 true 时调用 RegisterAsync 修复，成功后保存 AppSettings=true。
验证关闭开关时调用 UnregisterAsync，成功后保存 AppSettings=false。
验证注册失败时 AppSettings 不写成 true，IsDirty 不清除，并显示安全错误。
验证 mismatch 状态显示安全 Needs repair 文案。
验证 AppSettings=true 但 OS Disabled 时显示警告但不自动覆盖草稿。
验证 Settings dirty 时后台刷新不触发注册表写入。
验证 StartupRegistrationStatusText 更新不设置 IsDirty=true。
验证只修改 AutoStartAgentWhenAppStarts 时不调用 RegisterAsync / UnregisterAsync。
验证 AutoStartAgentWhenAppStarts 保存逻辑不回归。
```

建议重点查看这些文件：

```text
src\QuantifiedSelf.Windows.App\ViewModels\SettingsViewModel.cs
src\QuantifiedSelf.Windows.App\Views\SettingsView.xaml
src\QuantifiedSelf.Windows.App\Services\SettingsService.cs
src\QuantifiedSelf.Windows.App\Services\StartupRegistrationService.cs
src\QuantifiedSelf.Windows.App\Services\StartupRegistrationStatus.cs
src\QuantifiedSelf.Windows.Core\Options\AppSettings.cs
src\QuantifiedSelf.Windows.Infrastructure\Settings\AppSettingsStore.cs
tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs
```

验收标准：

- build 0 warning / 0 error。
- test 全部通过。
- Settings 显示两个独立开关（StartAppOnWindowsLogin 和 AutoStartAgentWhenAppStarts）。
- Save App Settings 能启用自启注册。
- Save App Settings 能禁用自启注册。
- 注册失败时显示安全错误，不宣称启用成功。
- 注册 mismatch 时显示 Needs repair / Mismatch。
- Settings dirty 不被状态刷新覆盖。
- AppSettings 与 OS 注册状态不一致时有明确提示。
- AutoStartAgentWhenAppStarts 保存不回归。
- 错误文案不泄露完整路径、完整 command、注册表异常原文、SID、用户目录。
- 不改 IPC、不改托盘命令、不改 Agent 命令链路。
- 阶段 11.1 / 11.2 测试不回归。
- 阶段 10 托盘语义不回归。

请完成代码和测试修改后运行：

```powershell
dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex/build-output/
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex/test-output/
```

请不要并行运行 build 和 test，避免 obj 文件锁。

如果 build/test 产生 `.codex` 临时输出，请在确认结果后清理，清理前必须确认路径位于当前 workspace 内。

建议清理方式：

```powershell
$root = (Resolve-Path '.').Path
Get-ChildItem -Path src,tests -Recurse -Directory -Filter .codex | ForEach-Object {
    $full = $_.FullName
    if (-not $full.StartsWith($root, [System.StringComparison]::OrdinalIgnoreCase)) { throw "Refusing to remove outside workspace: $full" }
    Remove-Item -LiteralPath $full -Recurse -Force
}
```

完成后请把完成情况落盘为完成说明文档：

```text
docs\下一步计划-2026-07-04-开机自启MVP\阶段11.3-完成说明-YYYY-MM-DD.md
```

`YYYY-MM-DD` 使用执行当天日期；如果执行环境无法确定日期，使用 `2026-07-05`。

完成说明至少包含：

```text
1. 阶段目标
2. 修改文件清单
3. Settings UI 变更说明
4. 保存同步注册状态说明
5. 注册状态展示说明
6. Settings dirty 规则说明
7. 新增 / 更新测试
8. build / test 结果
9. 明确未做事项
10. 风险与后续注意点
```

其中“明确未做事项”必须特别确认：

```text
未实现自启启动到托盘（阶段 11.4）
未做 Diagnostics 自启状态展示（阶段 11.5）
未做手动验收 / 收口（阶段 11.6）
未做 single-instance
未直接从 Settings 启动或停止 Agent
未改变 ReloadConfig
未改变 PruneData / ClearHistory
未新增管理员权限提示
未改 IPC
未改托盘命令
未做安装包 / Windows Service / 管理员权限自启 / 计划任务
未改阶段 10 托盘语义
未改阶段 8 IPC 协议
未改 AgentStateMachine 业务语义
未改 PruneData / ClearHistory 规则
```

注意：

- `Get-Content` 读取中文文档时使用 `-Encoding UTF8`。
- 不要回滚用户或其他 agent 的改动。
- 手工编辑文件请使用 `apply_patch`。
- 完成说明中不需要自行判断是否进入下一阶段。
- 不要把阶段 11.4 的自启隐藏到托盘提前做进来。
- 不要把阶段 11.5 的 Diagnostics 自启状态展示提前做进来。
