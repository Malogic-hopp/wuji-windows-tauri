可行，这份阶段 10.6 提示词**可以交给 agent 执行**。它的定位是正确的：10.6 不是再新增一套托盘功能，而是在 10.4/10.5 已有的托盘状态展示与基础控制命令上，补齐 **NotRunning、Stale、IPC fallback、Maintenance、状态恢复、安全文案、低噪声提示** 的异常场景验收。这个和阶段 10.6 文档目标一致。

---

# 总体评价

这份提示词已经比较完整，可以执行。

它把阶段 10.6 的边界控制得比较好：

```text id="hhlvnk"
不做复杂通知中心
不做持久化通知历史
不写 agent_events
不做真正状态流
不新增托盘专用状态轮询
不改阶段 9 status polling
不改阶段 10.5 Start / Pause / Resume / Stop 命令链路
```

这很重要。阶段 10.6 应该是“异常状态体验验收 + 安全加固”，而不是重新设计托盘通知系统。

---

# 我认为写得好的地方

## 1. 异常状态覆盖完整

你覆盖了几个关键场景：

```text id="kfd0lw"
Agent NotRunning
Stale + IsRunning=false
Stale + IsRunning=true
IPC unavailable / fallback
Maintenance
恢复 Running / Paused
```

这些正好是托盘长期常驻后最容易遇到的状态。

---

## 2. Stale 的两种情况区分清楚

这点很关键：

```text id="ydfyx9"
Stale + IsRunning=false:
    Start 可用，Stop 禁用

Stale + IsRunning=true:
    Stop 可用，Start 禁用
```

这延续了阶段 9.6 的真实问题修复，不能回退。

---

## 3. 通知策略足够保守

你写了：

```text id="iv156t"
MVP 可以不弹 toast，只更新 tooltip / menu 状态。
不要对每次 2 秒轮询失败弹通知。
如果实现通知，只对关键状态转换通知一次。
```

这个策略很好。托盘通知最怕刷屏，MVP 阶段可以只做 tooltip/menu 状态恢复，不强制 toast。

---

# 建议补强 1：明确“没有 toast 也算完成”

你虽然写了“MVP 可以不弹 toast”，但测试里还有 `TrayNotification_*`。建议再加一句：

```text id="ei4bca"
如果本阶段选择不实现 toast/balloon 通知，则 TrayNotification_* 测试可改为验证“没有重复通知调用”或在完成说明中明确未实现 toast，验收以 tooltip/menu 状态更新为准。
```

否则 agent 可能为了满足测试名而引入通知系统，扩大范围。

---

# 建议补强 2：状态转换通知如果实现，必须有“上一状态”记忆但不持久化

如果 agent 选择实现关键状态转换通知，需要比较 previous/current。建议补一句：

```text id="n752p3"
如果实现状态转换通知，只允许在 App 内存中维护上一份 TrayMenuState / Agent 状态用于去重；不得写文件、SQLite、agent_events 或 JSONL。
```

这能避免为了通知去做持久化历史。

---

# 建议补强 3：恢复路径必须经过已应用状态，而不是单独更新托盘

你已经强调复用阶段 9。建议更具体：

```text id="w8wpso"
状态恢复时，托盘更新必须来自 MainWindowViewModel 已应用的最新 AgentStatusSnapshot / TrayMenuState；不得在托盘层单独根据 fake/临时状态直接覆盖菜单。
```

这可以防止托盘恢复状态和 MainWindow 不一致。

---

# 建议补强 4：fallback 文案只显示 source，不显示错误详情

你要求安全文本，这很好。建议把托盘层级再降噪：

```text id="yg2622"
托盘 tooltip/menu 中 IPC fallback 只显示简短 source，例如 IPC: FileFallback / unavailable；详细错误仍留在 Diagnostics，不放进托盘。
```

托盘是状态提示，不是诊断详情页。

---

# 建议补强 5：Settings dirty 测试不要触发配置重载

你已经要求 Settings dirty 不受影响。建议补一句：

```text id="xvqwpe"
Settings dirty 场景下，只验证托盘状态更新和 Settings 草稿保持；不得为了测试调用 SettingsViewModel.LoadAsync 或重载配置文件。
```

避免 agent 在测试里误走配置加载路径。

---

# 建议补强 6：重复 fallback 不刷屏的测试可以用 fake notification service

你已经写了 fake notification service。建议再明确：

```text id="cdcbwl"
重复 fallback 不刷屏测试应模拟相同状态连续多次更新，断言通知次数不增加；不同状态转换可以允许一次新通知。
```

这样测试更明确。

---

# 可直接追加到提示词里的补充段

建议你把下面这段加到“通知策略”或“可测试性要求”后面：

```text id="h1p8n7"
补充要求：

1. 如果本阶段选择不实现 toast / balloon 通知，则 TrayNotification_* 测试可改为验证“没有重复通知调用”或在完成说明中明确未实现 toast，验收以 tooltip / menu 状态更新为准。
2. 如果实现状态转换通知，只允许在 App 内存中维护上一份 TrayMenuState / Agent 状态用于去重；不得写文件、SQLite、agent_events 或 JSONL。
3. 状态恢复时，托盘更新必须来自 MainWindowViewModel 已应用的最新 AgentStatusSnapshot / TrayMenuState；不得在托盘层单独根据临时状态直接覆盖菜单。
4. 托盘 tooltip / menu 中 IPC fallback 只显示简短 source，例如 IPC: FileFallback / unavailable；详细错误仍留在 Diagnostics，不放进托盘。
5. Settings dirty 场景下，只验证托盘状态更新和 Settings 草稿保持；不得为了测试调用 SettingsViewModel.LoadAsync 或重载配置文件。
6. 重复 fallback 不刷屏测试应模拟相同状态连续多次更新，断言通知次数不增加；不同状态转换可以允许一次新通知。
```

---

# 最终结论

这份阶段 10.6 提示词**可行，可以执行**。

我建议只补上三类内容后直接给 agent：

```text id="d17fg5"
1. 明确不实现 toast 也可以收口，以 tooltip/menu 恢复为主。
2. 如果做通知，只用 App 内存去重，不写任何持久化。
3. 托盘 fallback 只显示简短 source，详细错误留在 Diagnostics。
```

其余内容已经足够清楚。完成 10.6 后，就可以进入 10.7 的手动验收、长跑验证和阶段 10 收口。
