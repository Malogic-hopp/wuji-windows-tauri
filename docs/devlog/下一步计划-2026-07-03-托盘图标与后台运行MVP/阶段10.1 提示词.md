请在仓库 `C:\Users\WangZhenchi\work\WUJI\Win` 中实施阶段 10.1。

请先阅读并严格遵循这些文档，中文文档用 UTF-8 读取：

1. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP.md`
2. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\README.md`
3. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\01-阶段10.1-状态可见性与托盘前置整理.md`
4. `docs\下一步计划-2026-07-01-Agent状态流订阅RefreshService优化MVP\阶段9-完成说明-2026-07-03.md`
5. `docs\下一步计划-2026-07-01-Agent状态流订阅RefreshService优化MVP\阶段9.6-手动验收清单-2026-07-02.md`

任务目标：

实现“阶段 10.1：状态可见性与托盘前置整理”。

本阶段只修复阶段 9.6 手动验收中发现的按钮禁用态视觉不明显问题，并做托盘接入前的轻量边界确认。不要实现托盘图标、CloseToTray、MinimizeToTray、Show、Exit、托盘菜单或任何后台运行生命周期。

尤其注意本阶段不做：

- 不做托盘图标。
- 不新增 TrayService。
- 不新增 ITrayService / TrayMenuState / TrayCommandBridge。
- 不引用 `System.Windows.Forms.NotifyIcon`。
- 不引入 H.NotifyIcon.Wpf 或任何第三方托盘库。
- 不做 close to tray。
- 不做 minimize to tray。
- 不改变窗口关闭语义。
- 不改变 App shutdown 语义。
- 不改变 status polling / page refresh 语义。
- 不改变 Start / Pause / Resume / Stop / ReloadConfig / PruneData / ClearHistory 的可用性规则。
- 不改变 AgentCommandAvailability 的业务判断。
- 不改 IPC 协议。
- 不改 Agent 命令链路。
- 不改 AgentControlService / AgentProcessService。
- 不改 Settings 数据管理逻辑。
- 不做开机自启、安装包、Windows Service。

需要实现的范围：

1. 修复 `SecondaryButtonStyle` 的 disabled visual state。

建议查看：

- `src\QuantifiedSelf.Windows.App\MainWindow.xaml`

行为要求：

```text
SecondaryButtonStyle 在 IsEnabled=false 时必须肉眼明显可见。
禁用态可以通过 Opacity、Foreground、Background、BorderBrush 或 Cursor 等方式表达。
禁用态应与 hover / pressed / normal 状态区分清楚。
禁用态不应影响按钮布局尺寸。
禁用态文字仍应可读，不要低到完全看不清。
不要把 disabled visual 写成只对某一个按钮生效的局部 hack。
```

优先做法：

```text
在现有 SecondaryButtonStyle 中补充 Trigger / Setter。
优先使用 Trigger Property="IsEnabled" Value="False" 设置禁用态。
尽量复用现有颜色资源、BorderBrush、Foreground、Background 风格。
保持 XAML 改动小而集中。
不要通过 code-behind 或 ViewModel 属性控制视觉状态。
```

如果项目中还有同类次级按钮样式或共享 Button style，请先确认 `Pause` / `Resume` 实际使用的是哪个 style，再修改正确的 style。

补充要求：

```text
确认 disabled trigger 的视觉优先级不会被 MouseOver / Pressed trigger 覆盖。
禁用态下 hover 不应显示为可点击状态。
除非确认 Pause / Resume 实际使用的不是 SecondaryButtonStyle，否则不要修改 PrimaryButtonStyle。
不要修改全局 Button 默认样式，避免扩大视觉影响面。
```

2. 确认可用性规则不变。

建议查看：

- `src\QuantifiedSelf.Windows.App\ViewModels\MainWindowViewModel.cs`
- `src\QuantifiedSelf.Windows.App\Services\AgentCommandAvailability.cs`
- `src\QuantifiedSelf.Windows.App\ViewModels\SettingsViewModel.cs`

要求：

```text
不要改变 CanStart / CanPause / CanResume / CanStop 的计算逻辑。
不要改变 Stale + IsRunning=false 时 Start 可用的修复结果。
不要改变 Maintenance 状态下控制命令禁用的规则。
不要改变 Settings 里的 ReloadConfig / PruneData / ClearHistory 可用性规则。
不要改变 ConfirmClearHistoryCommand 的二次确认语义。
不要改变 Save Agent Options / Save and Reload 的语义。
```

如果为了验证样式需要阅读 ViewModel，可以阅读；但本阶段原则上不应修改这些业务逻辑文件。除非发现阶段 10.1 文档明确相关的低风险命名或引用问题，否则不要改。

3. 托盘接入前边界确认。

本阶段可以只在完成说明中确认以下边界，没有必要为了“整理”而新增代码：

```text
阶段 10.1 未新增托盘生命周期代码。
阶段 10.1 未改变窗口 Close / Minimize 行为。
阶段 10.1 未改变 status polling。
阶段 10.1 未改变 page refresh。
阶段 10.1 未改变 AgentCommandAvailability。
阶段 10.1 未改变任何 Agent 命令执行路径。
```

不要为了“前置整理”提前创建空的 TrayService 或占位接口。托盘基础设施留到阶段 10.2。

建议重点查看这些现有文件：

- `src\QuantifiedSelf.Windows.App\MainWindow.xaml`
- `src\QuantifiedSelf.Windows.App\ViewModels\MainWindowViewModel.cs`
- `src\QuantifiedSelf.Windows.App\Services\AgentCommandAvailability.cs`
- `tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs`

建议新增或调整测试：

```text
本阶段主要是 WPF 样式 polish，自动化测试不强制新增。
现有 AgentCommandAvailability / MainWindowViewModel tests 必须全部通过，证明可用性逻辑不回归。
```

如果当前测试集中已有对 Button style 或 resource dictionary 的轻量测试，可以补一个非常小的测试确认 `SecondaryButtonStyle` 存在 disabled trigger；如果没有，不要为了这个阶段大规模引入 WPF UI 测试框架。

建议人工核验点：

```text
Running 状态下 Pause 可用、Resume 禁用，Resume 禁用态明显。
Paused 状态下 Resume 可用、Pause 禁用，Pause 禁用态明显。
NotRunning / Stopped 状态下 Pause / Resume 禁用态明显。
Maintenance 状态下相关控制按钮禁用态明显。
按钮尺寸、文字、图标不跳动。
```

验收标准：

- `SecondaryButtonStyle` disabled visual state 明显可见。
- 禁用按钮与可用按钮肉眼容易区分。
- Pause / Resume 禁用态不再让用户误以为“按钮没反应”。
- Start / Pause / Resume / Stop 可用性逻辑不变。
- Settings 数据管理按钮逻辑不变。
- AgentCommandAvailability 不回归。
- 阶段 9 RefreshService / status polling 测试不回归。
- 阶段 8 IPC 测试不回归。
- 阶段 7 PruneData / ClearHistory 测试不回归。
- build 0 warning / 0 error。
- test 全部通过。

请完成代码修改后运行：

```powershell
dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\build-output\
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\test-output\
```

请不要并行运行 build 和 test，避免 obj 文件锁。

如果 build/test 产生 `.codex` 临时输出，请在确认结果后清理，清理前必须确认路径位于当前 workspace 内。

建议清理方式：

```powershell
$root = (Resolve-Path '.').Path
Get-ChildItem -Path src,tests -Recurse -Directory -Filter .codex | ForEach-Object {
    $full = $_.FullName
    if (-not $full.StartsWith($root, [System.StringComparison]::OrdinalIgnoreCase)) { throw "Refusing to remove outside workspace: $full" }
    Remove-Item -LiteralPath $full -Recurse -Force
}
```

完成后请把完成情况落盘为完成说明文档：

```text
docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\阶段10.1-完成说明-YYYY-MM-DD.md
```

`YYYY-MM-DD` 使用执行当天日期；如果执行环境无法确定日期，使用 `2026-07-03`。

完成说明至少包含：

```text
1. 阶段目标
2. 修改文件清单
3. 实现行为说明
4. 测试与验证结果
5. 明确未做事项
6. 人工核验情况
7. 风险与后续注意点
```

“人工核验情况”需要说明：

```text
Running 状态下 Pause 可用、Resume 禁用时，Resume 禁用态是否肉眼可见。
Paused 状态下 Resume 可用、Pause 禁用时，Pause 禁用态是否肉眼可见。
NotRunning / Stopped 状态下 Pause / Resume 禁用态是否肉眼可见。
Maintenance 状态下相关控制按钮禁用态是否肉眼可见。
如果未进行人工截图或运行验收，需要明确写“未进行人工截图验收 / 未进行运行态人工验收”。
```

其中“明确未做事项”必须特别确认：

```text
未新增托盘图标
未新增 TrayService
未接入 NotifyIcon
未改变 Close / Minimize / Shutdown 行为
未改变 status polling / page refresh
未改变 AgentCommandAvailability
未改变 Agent 命令执行链路
未改变 Settings 数据管理逻辑
未做开机自启或安装包
```

注意：

- `Get-Content` 读取中文文档时使用 `-Encoding UTF8`。
- 不要回滚用户或其他 agent 的改动。
- 手工编辑文件请使用 `apply_patch`。
- 完成说明中不需要自行判断是否进入下一阶段。
- 不要把阶段 10.2 的 TrayService / NotifyIcon 提前做进来。
- 不要把阶段 10.3 的 CloseToTray / MinimizeToTray 提前做进来。
- 本阶段越小越好，目标是把禁用态视觉问题修干净。
