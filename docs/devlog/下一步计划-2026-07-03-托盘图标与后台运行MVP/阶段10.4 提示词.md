请在仓库 `C:\Users\WangZhenchi\work\WUJI\Win` 中实施阶段 10.4。

请先阅读并严格遵循这些文档，中文文档用 UTF-8 读取：

1. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP.md`
2. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\README.md`
3. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\04-阶段10.4-托盘状态展示与菜单可用性绑定.md`
4. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\阶段10.1-完成说明-2026-07-03.md`
5. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\阶段10.2-完成说明-2026-07-03.md`
6. `docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\阶段10.3-完成说明-2026-07-03.md`
7. `docs\下一步计划-2026-07-01-Agent状态流订阅RefreshService优化MVP\阶段9-完成说明-2026-07-03.md`

任务目标：

实现“阶段 10.4：托盘状态展示与菜单可用性绑定”。

本阶段在阶段 10.3 的 CloseToTray / MinimizeToTray / Show / Exit 语义基础上，让托盘 tooltip 和菜单可用性跟随阶段 9 的 `AgentStatusSnapshot` / `AgentCommandAvailability`，确保 MainWindow 和托盘菜单状态不漂移。本阶段不实现托盘 Agent 控制命令，只实现状态展示与菜单可用性。

尤其注意本阶段不做：

- 不做托盘 Start / Pause / Resume / Stop 命令执行（阶段 10.5）。
- 不做托盘异常提示 / 通知 toast（阶段 10.6）。
- 不做开机自启、安装包、Windows Service。
- 不做真正 Named Pipe 状态流 / subscribe 协议。
- 不做复杂托盘通知中心。
- 不做 Exit App + Stop Agent 组合操作。
- 不新增托盘专用状态轮询。
- 不改阶段 9 的 2 秒 status polling interval。
- 不改阶段 9 的 status/page refresh gate 架构。
- 不改 Named Pipe IPC 协议。
- 不改 AgentStateMachine 业务语义。
- 不改 PruneData / ClearHistory 规则。
- 不改 ClearHistory 的 `CLEAR` 二次确认。
- 不回退阶段 9.6 的 Stale + IsRunning=false 时 Start 可用修复。

需要实现的范围：

1. TrayMenuState 纯模型。

建议新增或完善：

```text
src\QuantifiedSelf.Windows.App\Services\TrayMenuState.cs
```

`TrayMenuState` 至少包含：

```text
string TooltipText
string SafeStatusText
string IpcStatusText
bool CanShowMainWindow
bool CanExitApp
bool CanStart
bool CanPause
bool CanResume
bool CanStop
bool IsMaintenance
```

要求：

```text
TrayMenuState 是纯模型，不依赖 NotifyIcon。
提供工厂方法 TrayMenuState.From(AgentStatusSnapshot, AgentCommandAvailability, AgentIpcStatusSnapshot / safe ipc text)。
TrayMenuState.From 接收安全 IPC 展示文本或轻量 snapshot，不直接依赖 AgentIpcStatusService 实例。
使用 AgentCommandAvailability 推导菜单可用性，不单独写一套规则。
不显示 FullPipeName / SID / 本机路径 / SQL / 异常原文 / 原始 JSON。
NotifyIcon.Text 有长度限制，tooltip 文本需做安全截断（建议 63 字符以内）。
```

2. Tooltip 状态展示。

托盘 tooltip 建议显示：

```text
WUJI
Agent: Running / Paused / NotRunning / Stale / Maintenance
IPC: NamedPipe / FileFallback / unavailable 简短状态
```

要求：

```text
tooltip 第一行固定为 WUJI 或应用名。
Agent 状态使用 SafeStatusText，例如 Running、Paused、NotRunning、Stale、Maintenance。
IPC 状态使用安全短句，例如 NamedPipe、FileFallback、unavailable。
IpcStatusText 优先复用已有 safe IPC status/source 文案。
不要为了托盘重新暴露 DisplayPipeName、FullPipeName、SID 或 LastIpcError 原文。
不得显示完整 pipe name、SID、本机路径、SQL、异常原文、原始 JSON。
NotifyIcon.Text 超长时安全截断，避免运行时异常。
Tooltip 截断时优先保留应用名和 Agent 状态。
IPC 状态可简化为 IPC: NamedPipe / FileFallback / unavailable。
```

3. 菜单可用性绑定。

托盘菜单项可用性必须来源于 `AgentCommandAvailability`：

```text
Start 可用性 = AgentCommandAvailability.CanStart
Pause 可用性 = AgentCommandAvailability.CanPause
Resume 可用性 = AgentCommandAvailability.CanResume
Stop 可用性 = AgentCommandAvailability.CanStop
Show Main Window 与 Exit App 始终可用（或按阶段 10.2 语义）
Maintenance 时 Start / Pause / Resume / Stop 禁用
Stale + IsRunning=false 时 Start 可用
Stale + IsRunning=true 时 Stop 可用，Start 不可用
```

要求：

```text
TrayService 只负责把 TrayMenuState 渲染到 NotifyIcon / ContextMenu。
不要在 TrayService 中单独写一套命令可用性规则。
不要直接读 runtime_state / health_state 文件。
不要直接查询 SQLite。
本阶段 Start / Pause / Resume / Stop 菜单项可以存在但不绑定执行回调。
即使 Start / Pause / Resume / Stop 菜单项被点击，也不得调用任何 Agent 命令。
不得调用 AgentControlService / AgentProcessService / command bridge。
不得写 agent_control.json。
```

4. 状态更新路径。

建议更新 `MainWindowViewModel` 或新增轻量 bridge：

```text
当 AgentStatusSnapshot 更新时，同步生成 TrayMenuState。
调用 TrayService.UpdateMenuState(TrayMenuState state)。
TrayService 在 NotifyIcon 的 UI 线程上应用新的 tooltip 和菜单可用性。
```

要求：

```text
复用阶段 9 的 2 秒 status polling，不新增托盘轮询。
Settings dirty 时托盘状态仍应更新，但不覆盖 Settings 草稿。
断连 / fallback / Stale / Maintenance 状态变化后，托盘 tooltip 和菜单同步更新。
状态更新触碰 NotifyIcon 时必须经 Dispatcher，如果 Dispatcher 已 shutdown 则安全忽略。
MainWindowViewModel 只在状态结果通过 latest-wins 并已应用到 MainWindow 后更新 TrayMenuState。
被 latest-wins 拒绝的旧状态不得刷新托盘菜单。
```

5. TrayService.UpdateMenuState 实现。

要求：

```text
TrayService 提供 UpdateMenuState(TrayMenuState state) 方法。
方法内部更新 NotifyIcon.Text（tooltip）和 ContextMenuStrip 菜单项 Enabled 状态。
更新应在 UI 线程执行。
如果 NotifyIcon 尚未创建或已 Dispose，安全忽略。
如果 Dispatcher 已 shutdown，安全忽略。
TrayService / adapter 应使用稳定菜单项标识更新 Start / Pause / Resume / Stop / Show / Exit。
不要依赖菜单项显示文本查找。
```

6. 与阶段 10.2 / 10.3 的衔接。

要求：

```text
保留阶段 10.2 的 Show Main Window / Exit App 基础菜单。
保留阶段 10.3 的 CloseToTray / MinimizeToTray 语义。
在 Show / Exit 之外新增 Start / Pause / Resume / Stop 菜单项，但本阶段只绑定可用性，不绑定执行命令。
菜单项文本安全，不泄露敏感信息。
Start / Pause / Resume / Stop 菜单项点击在本阶段必须是 no-op 或安全忽略。
不得提前接入阶段 10.5 的命令执行链路。
```

7. 可测试性要求。

单元测试不要依赖真实 Windows 托盘 shell，不要启动真实 WPF App。

建议新增测试：

```text
TrayMenuState_FollowsRunningStatus
TrayMenuState_FollowsPausedStatus
TrayMenuState_FollowsNotRunningStatus
TrayMenuState_DisablesCommandsDuringMaintenance
TrayMenuState_AllowsStartWhenStaleButProcessGone
TrayMenuState_StopsOnlyWhenStaleWithRunningProcess
TrayMenuState_StaysConsistentWithAgentCommandAvailability
TrayMenuState_DoesNotExposeSensitiveIpcDetails
TrayMenuState_TooltipTextIsSafelyTruncated
TrayService_UpdateMenuStateSetsTooltipText
TrayService_UpdateMenuStateEnablesMenuItems
TrayService_UpdateMenuStateUsesDispatcher
MainWindowViewModel_StatusUpdateRefreshesTrayMenuState
MainWindowViewModel_TrayMenuStateUpdatesOnStatusPolling
```

测试要求：

```text
测试不要创建真实 NotifyIcon。
测试不要启动真实 WPF App。
测试不要依赖系统托盘存在。
TrayMenuState 测试直接传入 AgentStatusSnapshot / AgentCommandAvailability / fake ipc status。
验证菜单可用性与 AgentCommandAvailability 一致。
验证 Start / Pause / Resume / Stop 菜单项点击不调用任何 Agent 命令或 command bridge。
验证被 latest-wins 拒绝的旧状态不会更新托盘菜单。
验证 tooltip 不包含 C:\、Users、SID、完整 pipe name 等敏感信息。
验证超长 tooltip 被安全截断。
TrayService 测试使用 fake tray icon adapter 验证 Visible / Tooltip / MenuItems 更新。
fake tray icon adapter 应使用稳定菜单项 id 记录 Enabled 状态，不依赖显示文本。
```

如果 Dispatcher 调度边界不易自动化测试，可以在完成说明中明确说明手动/代码审查验证方式；但 TrayMenuState 推导逻辑和 UpdateMenuState 对 fake adapter 的调用必须尽量自动化覆盖。

建议重点查看这些文件：

```text
src\QuantifiedSelf.Windows.App\Services\TrayService.cs
src\QuantifiedSelf.Windows.App\Services\TrayMenuState.cs
src\QuantifiedSelf.Windows.App\Services\AgentCommandAvailability.cs
src\QuantifiedSelf.Windows.App\Services\AgentStatusService.cs
src\QuantifiedSelf.Windows.App\Services\AgentIpcStatusService.cs
src\QuantifiedSelf.Windows.App\Services\RefreshService.cs
src\QuantifiedSelf.Windows.App\Services\AgentStatusSnapshot.cs
src\QuantifiedSelf.Windows.App\ViewModels\MainWindowViewModel.cs
src\QuantifiedSelf.Windows.App\App.xaml.cs
src\QuantifiedSelf.Windows.App\MainWindow.xaml.cs
tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs
```

验收标准：

- MainWindow 和托盘菜单状态不漂移。
- 2 秒 status polling 后托盘 tooltip 自动更新。
- Stop / Start / Pause / Resume 后托盘 tooltip 和菜单快速恢复。
- Maintenance 时托盘控制项禁用。
- Stale + IsRunning=false 时托盘 Start 可用。
- Stale + IsRunning=true 时托盘 Start 不可用、Stop 可用。
- Settings dirty 不影响托盘状态更新。
- 不新增托盘专用状态轮询。
- 不显示完整 pipe name / SID / 本机路径。
- tooltip 超长时安全截断。
- CloseToTray / MinimizeToTray 后托盘状态仍继续更新。
- Dispatcher shutdown 时托盘状态更新安全忽略。
- 不执行 Start / Pause / Resume / Stop 命令（阶段 10.5）。
- Start / Pause / Resume / Stop 菜单项点击不调用 AgentControlService / AgentProcessService / command bridge。
- 被 latest-wins 拒绝的旧状态不得覆盖托盘 tooltip / 菜单状态。
- 菜单项 Enabled 更新使用稳定菜单项标识，不依赖显示文本。
- build 0 warning / 0 error。
- test 全部通过。

请完成代码和测试修改后运行：

```powershell
dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\build-output\
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\test-output\
```

请不要并行运行 build 和 test，避免 obj 文件锁。

如果 build/test 产生 `.codex` 临时输出，请在确认结果后清理，清理前必须确认路径位于当前 workspace 内。

建议清理方式：

```powershell
$root = (Resolve-Path '.').Path
Get-ChildItem -Path src,tests -Recurse -Directory -Filter .codex | ForEach-Object {
    $full = $_.FullName
    if (-not $full.StartsWith($root, [System.StringComparison]::OrdinalIgnoreCase)) { throw "Refusing to remove outside workspace: $full" }
    Remove-Item -LiteralPath $full -Recurse -Force
}
```

完成后请把完成情况落盘为完成说明文档：

```text
docs\下一步计划-2026-07-03-托盘图标与后台运行MVP\阶段10.4-完成说明-YYYY-MM-DD.md
```

`YYYY-MM-DD` 使用执行当天日期；如果执行环境无法确定日期，使用 `2026-07-03`。

完成说明至少包含：

```text
1. 阶段目标
2. 修改文件清单
3. TrayMenuState 设计说明
4. Tooltip 状态展示说明
5. 菜单可用性绑定说明
6. 状态更新路径说明（如何复用阶段 9 status polling）
7. latest-wins 与托盘状态更新说明
8. Dispatcher / shutdown 安全说明
9. 新增 / 更新测试
10. build / test 结果
11. 明确未做事项
12. 风险与后续注意点
```

其中“明确未做事项”必须特别确认：

```text
未做托盘 Start / Pause / Resume / Stop 命令执行（阶段 10.5）
Start / Pause / Resume / Stop 菜单项点击仍为 no-op 或安全忽略
未调用 AgentControlService / AgentProcessService / command bridge
未写 agent_control.json
未做托盘异常提示 / 通知 toast（阶段 10.6）
未新增托盘专用状态轮询
未改阶段 9 的 2 秒 status polling interval
未改阶段 9 status/page refresh gate 架构
未做开机自启、安装包、Windows Service
未做真正 Named Pipe 状态流 / subscribe 协议
未做复杂托盘通知中心
未做 Exit App + Stop Agent 组合操作
未显示完整 pipe name / SID / 本机路径 / SQL / 异常原文 / 原始 JSON
```

注意：

- `Get-Content` 读取中文文档时使用 `-Encoding UTF8`。
- 不要回滚用户或其他 agent 的改动。
- 手工编辑文件请使用 `apply_patch`。
- 完成说明中不需要自行判断是否进入下一阶段。
- 不要把阶段 10.5 的托盘 Start / Pause / Resume / Stop 命令执行提前做进来。
- 不要把阶段 10.6 的托盘异常提示 / 通知提前做进来。
