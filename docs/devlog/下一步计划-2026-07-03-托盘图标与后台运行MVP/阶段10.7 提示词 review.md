可行，这份阶段 10.7 提示词**可以交给 agent 执行**。它的定位很准确：10.7 是**验收、补测、长跑验证和收口文档阶段**，不是新增功能阶段。提示词已经明确禁止开机自启、安装包、真正状态流、趋势图、导出、应用分类等后续候补功能，也要求不要把未执行的手动验收和长跑验证标成通过，这一点非常重要。

---

# 总体评价

这份提示词已经比较成熟，可以执行。

阶段 10.7 的核心目标应该是：

```text
1. 复核自动化测试；
2. 复核阶段 10.1-10.6 的关键风险；
3. 生成阶段 10 验收清单；
4. 生成阶段 10 完成说明；
5. 如发现小缺口，补少量测试或最小修复；
6. 不新增业务功能。
```

你这份提示词正是按这个思路写的。阶段 10.7 文档中列出的自动化验收、手动验收、风险复核和收口模板也都被吸收进来了。

---

# 我认为写得好的地方

## 1. 明确“不是新增业务功能阶段”

提示词开头就写清楚：

```text
本阶段不是新增业务功能阶段，而是完成阶段 10 的自动化验收复核、必要补测、手动验收清单、长跑验证和收口文档。
```

这很好。10.7 最怕 agent 借“收口”之名顺手做后续功能，你已经明确禁止了。

---

## 2. 手动验收和长跑验证没有强行标 Passed

你写了：

```text
手动验收项如果没有实际执行，请保留 [ ]，不要打勾。
长跑验证如果没有实际执行，也保留 [ ]。
```

这是非常必要的。验收文档最重要的是可信，没做就是没做，不能为了收口写成通过。

---

## 3. 风险复核清单很完整

你要求复核：

```text
CloseToTray / MinimizeToTray 只 Hide，不停止 status polling
Exit App 才 StopStatusPolling + Dispose TrayService + Shutdown
Exit App 不默认 Stop Agent
TrayService 不直接实现 Agent 控制逻辑
托盘菜单状态来自 AgentCommandAvailability
托盘不新增状态轮询
Settings IsDirty 不被托盘状态更新覆盖
NotifyIcon Dispose 前 Visible=false
Dispatcher shutdown / window released 时托盘回调安全忽略
```

这些正好覆盖阶段 10 最容易出问题的地方。

---

# 建议补强 1：完成说明里不要写“建议进入下一阶段”

你最后注意事项已经写了：

```text
完成说明中不需要自行判断是否进入下一阶段；只写事实、验证结果、未做事项、风险与遗留问题。
```

很好。我建议把“是否建议阶段 10 收口”这一项稍微改成：

```text
阶段 10 是否满足收口条件：是 / 否 / 部分满足，并列出依据。
```

这样比“是否建议进入下一阶段”更客观，也不会让 agent 自行规划后续。

---

# 建议补强 2：把“缺测试支撑”分级处理

你写了如果发现没有测试支撑，优先补少量低风险测试。建议再补一句：

```text
如果某项只能手动验证，或补自动化测试需要引入真实 WPF/NotifyIcon/Agent 进程依赖，则不要硬补复杂测试；在验收清单中标为手动验证项，并说明原因。
```

这样可以避免 agent 为了补测引入脆弱的 UI/托盘测试。

---

# 建议补强 3：长跑验证如果不能执行，要单独列“待人工执行”

你已经说未执行不打勾。建议在完成说明中明确分区：

```text
已自动化验证
已手动验证
待人工执行
未执行原因
```

这样后续你审核阶段 10 完成情况时会更清楚。

---

# 建议补强 4：收口文档中保留真实测试数量

你已经要求写真实 build/test 数字。建议再要求：

```text
如果阶段 10.7 补了测试，需要写明测试总数变化，例如 260/260 passed，以及新增/修改测试名。
```

这样能追踪阶段 10.7 是否只是文档收口，还是也补了自动化测试。

---

# 建议补强 5：不要把“长跑 30 分钟”写成自动完成

阶段 10.7 文档里有 15-30 分钟长跑验证。提示词已经允许未执行保留空项。建议再加一句：

```text
如果 agent 无法实际进行 15-30 分钟人工长跑，不要模拟或假称完成；只生成待执行清单。
```

这个很关键，避免虚假验收。

---

# 可直接追加到提示词里的补充段

建议你把下面这段加到“注意”或“完成说明”部分：

```text
补充要求：

1. 完成说明中不要写“建议进入下一阶段”；改为记录“阶段 10 是否满足收口条件：是 / 否 / 部分满足”，并列出依据。
2. 如果某项只能手动验证，或补自动化测试需要引入真实 WPF / NotifyIcon / Agent 进程依赖，则不要硬补复杂测试；在验收清单中标为手动验证项，并说明原因。
3. 完成说明请分开列出：已自动化验证、已手动验证、待人工执行、未执行原因。
4. 如果阶段 10.7 补了测试，需要写明测试总数变化和新增 / 修改测试名。
5. 如果无法实际进行 15-30 分钟人工长跑，不要模拟或假称完成；只生成待执行清单，并保持未执行项未勾选。
```

---

# 最终结论

这份阶段 10.7 提示词**可行，可以执行**。

我建议只补上三点后直接给 agent：

```text
1. 未执行的手动验收 / 长跑验证必须保持未勾选。
2. 只能手动验证的项目不要强行补复杂 UI 测试。
3. 完成说明按“已自动化 / 已手动 / 待人工执行 / 风险遗留”分区写清楚。
```

其余内容已经足够完整。执行完 10.7 后，就可以审核阶段 10 的总完成说明和验收清单，判断托盘图标与后台运行 MVP 是否正式收口。
