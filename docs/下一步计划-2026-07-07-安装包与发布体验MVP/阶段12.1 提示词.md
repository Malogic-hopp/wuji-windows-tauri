请在仓库 `C:\Users\WangZhenchi\work\WUJI\Win` 中实施阶段 12.1。

请先阅读并严格遵循这些文档，中文文档用 UTF-8 读取：

1. `docs\下一步计划-2026-07-07-安装包与发布体验MVP.md`
2. `docs\下一步计划-2026-07-07-安装包与发布体验MVP\README.md`
3. `docs\下一步计划-2026-07-07-安装包与发布体验MVP\01-阶段12.1-发布配置与self-contained-win-x64输出.md`
4. `docs\下一步计划-2026-07-04-开机自启MVP\阶段11-完成说明-2026-07-06.md`

任务目标：

实现“阶段 12.1：发布配置与 self-contained win-x64 输出”。

本阶段是安装包 MVP 的第一步，只做发布配置、版本号统一、Agent exe 路径解析修复，以及一个能把 App 和 Agent 发布到同一目录的脚本/目标。不做安装包、不做图标、不做注册表命令、不改 UI。

尤其注意本阶段不做：

- 不做安装包（阶段 12.3）。
- 不改图标资源（阶段 12.2）。
- 不引入 MSIX / WiX / Inno Setup 配置，不新增 installer/ 目录。
- 不实现 `--register-autostart` / `--unregister-autostart` 命令行入口（阶段 12.4）。
- 不改阶段 11 StartupRegistrationService / StartupCommandBuilder 的核心逻辑（但如改名需同步更新引用的 exe 名）。
- 不改阶段 11 自启注册逻辑。
- 不改阶段 10 CloseToTray / MinimizeToTray / 托盘命令语义。
- 不改阶段 8 IPC 协议。
- 不改阶段 7 PruneData / ClearHistory 规则。

补充要求：

```text
1. 先运行一次 dotnet publish 检查实际输出名。本阶段建议保持默认名 QuantifiedSelf.Windows.App.exe / QuantifiedSelf.Windows.Agent.exe，以降低 12.1 风险；如你判断必须改名为 WUJI.exe / WUJI.Agent.exe，必须同步更新 AgentProcessService 和 StartupCommandBuilder 中的所有引用。
2. App 不新增对 Agent 项目的项目引用；ReferenceOutputAssembly=false 现状保持不变。
3. 发布目录中的 Agent 必须来自 Release / win-x64 / self-contained publish 产物，不允许复制 Debug / obj / bin 输出。
4. StartupCommandBuilder 继续基于 Environment.ProcessPath / Process.MainModule.FileName 构建 command；不要改用 Assembly.Location。
5. AgentProcessService 找不到 Agent exe 时返回安全错误：AgentExeLocator / ResolveStartInfo 可抛 FileNotFoundException，但 StartAgentAsync 调用链必须被 UI 捕获并显示安全错误，不得导致 App 进程崩溃；日志中不得泄露完整用户路径、SID、用户名。
6. build\publish.ps1 的最终合并发布目录固定为 .codex\publish\App，Agent 临时发布目录为 .codex\publish\Agent；最终可运行产物以 .codex\publish\App 为准。
7. 完成说明落盘前不要删除 .codex\publish\App 最终发布目录；如清理 build/test 临时目录，不要误删 publish 验收产物。
8. Agent exe 查找失败时，错误文案可以显示候选来源类型（BaseDirectory / EnvVar / DevelopmentFallback），但不得显示完整用户目录、SID、用户名或完整 home 路径。
9. 如本阶段保持默认 exe 名 QuantifiedSelf.Windows.App.exe / QuantifiedSelf.Windows.Agent.exe，请在完成说明中明确记录“未改名，品牌 exe 名留到后续阶段评估”。
```

需要实现的范围：

1. 统一版本号。

建议修改：

```text
Directory.Build.props
```

要求：

```text
新增统一的 <Version>、<AssemblyVersion>、<FileVersion>，例如 0.1.0。
App 和 Agent 项目自动继承这些属性，无需在每个 csproj 重复设置。
如需 Company/Product/AssemblyTitle 等也可在 Directory.Build.props 统一，但不得破坏 App 现有已设置的 WUJI 品牌属性。
```

2. 添加 publish 配置。

建议新增：

```text
src\QuantifiedSelf.Windows.App\Properties\PublishProfiles\FolderProfile.pubxml
src\QuantifiedSelf.Windows.Agent\Properties\PublishProfiles\FolderProfile.pubxml
```

要求：

```text
配置 self-contained win-x64 Release 发布。
目标框架 net8.0-windows。
publish profile 可配置默认 PublishDir；实际最终发布目录由 build\publish.ps1 固定为 .codex\publish\App 和 .codex\publish\Agent。
不要强制启用单文件输出；可将单文件作为可选备注或单独 profile，但不得牺牲路径可靠性。
```

3. 添加 publish 脚本，把 App 和 Agent 放到同一目录。

建议新增：

```text
build\publish.ps1
```

要求：

```text
1. 最终合并发布目录固定为 .codex\publish\App；Agent 临时发布目录固定为 .codex\publish\Agent；最终可运行产物以 .codex\publish\App 为准。
2. 先 dotnet publish App.csproj -c Release -r win-x64 --self-contained 到 .codex\publish\App。
3. 再 dotnet publish Agent.csproj -c Release -r win-x64 --self-contained 到 .codex\publish\Agent。
4. 将 .codex\publish\Agent 目录中的内容复制到 .codex\publish\App，使 App 和 Agent 最终在同一目录；如遇同名文件且内容不同，应失败或明确 warning，不得静默覆盖未知差异。
5. 验证 .codex\publish\App 下同时存在 App exe 和 Agent exe（名称与 12.1 实际输出一致）。
6. 脚本接受 -Configuration、-RuntimeIdentifier、-OutputPath 等参数；其中 -OutputPath 为根目录，默认 .codex\publish，脚本内部固定生成 App / Agent 子目录。
7. self-contained / Release / win-x64 的验证以 publish 命令参数和输出产物为准；脚本必须显式调用 `-c Release -r win-x64 --self-contained true`，并验证输出目录中存在 .exe，且不位于 bin\Debug、obj 等开发目录；可选检查 .deps.json / runtime 文件存在。不要只依赖目录名字符串判断。
8. 脚本执行失败时返回非 0 exit code，并输出安全错误信息（不泄露完整路径、SID、用户名）。
```

可选替代：

```text
如你认为 MSBuild target 更合适，可在 App.csproj 中新增 AfterPublish target，自动 publish Agent 并复制到 App publish 目录。但不得因此破坏 dotnet build / dotnet test 的日常开发流程。
```

4. 修复 AgentProcessService 的 Agent exe 路径解析。

建议修改：

```text
src\QuantifiedSelf.Windows.App\Services\AgentProcessService.cs
```

当前 ResolveStartInfo() 直接从开发目录 ../../../../.. 推导 Agent 路径，发布后会失效。要求改为：

```text
1. 优先查找 AppContext.BaseDirectory 下的 Agent exe。
   - 优先候选名 QuantifiedSelf.Windows.Agent.exe。
   - 如 12.1 决定改名，则同时查找 WUJI.Agent.exe。
2. 其次读取环境变量 QUANTIFIEDSELF_WINDOWS_AGENT_EXE 指定路径。
3. 最后 fallback 到开发目录推导路径（保持现有行为，用于 dotnet run / 开发调试）。
4. 找不到时：
   - 记录安全错误日志，可显示候选来源类型（BaseDirectory / EnvVar / DevelopmentFallback），但不得显示完整用户目录、SID、用户名或完整 home 路径。
   - 抛出 FileNotFoundException 等可控异常，不要让 InvalidOperationException / NullReferenceException 逃逸。
```

如果直接测试 ResolveStartInfo 困难，可将其核心路径解析逻辑提取为内部静态方法或辅助类（如 AgentExeLocator），使其可注入 baseDirectory 和环境变量值进行测试。

5. 如决定改名，同步更新 StartupCommandBuilder 中的引用。

建议修改：

```text
src\QuantifiedSelf.Windows.App\Services\StartupCommandBuilder.cs
```

要求：

```text
由于 StartupCommandBuilder 基于 Environment.ProcessPath，通常无需改动。
但如 12.1 将 App exe 改名为 WUJI.exe，请确保 BuildCommand()、IsValidProcessPath()、CommandsMatch() 等逻辑不依赖硬编码的旧名称。
本阶段不需要新增功能。
```

6. 可测试性要求。

单元测试不要依赖真实 Windows 注册表，不要启动真实 WPF App，不要依赖系统托盘。

建议新增测试：

```text
AgentExeLocator_PrefersBaseDirectory
AgentExeLocator_FallsBackToEnvironmentVariable
AgentExeLocator_FallsBackToDevelopmentPath
AgentExeLocator_ThrowsFileNotFound_WhenNotFound
AgentExeLocator_LogsRedactedPaths
AgentProcessService_ResolveStartInfo_UsesBaseDirectoryAgentExe
AssemblyVersion_MatchesDirectoryBuildProps
FileVersion_MatchesDirectoryBuildProps
```

测试要求：

```text
单元测试只测 AgentExeLocator、版本属性、路径安全，不执行完整 dotnet publish。
AgentExeLocator 测试使用临时目录构造候选文件，不依赖真实 publish 输出。
路径失败测试中，断言日志/异常消息不包含完整用户路径、SID、用户名。
版本号测试读取 App 和 Agent 程序集属性，验证与 Directory.Build.props 一致。
PublishedOutput_* 验证（目录包含 App/Agent、同目录、Release/self-contained/win-x64）由 build\publish.ps1 脚本验收或完成说明记录，不放入普通 dotnet test。
如新增 publish 脚本测试，可只验证脚本参数解析和路径安全性；不要每次运行完整 publish（避免耗时）。
```

建议重点查看这些文件：

```text
Directory.Build.props
src\QuantifiedSelf.Windows.App\QuantifiedSelf.Windows.App.csproj
src\QuantifiedSelf.Windows.Agent\QuantifiedSelf.Windows.Agent.csproj
src\QuantifiedSelf.Windows.App\Properties\PublishProfiles\FolderProfile.pubxml（新建）
src\QuantifiedSelf.Windows.Agent\Properties\PublishProfiles\FolderProfile.pubxml（新建）
src\QuantifiedSelf.Windows.App\Services\AgentProcessService.cs
src\QuantifiedSelf.Windows.App\Services\StartupCommandBuilder.cs
build\publish.ps1（新建）
tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs
```

验收标准：

- build 0 warning / 0 error。
- test 全部通过。
- `build\publish.ps1` 执行后，输出目录同时包含 App exe 和 Agent exe，且 Agent 来自 Release / win-x64 / self-contained publish。
- `dotnet run` 开发行为不回归。
- 发布后的 App 可手动启动并显示主窗口。
- 发布后的 App 可启动 Agent。
- 发布后的 App 可通过 `--from-autostart --start-hidden` 启动到托盘（不显示主窗口）。
- AgentProcessService 在发布目录下能找到 Agent，在开发目录下也能找到 Agent。
- 版本号在 App 和 Agent 的 Assembly / File 版本中可见，且与 Directory.Build.props 一致。
- 不改阶段 11 自启注册逻辑。
- 不引入安装包、图标、MSIX / WiX / Inno Setup。

请完成代码和测试修改后运行：

```powershell
dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex/build-output/
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex/test-output/
```

并手动验证 publish 脚本：

```powershell
.\build\publish.ps1
Get-ChildItem .codex\publish\App
# 确认存在 QuantifiedSelf.Windows.App.exe 和 QuantifiedSelf.Windows.Agent.exe

# 从发布目录启动 App，验证主窗口
cd .codex\publish\App
.\QuantifiedSelf.Windows.App.exe

# 再次用自启参数验证隐藏到托盘
.\QuantifiedSelf.Windows.App.exe --from-autostart --start-hidden
```

请不要并行运行 build 和 test，避免 obj 文件锁。

如果 build/test 产生 `.codex` 临时输出，请在确认结果后清理，清理前必须确认路径位于当前 workspace 内。
完成说明落盘前不要删除 .codex\publish\App 最终发布目录；如清理 build/test 临时目录，不要误删 publish 验收产物。

补充约束：

```text
1. AgentExeLocator / ResolveStartInfo 找不到 Agent 时可以抛 FileNotFoundException，但 UI 调用链必须捕获并显示安全错误，不得导致 App 崩溃。
2. build\publish.ps1 复制 Agent publish 产物到 App publish 目录时，如遇同名文件且内容不同，应失败或明确 warning，不得静默覆盖未知差异。
3. self-contained / Release / win-x64 的验证以 publish 命令参数和输出产物为准；不要只依赖目录名字符串判断。
4. 普通单元测试不要执行完整 dotnet publish；PublishedOutput_* 验证放在 publish 脚本验收或完成说明中记录。
5. publish 脚本验收必须从 .codex\publish\App 实际启动 App exe，并验证主窗口启动与 --from-autostart --start-hidden 隐藏启动。
```

建议清理方式：

```powershell
$root = (Resolve-Path '.').Path
Get-ChildItem -Path src,tests,build -Recurse -Directory -Filter .codex | ForEach-Object {
    $full = $_.FullName
    if (-not $full.StartsWith($root, [System.StringComparison]::OrdinalIgnoreCase)) { throw "Refusing to remove outside workspace: $full" }
    Remove-Item -Recurse -Force $full
}
```

完成后请更新或新建：

```text
docs\下一步计划-2026-07-07-安装包与发布体验MVP\阶段12.1-完成说明-YYYY-MM-DD.md
```

记录实际 publish 输出名、是否改名（如保持默认名需明确记录“未改名，品牌 exe 名留到后续阶段评估”）、路径解析策略、publish 脚本位置、.codex\publish\App 验证结果、测试通过情况、遇到的问题。
