请在仓库 `C:\Users\WangZhenchi\work\WUJI\Win` 中实施阶段 12.2。

请先阅读并严格遵循这些文档，中文文档用 UTF-8 读取：

1. `docs\下一步计划-2026-07-07-安装包与发布体验MVP.md`
2. `docs\下一步计划-2026-07-07-安装包与发布体验MVP\README.md`
3. `docs\下一步计划-2026-07-07-安装包与发布体验MVP\02-阶段12.2-应用图标与品牌资源.md`
4. `docs\下一步计划-2026-07-07-安装包与发布体验MVP\阶段12.1-完成说明-YYYY-MM-DD.md`（如已存在，了解 12.1 最终 exe 名和 publish 目录）

任务目标：

实现“阶段 12.2：应用图标与品牌资源”。

图标源文件已由用户放在：

```text
src\img\orbit_space_univers_icon_148940.ico          (32x32)
src\img\orbit_space_univers_icon_148940 (1).ico       (64x64)
src\img\orbit_space_univers_icon_148940 (2).ico       (128x128)
src\img\orbit_space_univers_icon_148940 (3).ico       (128x128，与 (2) 相同)
```

本阶段目标是把上述图标整合为一套内嵌 .ico 资源，应用到 App 主窗口、任务栏、托盘，并确保发布输出包含该图标。

尤其注意本阶段不做：

- 不做安装包（阶段 12.3）。
- 不改发布配置（阶段 12.1）。
- 不实现 `--register-autostart` / `--unregister-autostart` 命令行入口（阶段 12.4）。
- 不改阶段 11 自启注册逻辑。
- 不改阶段 10 CloseToTray / MinimizeToTray / 托盘命令语义。
- 不改阶段 8 IPC 协议。
- 不改阶段 7 PruneData / ClearHistory 规则。
- 不改 Agent 路径解析逻辑。

补充要求：

```text
1. 当前 src\img 中的图标均为单尺寸文件（32x32、64x64、128x128）。本阶段需要生成一个多尺寸 wuji.ico，至少包含 16x16、32x32、48x48、256x256；如源文件不足，允许用 Pillow / ImageMagick / .NET 生成缺失尺寸（通过高质量缩放）。
2. 最终 wuji.ico 必须内嵌到 App 项目中，路径固定为 src\QuantifiedSelf.Windows.App\Resources\wuji.ico；不要从 src\img 直接引用，避免发布目录结构混乱。
3. 托盘图标、窗口图标、任务栏图标使用同一个 wuji.ico。
4. 图标应用后，托盘文字仍为 WUJI，但图标不再使用 SystemIcons.Application。
5. 如无法生成多尺寸图标（工具缺失或源文件异常），允许使用最大的 128x128 源图标作为单尺寸 wuji.ico 占位，但必须在完成说明中记录“缺少 16/48/256 尺寸，Windows 会自动缩放，后续需补全多尺寸图标”。
6. 不要修改图标本身的视觉内容；只做尺寸整合、嵌入和应用。
```

需要实现的范围：

1. 生成多尺寸 wuji.ico。

建议新增：

```text
src\QuantifiedSelf.Windows.App\Resources\wuji.ico
```

要求：

```text
1. 读取 src\img 中的图标源文件，去重（(2) 和 (3) 内容相同，只需保留一个）。
2. 使用 Pillow（python -m pip install Pillow 如未安装）或 ImageMagick 或 .NET 生成缺失尺寸。
3. 最终 wuji.ico 至少包含：16x16、32x32、48x48、64x64、128x128、256x256。
4. 如果源图标是 128x128 22bpp，建议先生成 256x256（高质量缩放），再向下生成 16/32/48/64。
5. 生成脚本/命令可放在 build\generate-icon.ps1 或 build\generate-icon.py（二选一），并在完成说明中记录使用方式。
6. 如选择不生成缺失尺寸，必须按补充要求第 5 条记录。
```

2. 将 wuji.ico 作为资源嵌入 App 项目。

建议修改：

```text
src\QuantifiedSelf.Windows.App\QuantifiedSelf.Windows.App.csproj
```

要求：

```text
1. 添加 <Resource Include="Resources\wuji.ico" /> 或等效方式，使图标随 App 程序集嵌入/发布。
2. 确保 dotnet publish 输出目录中包含 wuji.ico 或可执行文件已包含图标资源。
3. 不要破坏现有 WPF / WindowsForms 引用。
```

3. 设置 WPF 主窗口图标。

建议修改：

```text
src\QuantifiedSelf.Windows.App\MainWindow.xaml
```

要求：

```text
1. 设置 Window.Icon="pack://application:,,,/Resources/wuji.ico" 或等效方式。
2. 窗口标题栏和任务栏显示 wuji.ico。
3. 如果 pack URI 在发布目录下异常，需兼容处理（如使用相对路径或代码设置）。
```

4. 设置托盘图标。

建议修改：

```text
src\QuantifiedSelf.Windows.App\Services\NotifyIconTrayIconAdapter.cs
```

当前代码：

```csharp
_notifyIcon = new NotifyIcon
{
    Icon = SystemIcons.Application,
    Text = "WUJI",
    Visible = false
};
```

要求：

```text
1. 将 NotifyIcon.Icon 从 SystemIcons.Application 改为 wuji.ico。
2. 加载方式需同时兼容开发时 dotnet run 和发布目录：
   - 优先从嵌入资源加载。
   - 或从 AppContext.BaseDirectory / 程序集所在目录加载 Resources\wuji.ico。
3. 如加载失败，回退到 SystemIcons.Application 并记录 warning，不要崩溃。
4. 托盘 tooltip、菜单行为保持不变。
```

可选：如果托盘图标和窗口图标需要不同尺寸，使用同一个 wuji.ico 即可，Windows 会自动选择合适尺寸。

5. 确保发布输出包含图标。

建议验证：

```text
运行 build\publish.ps1 后，.codex\publish\App 目录下应包含 wuji.ico（或 App exe 本身已包含图标资源）。
```

如 12.1 的 publish 脚本未复制 Resources 目录，需同步更新 build\publish.ps1 确保资源文件进入最终目录。

6. 可测试性要求。

单元测试不要依赖真实 Windows 注册表，不要启动真实 WPF App，不要依赖系统托盘。

建议新增测试：

```text
AppIconResource_Exists
AppIconResource_ContainsMultipleSizes
MainWindow_HasIconSet
NotifyIconAdapter_UsesWujiIcon
PublishedOutput_ContainsWujiIcon
```

测试要求：

```text
AppIconResource_Exists：验证 Resources\wuji.ico 存在。
AppIconResource_ContainsMultipleSizes：读取 .ico 文件头，验证至少包含 16x16、32x32、48x48、256x256 中的最小集合；如本阶段使用单尺寸占位，则测试需反映实际尺寸并在完成说明记录。
MainWindow_HasIconSet：通过反射/解析 XAML 验证 Window.Icon 不为空。
NotifyIconAdapter_UsesWujiIcon：验证 NotifyIconTrayIconAdapter 构造后 Icon 不是 SystemIcons.Application（允许嵌入资源加载失败后的回退，但需记录）。
PublishedOutput_ContainsWujiIcon：作为 publish 脚本验收项，不放入普通 dotnet test。
```

建议重点查看这些文件：

```text
src\QuantifiedSelf.Windows.App\QuantifiedSelf.Windows.App.csproj
src\QuantifiedSelf.Windows.App\MainWindow.xaml
src\QuantifiedSelf.Windows.App\Services\NotifyIconTrayIconAdapter.cs
src\QuantifiedSelf.Windows.App\Services\TrayService.cs
src\img\*.ico
build\publish.ps1（如 12.1 已创建）
tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs
```

验收标准：

- build 0 warning / 0 error。
- test 全部通过。
- `src\QuantifiedSelf.Windows.App\Resources\wuji.ico` 存在且可被项目引用。
- 主窗口标题栏显示 wuji.ico。
- 任务栏显示 wuji.ico。
- 托盘显示 wuji.ico（不再是系统默认图标）。
- 发布后的 App 仍显示正确图标。
- 不改变阶段 12.1 publish 行为（如破坏需修复）。
- 不改阶段 10 托盘生命周期语义。
- 不改阶段 11 自启注册逻辑。

请完成代码和测试修改后运行：

```powershell
dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex/build-output/
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex/test-output/
```

并手动验证：

```powershell
# 生成图标（如使用脚本）
.\build\generate-icon.ps1

# 验证 wuji.ico 尺寸
python -c "import struct; d=open('src/QuantifiedSelf.Windows.App/Resources/wuji.ico','rb').read(); print(struct.unpack('<H', d[4:6])[0], 'images')"

# 运行 App 查看窗口/任务栏/托盘图标
dotnet run --project src/QuantifiedSelf.Windows.App/QuantifiedSelf.Windows.App.csproj

# 或发布目录验证
.\build\publish.ps1
.\QuantifiedSelf.Windows.App.exe
```

请不要并行运行 build 和 test，避免 obj 文件锁。

如果 build/test 产生 `.codex` 临时输出，请在确认结果后清理，清理前必须确认路径位于当前 workspace 内。

建议清理方式：

```powershell
$root = (Resolve-Path '.').Path
Get-ChildItem -Path src,tests,build -Recurse -Directory -Filter .codex | ForEach-Object {
    $full = $_.FullName
    if (-not $full.StartsWith($root, [System.StringComparison]::OrdinalIgnoreCase)) { throw "Refusing to remove outside workspace: $full" }
    Remove-Item -Recurse -Force $full
}
```

完成后请更新或新建：

```text
docs\下一步计划-2026-07-07-安装包与发布体验MVP\阶段12.2-完成说明-YYYY-MM-DD.md
```

记录图标来源、生成方式、最终 wuji.ico 包含的尺寸、应用到哪些位置、遇到的问题、以及是否使用单尺寸占位。
