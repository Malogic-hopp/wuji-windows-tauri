请在仓库 `C:\Users\WangZhenchi\work\WUJI\Win` 中实施阶段 9.5。

请先阅读并严格遵循这些文档，中文文档用 UTF-8 读取：

1. `docs\下一步计划-2026-07-01-Agent状态流订阅RefreshService优化MVP.md`
2. `docs\下一步计划-2026-07-01-Agent状态流订阅RefreshService优化MVP\README.md`
3. `docs\下一步计划-2026-07-01-Agent状态流订阅RefreshService优化MVP\05-阶段9.5-Diagnostics刷新健康状态展示.md`
4. `docs\下一步计划-2026-07-01-Agent状态流订阅RefreshService优化MVP\阶段9.4-完成说明-2026-07-02.md`
5. `docs\Agent Start按钮无响应问题记录-2026-07-02.md`
6. `docs\下一步计划-2026-07-01-NamedPipe控制通道MVP\阶段8-完成说明-2026-07-01.md`

任务目标：

实现“阶段 9.5：Diagnostics 刷新健康状态展示”。

本阶段目标是让 Diagnostics 页面能解释 WPF UI 自身刷新循环是否健康，包括 2 秒 status polling、当前页 page refresh、skipped count 和最近安全错误。

尤其注意本阶段不做：

- 不改变阶段 9.2 的 2 秒 status polling timer interval。
- 不改变阶段 9.4 的 status/page refresh gate 结构。
- 不改变阶段 9.3 的 command availability 规则。
- 不回退 `Stale + IsRunning=false` 时 Start Agent 可用的修复。
- 不改变 Start / Pause / Resume / Stop / ReloadConfig / PruneData / ClearHistory 的命令执行链路。
- 不改变 ClearHistory 的 `CLEAR` 二次确认流程。
- 不改 AgentStateMachine。
- 不改 Named Pipe IPC 协议。
- 不把 Samples / Sessions / Apps 数据查询搬到 IPC。
- 不改 SQLite 查询服务语义。
- 不把 RefreshHealth 写入 SQLite。
- 不新增 JSONL 日志文件。
- 不新增 runtime refresh health 文件。
- 不写 agent_events 记录 refresh health。
- 不做复杂刷新图表。
- 不做真正长连接状态流或 subscribe 协议。
- 不做托盘、自启、安装包。

需要实现的范围：

1. Diagnostics 展示 RefreshHealth。

阶段 9.4 已扩展：

```text
src\QuantifiedSelf.Windows.App\Services\RefreshHealthSnapshot.cs
```

现有字段包括：

```text
LastStatusRefreshSuccessUtc
LastStatusRefreshErrorUtc
LastStatusRefreshError
SkippedStatusRefreshCount
IsStatusRefreshing

LastPageRefreshSuccessUtc
LastPageRefreshErrorUtc
LastPageRefreshError
SkippedPageRefreshCount
IsPageRefreshing
LastPageRefreshPage
```

阶段 9.5 要求在 Diagnostics 页面展示这些内存状态，建议至少包括：

```text
Refresh loop status: healthy / degraded / refreshing
Last status refresh success
Last status refresh error
Status skipped count
Last page refresh success
Last page refresh error
Page skipped count
Last page refresh page
Status polling interval
Page refresh interval
```

如果字段命名按现有代码需要调整，可以使用等价名称，但 Diagnostics 必须能区分：

```text
status refresh health
page refresh health
status skipped count
page skipped count
```

2. MainWindowViewModel 暴露安全绑定属性。

建议在 `MainWindowViewModel` 中新增只读绑定属性，例如：

```text
RefreshLoopStatusText
LastStatusRefreshSuccessText
LastStatusRefreshErrorText
SkippedStatusRefreshCountText
LastPageRefreshSuccessText
LastPageRefreshErrorText
SkippedPageRefreshCountText
LastPageRefreshPageText
StatusPollingIntervalText
PageRefreshIntervalText
```

也可以合并为少量 summary 文本，但至少要让测试能直接断言：

```text
refresh loop healthy / degraded
status skipped count
page skipped count
safe status error
safe page error
polling interval
page refresh interval
```

绑定属性应由一个集中方法更新，例如：

```text
UpdateRefreshHealthPresentation()
```

调用时机建议：

```text
RefreshDiagnosticsAsync 进入 Diagnostics 当前页时
status refresh / page refresh 结束后，如当前页是 Diagnostics，可以更新展示
page refresh skipped / error 后，如当前页是 Diagnostics，可以更新展示
```

不要为了展示 RefreshHealth 而触发额外页面数据查询。

补充要求：

```text
Diagnostics 展示 RefreshHealth 时，只读取当前 App 内存中的 RefreshHealthSnapshot。
不得为了展示 RefreshHealth 主动调用 RefreshService.RefreshStatusAsync。
不得为了展示 RefreshHealth 主动调用 RefreshService.RefreshAsync。
不得为了展示 RefreshHealth 主动调用 RefreshCurrentPageDataAsync 或任何页面 LoadAsync。
不得形成 Diagnostics 刷新 -> 触发 refresh -> 再刷新 Diagnostics 的递归链路。
```

3. Diagnostics UI 小范围增加展示。

当前 Diagnostics 仍在：

```text
src\QuantifiedSelf.Windows.App\MainWindow.xaml
```

要求：

```text
在现有 Diagnostics Overview 区域增加 Refresh loop health 信息。
不要重构整个 MainWindow.xaml。
不要把 Diagnostics 拆成新 UserControl，除非现有代码已经明显需要且改动很小。
不要改变导航顺序。
不要影响 Recent Events / Recent Errors 列表。
不要删除现有 IPC 状态展示。
```

建议用简单的 `TextBlock` 或现有卡片风格展示：

```text
Refresh loop: Healthy / Degraded / Refreshing
Status refresh: last success / last safe error / skipped count
Page refresh: last success / last safe error / skipped count / last page
Intervals: status polling 2s / page refresh app-settings refreshIntervalSeconds
```

避免做复杂图表、进度条或新控件。

4. IPC 状态展示必须保留。

现有 Diagnostics 已展示：

```text
IpcStatusText
DisplayPipeName
Last IPC success
Last IPC error
Last fallback used
Last command source
```

阶段 9.5 要求：

```text
IPC connected / unavailable / fallback 状态仍可见。
DisplayPipeName 仍可见。
Last IPC error 继续使用安全短句。
不显示 FullPipeName。
不显示完整 SID。
不显示本机路径。
```

不要让 RefreshHealth 展示覆盖或替换 `IpcStatusText`。

5. 安全文本要求。

所有 RefreshHealth 错误展示必须安全：

```text
使用 DiagnosticMessageSanitizer.CreateSafeText / CreateSafeExceptionMessage 或等价安全格式化。
不展示完整本机路径。
不展示异常原文。
不展示 FullPipeName / SID。
不展示 SQL。
不展示窗口标题。
不展示原始 JSON。
```

注意：

```text
RefreshHealthSnapshot 内部已有的 LastStatusRefreshError / LastPageRefreshError 理论上应是安全文本。
Diagnostics 展示前仍应保守截断，例如 120 或 160 字符。
空错误显示 None / -，不要显示 null。
```

本阶段不要求清理现有 `CurrentJournalPathText` 历史展示行为，但不得新增更多本机路径暴露。RefreshHealth 和 IPC 错误必须保持安全展示。

不要在本阶段重构 `CurrentJournalPathText`、runtime/health/control JSON 文本框等既有 Diagnostics 路径展示字段；它们属于历史展示行为。阶段 9.5 只要求新增的 RefreshHealth / IPC 错误展示不引入新的路径、FullPipeName、SID、SQL、窗口标题或原始 JSON 泄露。

6. Refresh loop healthy / degraded 判定。

建议规则：

```text
Refreshing:
    IsStatusRefreshing 或 IsPageRefreshing 为 true

Degraded:
    LastStatusRefreshError 不为空
    或 LastPageRefreshError 不为空

Healthy:
    无 status/page 最近错误
    且至少有一次 status refresh 或 page refresh 成功

Ready:
    尚无任何 refresh 结果
```

如采用不同规则，请在完成说明中写清楚。

错误清除语义必须保持 status/page 分离：

```text
最近一次 status refresh 成功后，可以清空 LastStatusRefreshError。
最近一次 page refresh 成功后，可以清空 LastPageRefreshError。
status refresh 成功不得清空 page error。
page refresh 成功不得清空 status error。
status/page 错误不得互相覆盖或互相清空。
```

7. Interval 展示。

Status polling interval：

```text
来自 RefreshOptions.StatusPollingInterval，默认 2 seconds。
```

Page refresh interval：

```text
来自 AppSettings.RefreshIntervalSeconds / 当前 _refreshTimer interval。
```

如果现有代码不方便直接从 `RefreshOptions` 暴露，可以先在 MainWindowViewModel 中按实际 timer interval 展示，但不要新增用户可编辑配置项。

补充要求：

```text
StatusPollingIntervalText 和 PageRefreshIntervalText 只展示当前实际使用值。
不得新增设置项。
不得修改 app-settings.json schema。
不得新增 WindowsAgentOptions 字段。
```

8. 不产生持久化副作用。

阶段 9.5 的 RefreshHealth 第一版只保存在 WPF App 内存中。

禁止：

```text
写入 agent_events
写入 JSONL
写入 SQLite
写入 runtime refresh health 文件
新增配置文件字段
```

如果测试需要验证“不写 agent_events”，可以用现有 DiagnosticsDataService / 数据库事件计数做轻量断言；不要为了测试引入新持久化。

补充要求：

```text
如果现有测试环境不方便精确验证 JSONL / SQLite 写入次数，可以用轻量方式验证 RefreshHealth 展示路径没有调用事件写入逻辑。
不要为了测试新增持久化 hook。
不要新增 AgentEventLogger / Diagnostics writer 的生产代码依赖。
```

建议重点查看这些现有文件：

- `src\QuantifiedSelf.Windows.App\ViewModels\MainWindowViewModel.cs`
- `src\QuantifiedSelf.Windows.App\Services\RefreshHealthSnapshot.cs`
- `src\QuantifiedSelf.Windows.App\Services\RefreshService.cs`
- `src\QuantifiedSelf.Windows.App\Services\RefreshOptions.cs`
- `src\QuantifiedSelf.Windows.App\Services\AgentIpcStatusService.cs`
- `src\QuantifiedSelf.Windows.App\MainWindow.xaml`
- `src\QuantifiedSelf.Windows.Core\Events\DiagnosticMessageSanitizer.cs`
- `tests\QuantifiedSelf.Windows.Tests\DataFlowTests.cs`

建议新增或调整测试：

- `MainWindowViewModel_DiagnosticsShowsRefreshHealth`
- `MainWindowViewModel_DiagnosticsShowsSkippedRefreshCount`
- `MainWindowViewModel_DiagnosticsRedactsRefreshError`
- `MainWindowViewModel_DiagnosticsKeepsIpcStatusVisible`
- `MainWindowViewModel_DiagnosticsShowsStatusAndPageHealthSeparately`
- `MainWindowViewModel_RefreshHealthDoesNotWriteAgentEvents`
- `MainWindowViewModel_DiagnosticsShowsPageRefreshInterval`

测试要求：

- Diagnostics 当前页刷新后，Refresh loop health 文本可见且能区分 Healthy / Degraded / Refreshing。
- status refresh success 和 page refresh success 分别格式化显示。
- status skipped count 和 page skipped count 分别可见。
- status error 和 page error 分别可见，且错误文本脱敏。
- 用包含 `C:\Users\...\secret.db`、UNC 路径、SQL 片段或 full pipe name 的错误构造测试，Diagnostics 显示不得包含这些原文。
- IPC 状态文本仍包含 safe display pipe / source / last success / fallback 信息。
- 不显示完整 SID 或 FullPipeName。
- Recent Events / Recent Errors 仍能加载，不因新增 RefreshHealth 区域回归。
- RefreshHealth 展示不写入 agent_events / JSONL / SQLite。
- 如果精确验证持久化写入成本过高，至少验证 Diagnostics RefreshHealth 展示路径只读取内存快照，不调用事件写入服务。
- 阶段 9.5 不允许回退 `AgentCommandAvailability` 中 `Stale + IsRunning=false => CanStart=true` 的修复。
- 保留并回归 `AgentCommandAvailability_AllowsStartWhenStaleButProcessGone` 和 `AgentCommandAvailability_HandlesStaleConservatively`。
- 测试不要用 `.GetAwaiter().GetResult()`；异步测试使用 `async Task` + `await`。

如果需要构造 RefreshHealth 状态：

```text
优先直接操作测试中的 RefreshService.Health，调用 RecordStatusSuccess / RecordStatusError / RecordStatusSkipped / RecordPageSuccess / RecordPageError / RecordPageSkipped。
不要依赖真实 2 秒 timer。
不要用 Thread.Sleep 或长时间 Task.Delay。
```

验收标准：

- build 0 warning / 0 error。
- test 全部通过。
- Diagnostics 显示 Refresh loop healthy / degraded / refreshing。
- Diagnostics 显示 status refresh 和 page refresh 的最近成功、最近错误、skipped count。
- Diagnostics 显示 status polling interval 和 page refresh interval。
- 最近刷新错误显示安全短句。
- IPC / fallback 状态仍可见。
- RefreshHealth 不产生高频 agent_events。
- 旧 Diagnostics recent events / errors 不回归。
- 阶段 9.4 status/page refresh gate 测试不回归。
- 阶段 9.3 command availability 测试不回归，包括 `Stale + IsRunning=false` 可 Start。
- 阶段 8 IPC 测试不回归。

请完成代码和测试修改后运行：

```powershell
dotnet build QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\build-output\
dotnet test QuantifiedSelf.Windows.sln --no-restore -p:BaseOutputPath=.codex\test-output\
```

如果 build/test 通过，请汇总：

- 修改了哪些文件
- 实现了哪些行为
- 新增/更新了哪些测试
- build/test 结果

同时请把完成情况落盘为完成说明文档：

```text
docs\下一步计划-2026-07-01-Agent状态流订阅RefreshService优化MVP\阶段9.5-完成说明-YYYY-MM-DD.md
```

`YYYY-MM-DD` 使用执行当天日期；如果执行环境无法确定日期，使用 `2026-07-02`。

完成说明至少包含：

```text
1. 阶段目标
2. 修改文件清单
3. Diagnostics RefreshHealth 展示说明
4. 安全脱敏说明
5. IPC 状态保留说明
6. 新增 / 更新测试
7. build / test 结果
8. 明确未做事项
9. 风险与遗留问题
```

其中“明确未做事项”需要特别确认：

```text
未改变 status polling timer interval
未改变 status/page refresh gate 结构
未改变 command availability 规则
未改变命令执行链路
未改变 ClearHistory 的 CLEAR 二次确认
未改变 IPC 协议
未把页面数据查询搬到 IPC
未写 agent_events / JSONL / SQLite / runtime refresh health 文件
未做复杂图表
未做真正长连接状态流
未做托盘、自启、安装包
```

注意：

- `Get-Content` 读取中文文档时使用 `-Encoding UTF8`。
- 不要回滚用户或其他 agent 的改动。
- 不要让完成说明自行判断是否进入下一阶段；只写事实、验证结果、未做事项、风险与遗留问题。
- 不要把阶段 9.6 的断连重连专项验收扩大实施。
- 不要引入新的持久化 refresh health。
- 不要让 RefreshHealth 错误展示泄露路径、SQL、FullPipeName、SID、窗口标题或原始 JSON。
