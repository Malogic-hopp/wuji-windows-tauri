可行，这份阶段 9.6 提示词**可以交给 agent 执行**。它的定位是对的：阶段 9.6 不是新增大功能，而是围绕 9.1–9.5 已完成的刷新机制，补齐 **断连、重连、Agent 重启、Maintenance、Settings dirty、Stale 状态** 的专项验收和小修复。这个和阶段 9.6 文档目标一致。

整体看，这份提示词边界控制得比较稳：不做真正状态流、不改 2 秒轮询、不改 status/page gate、不重写 command availability、不回退 Start 修复、不改 IPC、不改 SQLite、不写 refresh health 持久化。这些限制都很必要。

---

# 总体结论

可以执行。

这份提示词的核心目标很清楚：

```text
验证断连 / 重连 / Agent restart 后：
1. UI 不崩；
2. 状态能恢复；
3. IPC / fallback 可解释；
4. MainWindow 和 Settings 按钮不漂移；
5. Settings dirty 不被覆盖；
6. Stale + IsRunning=false 时 Start 可用不回归。
```

这个阶段应该以**测试、验收文档、小范围 bugfix**为主，不应该再引入新的架构能力。你的提示词已经把这一点写清楚了。

---

# 我认为写得好的地方

## 1. 明确这是验收阶段，不是功能扩展阶段

你写了：

```text
本阶段重点不是新增大功能，而是围绕阶段 9.1-9.5 已建立的机制补齐专项验证。
```

这很好。阶段 9.6 最怕 agent 借机大改刷新架构，你已经提前限制住了。

---

## 2. 把 Start 按钮修复纳入回归验收

你专门写了：

```text
不回退 Stale + IsRunning=false 时 Start Agent 可用的修复
```

并要求覆盖：

```text
Stale + IsRunning=false:
    CanStart = true

Stale + IsRunning=true:
    CanStart = false
    CanStop = true
```

这个非常重要。这个问题是真实用户路径触发的，必须进入回归测试矩阵。

---

## 3. latest-wins 断连恢复写得很到位

你提到：

```text
旧 fallback / stale 结果晚于新的 IPC Running 结果返回
旧 sequence 结果不得覆盖新 sequence 结果
```

这正是状态轮询中最容易出现的竞态。阶段 9.6 专门补这个测试是对的。

---

## 4. Settings dirty 场景覆盖完整

你写清楚了：

```text
Settings IsDirty=true 时：
    配置字段不被覆盖
    Agent 状态仍更新
    Settings 内按钮状态仍更新
```

这个很关键。阶段 9.2–9.5 做了更频繁刷新，如果不验 dirty，很容易回归。

---

# 建议补强 1：明确“可以修 bug，但必须最小修复”

你已经写了“可以修复小 bug，但不要大改刷新架构”。建议再强化一句：

```text
如果测试暴露 bug，只允许在当前断连 / 重连 / 状态派发 / RefreshHealth 范围内做最小修复；不得重构 RefreshService、AgentStatusService、AgentControlService 或 SettingsViewModel 的整体架构。
```

这样能防止 agent 为了修一个测试，改出大范围结构变化。

---

# 建议补强 2：把“自动化测试优先，手动清单不代替测试”写清楚

你要求新增手动验收清单，这很好。但阶段 9.6 不能只写清单。

建议补一句：

```text
手动验收清单不能替代能自动化覆盖的测试；凡是可用 fake service / fake status / fake IPC 构造的断连、重连、Stale、Maintenance、Settings dirty 场景，应优先写自动化测试。
```

这样能避免 agent 只补文档，不补测试。

---

# 建议补强 3：Agent restart 测试别依赖真实进程

你已经说不依赖真实 2 秒 timer。建议再补一句：

```text
Agent restart 自动化测试应优先用 fake status service 模拟 NotRunning/Stale -> Running，不要启动真实 Agent 进程，不要依赖真实 Named Pipe server。
```

真实启动/停止 Agent 更适合手动验收或 smoke test，不适合作为常规单元测试。

---

# 建议补强 4：断连恢复后要同步验证三类状态

你现在写了“Diagnostics 和按钮状态同步恢复”。建议再具体：

```text
断连恢复到 Running 后，至少验证三类状态同步恢复：
1. 顶部 AgentStatusText / LastHeartbeat；
2. MainWindow 控制按钮；
3. Settings ReloadConfig / PruneData / ClearHistory 可用性；
4. Diagnostics IPC / RefreshHealth 文案。
```

这样验收不会只看顶部状态。

---

# 建议补强 5：Refreshing flags 必须用 finally 清理

你已有测试：

```text
MainWindowViewModel_StatusFailureClearsRefreshingFlags
```

建议在提示词中加实现要求：

```text
任何 status/page refresh 异常、取消或跳过路径，都必须通过 finally 或等价机制确保 IsStatusRefreshing / IsPageRefreshing 最终恢复 false。
```

这个是阶段 9.6 很关键的稳定性检查。

---

# 建议补强 6：手动验收清单建议标明“待人工执行”

你已经写了“可以先不全部打勾”。建议让文档状态更清楚：

```text
手动验收清单创建后默认结果列填写 Pending / Not Run，不要把未执行项写成 Passed。
```

避免完成说明里误报“手动验收已通过”。

---

# 可直接追加到提示词里的补充段

你可以把下面这段加到“注意”或“测试要求”后面：

```text
补充要求：

1. 如果测试暴露 bug，只允许在当前断连 / 重连 / 状态派发 / RefreshHealth 范围内做最小修复；不得重构 RefreshService、AgentStatusService、AgentControlService 或 SettingsViewModel 的整体架构。
2. 手动验收清单不能替代能自动化覆盖的测试；凡是可用 fake service / fake status / fake IPC 构造的断连、重连、Stale、Maintenance、Settings dirty 场景，应优先写自动化测试。
3. Agent restart 自动化测试应优先用 fake status service 模拟 NotRunning/Stale -> Running，不要启动真实 Agent 进程，不要依赖真实 Named Pipe server。
4. 断连恢复到 Running 后，至少验证顶部状态、MainWindow 控制按钮、Settings ReloadConfig / PruneData / ClearHistory 可用性、Diagnostics IPC / RefreshHealth 文案同步恢复。
5. 任何 status/page refresh 异常、取消或跳过路径，都必须确保 IsStatusRefreshing / IsPageRefreshing 最终恢复 false。
6. 手动验收清单创建后，未实际执行的项目结果列填写 Pending / Not Run，不要把未执行项写成 Passed。
```

---

# 对这份提示词的最终判断

这份阶段 9.6 提示词**可行，可以用于实施**。

我建议你补上三类内容后直接执行：

```text
1. bugfix 只能最小修复，不许大改刷新架构；
2. 手动验收清单不能替代自动化测试；
3. 断连恢复要同时验证顶部状态、按钮状态、Settings 状态和 Diagnostics 状态。
```

其余部分已经足够完整，可以交给 agent。
