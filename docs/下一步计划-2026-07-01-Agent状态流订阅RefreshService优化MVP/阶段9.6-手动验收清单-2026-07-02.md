# 阶段 9.6 手动验收清单

更新时间：2026-07-03

`dotnet build` 0 warnings / 0 errors，`dotnet test` 234/234 passed。

## 手动验收

| 编号 | 场景 | 操作步骤 | 预期结果 | 结果 | 备注 |
|---|---|---|---|---|---|
| 1 | 启动 App + Agent | 1. 启动 App 2. 点击 Start Agent | 状态约 2 秒内更新为 Running，顶部状态栏显示 Running | Passed | 状态更新迅速 |
| 2 | Pause | 1. Agent Running 2. 点击 Pause | 顶部状态快速变为 Paused，Pause 按钮禁用，Resume 可用 | Passed | 功能正确，按钮禁用态 UI 不明显（SecondaryButtonStyle 无 disabled 触发器） |
| 3 | Resume | 1. Agent Paused 2. 点击 Resume | 顶部状态快速变为 Running，Resume 按钮禁用，Pause 可用 | Passed | 同上，UI 不明显 |
| 4 | PruneData | 1. Agent Running 2. Settings → Prune Old Data | Maintenance 中按钮禁用，完成后恢复 Running | Passed | Agent 日志显示 Running→Maintenance→Running；Diagnostics 显示 CommandAccepted→DataPruned→CommandCompleted，requestId 为 ipc-* |
| 5 | ClearHistory | 1. Agent Running 2. Settings → Clear All History 3. 输入 CLEAR 4. Confirm | 输入 CLEAR 后成功，完成后 Paused，Samples/Sessions/Apps 空态 | Passed | CLEAR 确认正常；Diagnostics 显示 HistoryCleared+CommandCompleted，requestId 为 ipc-*；Samples/Sessions/Apps 空态；Agent Paused |
| 6 | Stop Agent | 1. Agent Running 2. 点击 Stop Agent | UI 不崩溃，Diagnostics 显示 IPC unavailable / fallback，Source: FileFallback | Passed | Agent 正常退出（Stopping→Stopped），终端窗口自行关闭；App UI 流畅；Diagnostics 显示 IPC: IPC unavailable; using file fallback, Source: FileFallback, Refresh loop: Healthy |
| 7 | Agent 停止但状态文件残留 Running | 1. Agent Running 2. 先关 App 3. 再关 Agent 4. 重新启动 App | Start Agent 仍可用（不会因残留 Stale 文件被禁用） | Passed | 重启后状态栏显示 Stale，点击 Start Agent 可正常启动 |
| 8 | 重启 Agent 后 IPC 恢复 | 1. Agent Stopped 2. 点击 Start Agent | IPC 状态从 FileFallback 恢复为 NamedPipe，Diagnostics 显示 IPC connected | Passed | 停止后 Diagnostics 显示 FileFallback；重启后显示 IPC connected, Source: NamedPipe |
| 9 | Settings 编辑未保存时自动刷新 | 1. Settings 编辑 Agent Options 字段 2. 不保存 3. 等待自动刷新 | 编辑内容不被覆盖，IsDirty 保持 true，按钮可用性仍随 Agent 状态更新 | Passed | 编辑内容不被自动刷新覆盖；IsDirty 内部标记不方便手动观察，自动化测试已验证 |
| 10 | 慢页面刷新不拖慢顶部状态 | 1. 切到 Sessions 页 2. 观察顶部 AgentStatusText | 顶部 Agent 状态约 2 秒内独立更新，不被页面数据刷新拖慢 | Passed | 切到 Sessions 后 Pause/Resume，状态栏更新迅速 |

## 自动化覆盖（本阶段已确认）

| 场景 | 测试 |
|---|---|
| Stale + IsRunning=false → Start 可用 | `AgentCommandAvailability_AllowsStartWhenStaleButProcessGone` |
| Stale + IsRunning=true → Start 不可用，Stop 可用 | `AgentCommandAvailability_HandlesStaleConservatively` |
| Maintenance 禁用所有命令 | `AgentCommandAvailability_DisablesDuringMaintenance` |
| NotRunning 禁用清理 | `AgentCommandAvailability_DisablesCleanupWhenNotRunning` |
| Settings dirty 不被覆盖 | `SettingsViewModel_StatusUpdateDoesNotOverwriteDirtyEditors` |
| ClearHistory 确认面板 Maintenance 拒绝 | `SettingsViewModel_ClearHistoryConfirmDisabledWhenStatusChangesToMaintenance` |
| status 失败不残留 Refreshing | `MainWindowViewModel_StatusFailureClearsRefreshingFlags` |
| latest-wins 不覆盖新结果 | `MainWindowViewModel_LatestWinsDoesNotApplyOlderSequenceOverNewer` |
| page error 不阻塞 status poll | `MainWindowViewModel_PageErrorDoesNotBlockStatusPoll` |
| Stale → Running 恢复 | `MainWindowViewModel_StatusPollRecoversFromStaleToRunning` |
| Diagnostics 不泄露 SID/FullPipeName/路径 | `MainWindowViewModel_DoesNotExposeFullPipeNameOrSid` |
