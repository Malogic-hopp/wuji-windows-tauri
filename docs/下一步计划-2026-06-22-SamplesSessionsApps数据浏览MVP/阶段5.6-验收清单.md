# 阶段 5.6 验收清单

更新时间：2026-06-22

## 自动化验收

- [x] `dotnet build` 通过，0 warning / 0 error。
- [x] `dotnet test` 通过，55/55。
- [x] Sample 查询按 `sample_time_utc DESC, id DESC` 稳定排序。
- [x] Sample 查询有 LIMIT。
- [x] Session 查询按 `started_at_utc DESC, id DESC` 稳定排序。
- [x] Session 查询有 LIMIT。
- [x] Today Sessions 使用本地日重叠查询。
- [x] Last 24 Hours Sessions 使用显式 UTC 范围重叠查询。
- [x] AppUsage 查询按 `active_duration_seconds DESC` 排序。
- [x] AppUsage active 相同时按 total duration 降序。
- [x] AppUsage total 相同时按 process name 升序。
- [x] AppUsage 查询有 LIMIT。
- [x] AppUsage 查询返回 `last_used_utc`。
- [x] 缺库 / 缺表时数据浏览查询返回空列表。
- [x] SamplesViewModel 能加载最近 samples。
- [x] SamplesViewModel 支持 ActivityState 筛选。
- [x] SamplesViewModel 空态和错误态区分。
- [x] SamplesViewModel 加载失败文本脱敏。
- [x] SessionsViewModel 能加载 sessions。
- [x] SessionsViewModel 支持 close reason 筛选。
- [x] SessionsViewModel 未知 close reason 归入 Other。
- [x] SessionsViewModel open session 显示 `正在进行`。
- [x] SessionsViewModel 范围快速切换不会被旧查询结果覆盖。
- [x] SessionsViewModel 加载失败文本脱敏。
- [x] AppsViewModel 能加载今日应用排行。
- [x] AppsViewModel rank 正确。
- [x] AppsViewModel display name fallback 正确。
- [x] AppsViewModel 空态和错误态区分。
- [x] AppsViewModel 加载失败文本脱敏。
- [x] 导航顺序为 `Dashboard / Apps / Sessions / Samples / Diagnostics / Settings`。
- [x] `LastSelectedPage = Apps / Sessions / Samples / Diagnostics / Settings` 映射正确。
- [x] 全局 Refresh 只刷新当前页面数据。
- [x] Dashboard Top Apps 复用 AppsView Today 查询口径。
- [x] Dashboard Top Apps 与 AppsView Today 在跨日 session / open session 场景下顺序一致，duration 仅允许 1 秒级差异。

## 静态检查

- [x] `SampleQueryService` 使用 `SqliteConnectionFactory.OpenReadOnlyAsync`。
- [x] `SessionQueryService` 使用 `SqliteConnectionFactory.OpenReadOnlyAsync`。
- [x] `AppUsageQueryService` 使用 `SqliteConnectionFactory.OpenReadOnlyAsync`。
- [x] `SqliteConnectionFactory.OpenReadOnlyAsync` 使用 `Mode=ReadOnly` 且 `Pooling=false`。
- [x] SamplesView 不展示完整 `ExecutablePath`。
- [x] SessionsView 不展示 `WindowTitle`。
- [x] AppsView 不展示窗口标题或路径。
- [x] Samples / Sessions / Apps 都拆成独立 UserControl。
- [x] Samples / Sessions / Apps 都有独立 ViewModel。

## 手动验收

- [x] Samples tab 能显示真实数据。
- [x] Samples tab ActivityState 筛选可用。
- [x] Sessions tab 能显示真实数据。
- [x] Sessions tab 能看到 `ProcessChanged`。
- [x] Sessions tab 能看到 `Paused`。
- [x] Sessions tab 能看到 `Stopped`。
- [x] Apps tab 能显示真实今日排行。
- [x] Apps tab 排序与 Active duration 口径一致。
- [x] Dashboard 仍可显示状态、今日统计、Top Apps、Recent Sessions。
- [x] Diagnostics 仍可显示 runtime / health / events / errors。
- [x] Diagnostics 页面刷新和滚动可用。
- [x] Start / Pause / Resume / Stop 控制链路可用。
- [x] JSONL 当前路径可定位当天日志文件。
- [x] 打开日志目录按钮可用。
- [x] 命令失败时可看到 `CommandFailed` 和 `errorCode`。
- [x] 控制文件 malformed JSON 时可看到 `CommandInvalidJson`，原始 JSON 不进入事件。
- [x] 标题隐私规则命中时，Diagnostics 只显示泛化原因，不显示真实标题。
- [x] 进程隐私规则命中时，不写 foreground sample，事件有节流。

## 本阶段未重新执行但已有前序依据

- [x] 30 分钟正常运行后，`agent_events` 数量显著小于 `foreground_samples`。
- [x] 30 分钟正常运行后，未出现 `SampleCaptured` / `Heartbeat` 高频事件。
- [x] 30 分钟正常运行后，Diagnostics 页面仍能快速刷新和滚动。

## 明确未做

- [ ] 7 天趋势。
- [ ] 图表。
- [ ] 导出。
- [ ] Settings 编辑能力。
- [ ] PruneData / ClearHistory UI。
- [ ] Named Pipe / gRPC IPC。
- [ ] 托盘。
- [ ] 开机自启。
- [ ] 安装包。
- [ ] 应用分类。
- [ ] 浏览器网页级识别。

## 收口结论

Samples / Sessions / Apps 数据浏览 MVP 满足当前计划验收条件，可以收口。
