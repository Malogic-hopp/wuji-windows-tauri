# 阶段 4.5 复审修复提示词（2026-07-27）

请在仓库 `C:\Users\WangZhenchi\work\WUJI\Win-ui-tauri` 中修复阶段 4.5 本轮复审发现的问题。
本轮复审结论：**暂不通过，停在 4.5**。本提示词只覆盖复审阻断项与次要项的修复，
不得开始 4.6。

## 一、开始前必须阅读并核对

按顺序完整阅读：

1. 根目录 `AGENTS.md`；
2. 本目录 `README.md`；
3. `05-阶段4.5-LockSleep状态叠加与事件接线.md`；
4. `阶段4.5-启动准备-2026-07-27.md`（冻结的失败表 §4 与测试矩阵 §7 是验收准绳）；
5. `阶段4.5-完成说明-2026-07-27.md`（注意：其中 §6.1/§11 已落后于当前代码，以实际
   代码为准）；
6. 本文件 §二 的复审发现清单。

随后逐文件核对当前实现（不得只相信完成说明），并记录：

```powershell
git status --short
@(Get-Process -Name 'wuji-rebuild-agent-v01' -ErrorAction SilentlyContinue).Count
cd rebuild
cargo fmt --all -- --check
cargo check --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
```

硬性约束：保留全部既有未提交修改；禁止 `git checkout/restore/reset/clean`；不
commit、不 push；不读写旧 WUJI/WUJI-Dev 数据库；运行时测试只用唯一随机
`rebuild-v01-test-*` channel；不得回退 4.2–4.4 的可靠 Barrier、Settings
effectivity、unknown fencing、pipeline supervisor、RequestIdCache 语义；不得通过
删除测试、放宽断言、随机 sleep、扩大 timeout 或清队列换取绿色。

## 二、本轮复审发现（逐条修复，不得遗漏）

### B1：L01–L20 冻结矩阵缺 3 项、2 项错位/不完整

当前 `rebuild/apps/agent/tests/lock_sleep_lifecycle.rs` 共 18 项测试。必须补齐：

1. **新增 L11（Writer 明确失败）**：真实 Coordinator + Writer + 临时 SQLite 拓扑
   （可复用本文件 Wiring fixture 或 `barrier_reliability.rs` 的
   `BEGIN IMMEDIATE` 写锁注入模式）。证明：Lock 边界提交被 Writer 明确拒绝后
   ——DB 无 `session_locked` gap、`lock.active`/首次时间保留、Writer 精确诊断
   保留、采集不恢复 Running；修复故障后重复 Lock 事件用**首次事件时间**重试并
   **恰好提交一次**（gap start 等于首次事件时刻，不是重试时刻）。
2. **新增 L12（Writer ack unknown）**：control 已被 Writer lane 接受后丢弃 ack
   （或 ack 超时），`apply_system_lifecycle_event(Lock)` 必须进入 4.3.1 的
   unknown-outcome fencing：返回 `AGENT_WRITER_FAULTED`、锁存 writer_fault、
   强制 Stopped；随后 `capture_start`、`apply_settings`、
   `apply_system_lifecycle_event` 全部在副作用前被拒绝。
3. **新增 L14（Lock/Sleep × Settings/Pause 真实并发）**：参照
   `concurrent_pause_and_settings_are_strictly_serialized` 的判定方式——第一条
   transition 双 ack 未齐前第二条 `BarrierRequest` 不得到达（100ms timeout 断言
   为空），两条路径各自恰好一次、终态一致。必须走新的四态入口
   （`apply_system_lifecycle_event`），不得只测旧 `apply_system_event`。
4. **修正 L10（当前错位）**：现有 `l10_control_send_failure_keeps_suppression_retry_succeeds`
   （`lock_sleep_lifecycle.rs:682-705`）实际在 **injected ack 超时**处失败
   （`_barrier_rx` 被持有但无人消费），从未到达 `send_control`——控制面发送失败
   路径目前无测试。重写为：容量 1 的 control lane 先用测试持有的 sender 克隆
   预填占满 + 假 Capture Loop 消费 barrier 请求并 ack Ok（注入必须成功），
   使 Coordinator 的 `send_control` 真实超时（2s，paused 自动推进）；断言未提交、
   suppression 与首次时间保留、LifecyclePump 诊断；随后排空 control lane 并重试
   ——测试直接消费 `WriterControl::Lifecycle` 并断言其 `EngineEvent` 的
   `at_utc_ms` **等于首次事件时间**，ack Ok 后提交恰好一次。
5. **修正 L13（当前测错失败行）**：现有 `l13_release_publish_failure_stops_safely`
   （`:707-731`）覆盖的是失败表"Lock 安全冻结发布失败"行，不是矩阵 L13 的
   "Unlock/Resume 最终 publish 失败"行。保留现内容（可改名为
   `enter_freeze_publish_failure_*` 作为冻结行用例），新增真正的 L13：先成功
   Lock（假 Capture/Writer ack），再 drop capture watch 接收端使 release 的
   publish 失败；断言不虚假 Running、watch/shared/DTO 一致安全停止、suppression
   状态与诊断明确。

### B2：L18 仍然假绿

1. **删除**旧 `l18_bridge_backpressure_preserves_order_no_silent_drop`
   （`:567-609`，只断言 `try_send` 满容报错，不证明任何生产语义）。
2. **重写** `l18_production_bridge_preserves_order_under_backpressure`
   （`:787-827`）。当前版本未使用生产 bridge（`start_session_power_bridge` 中
   std 线程的 `bridge_tx.blocking_send`，`session_power_events.rs:126-134`），
   且 capture watch 接收端在构造时即被 drop——四个事件全部在 publish 处静默失败，
   测试零结果断言。要求：
   - 把生产 bridge 线程的转发体抽成生产与测试共用的单一 helper（如
     `run_session_power_forward(events_rx, bridge_tx)`），生产路径改调它；
   - 测试用容量 1 的 tokio channel 制造满载，证明 `blocking_send` 保序等待；
     释放容量后 Lock/Sleep/Unlock/Resume **全部按序到达**；
   - "按序到达"必须有可观察证据：用 capture_rx 存活 + 假 Capture/Writer ack 的
     Coordinator harness，断言 control lane 上 Lifecycle control 的顺序
     （Lock 先于 Sleep）与最终 suppression 全清、无诊断；或等价地用一个记录型
     consumer 包装。不允许只断言"consumer 在 N 秒内结束"。

### B3：完成说明与代码矛盾，必须重写

`阶段4.5-完成说明-2026-07-27.md` 保留旧轮内容作审计历史，新增本轮章节，至少包含：

- 本轮复审发现 → 修复 → 确定性证据的映射表（含 B1/B2/次要项全部条目）；
- 最新 L01–L20 测试映射（每项编号 ↔ 测试名 ↔ 证明内容）；
- 监督链与正常 shutdown 的最新描述（取代 §11.2 的过时描述："先信号、再有界等待、
  超时兜底 abort、join bridge/pump"）；
- 首次失败记录（不得删除）与最终验证原始计数；
- `git status --short`、测试前后 Agent 残留、明确未做事项与遗留风险；
- 明确标注：真实 Lock/Sleep 人工验收、package、8h soak 仍为 Pending/NotRun。

### B4：格式化门禁

`cargo fmt --all -- --check` 当前在 `session_power_events.rs:98`、
`session_power_events.rs:179`（tests 模块空白行）、`main.rs:253` 有 3 处 diff。
`cargo fmt --all` 后复查通过。

## 三、次要项（同轮处理）

1. `SessionPowerPumpHandle::join()`（`crates/wuji-windows/src/session_power.rs:44-63`）：
   不得在 current-thread runtime 的 shutdown 路径上用 `std::thread::sleep` 轮询。
   改为 pump 线程退出时发送异步完成信号（如 watch/oneshot），`SessionPowerBridge::
   shutdown` 用 `tokio::time::timeout` 等待信号、确认 finished 后立即 join（此时
   join 不再阻塞）；超时路径不得丢弃原 `JoinHandle`（保留清理责任或显式记录）。
   更新 `main.rs:290` 的过时注释。
2. 旧公开入口 `apply_system_event(EngineEvent)`（`capture_coordinator.rs:619`）
   绕过 Lock/Sleep suppression。迁移全部 9 处测试调用（capture_coordinator ×6、
   settings_effectivity ×2、production_wiring ×1）到四态入口后**删除该入口**；
   production_wiring 是生产路径测试，必须改用生产实际使用的
   `apply_system_lifecycle_event`。注意语义差异：四态 Sleep 后 suppression 保持
   （effective Paused 而非 Running），相应断言按冻结模型调整，不得放宽。
   settings_effectivity 的 fencing 用例可改用 `SystemLifecycleEvent::Lock`
   （writer_fault 检查在四态入口最前面，错误码不变）。
3. 补 consumer 异常检测测试：bridge channel 在非 shutdown 状态意外关闭 →
   `latch_monitor_fault` 生效（`session_power_events.rs:37-44`）；consumer panic →
   supervisor 锁存（可注入会 panic 的 consumer future）。pump 启动失败传播测试目前
   仅 `#[cfg(not(windows))]`，Windows 上永假——改为可注入失败源或在 Windows 上
   真实构造失败（如占用窗口类名），否则在完成说明如实标注未覆盖。
4. 删除 `l01` 中 `|| true` 恒真 rendezvous（`lock_sleep_lifecycle.rs:294-298`），
   改为对采样窗口的显式断言（推进一个采样周期后直接断言行数不变）。
5. L04 补"release 不改写最后 open gap kind"断言（Sleep→Lock 后 system_sleep gap
   已按 09 §6.7 关闭、session_locked 为唯一 open gap；Resume/Unlock 不改变 kind）。
6. 清理 `Suppressions.lock/sleep` 上不再需要的 `#[allow(dead_code)]`
   （`capture_coordinator.rs:190-194`）。

## 四、验证与交付

修复后至少实跑（记录原始计数）：

```powershell
cd rebuild
cargo fmt --all -- --check
cargo check --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
cargo test -p wuji-windows
cargo test -p wuji-rebuild-agent --lib
cargo test -p wuji-rebuild-agent --test lock_sleep_lifecycle
cargo test -p wuji-rebuild-agent --test capture_coordinator
cargo test -p wuji-rebuild-agent --test production_wiring
cargo test -p wuji-rebuild-agent --test barrier_reliability
cargo test -p wuji-rebuild-agent --test settings_effectivity
cargo test -p wuji-rebuild-agent --test agent_guard
cargo test -p wuji-rebuild-agent --test agent_e2e -- --test-threads=1
cargo test --workspace
git diff --check
```

测试前后确认 `wuji-rebuild-agent-v01` 残留为 0。真实进程/Named Pipe 测试如因
执行环境权限失败，如实记录并在获准后于沙箱外复跑。

## 五、停止条件

出现以下任一情况必须停下并报告，不得降低语义换绿：

- 需要破坏性 Git 命令或覆盖既有未提交修改；
- 需要绕过唯一 Coordinator/Barrier/Writer ack；
- 只能通过丢事件、清队列、随机 sleep 或扩大 timeout 让 L10/L18 等通过；
- 无法区分明确未提交与 unknown outcome；
- 发现 4.2–4.4 基线被破坏。

完成后停在 4.5 等待复审：不开始 4.6，不运行 package/8h soak，不执行真实
Lock/Sleep，不宣称 S2-03 或 v0.1 最终验收通过，不 commit、不 push。
