# 阶段 4.7：Soak 判据、文档同步与收口

## 阶段目标

关闭 P2-03、P2-04，并对 4.1–4.6 做自动化收口。此阶段修复验收脚本和文档，
但不执行正式 package 或 8 小时 soak。

## Soak 脚本要求

显式记录并纳入 verdict：

- shutdown_attempted；
- hello_ok；
- shutdown_ok；
- willExit=true；
- grace time 内 exit code 0；
- forced_kill=false。

任一条件失败必须进入 failures。Agent 在正式 shutdown 前自行 exit 0 也不得通过。
将 verdict 判定提取为纯函数并测试成功、IPC 失败、未执行、超时和强杀场景。

## 文档同步

1. 09 §16.1 改为 DB metadata + 双槽完整内容，不再写 `content_json`。
2. 09 §16.2 改为 BarrierId + injected ack + Coordinator，不再写 watermark。
3. migration-status 使用当前真实实现、日期、测试数和状态。
4. package、8h soak、真实 Lock/Sleep、UI 人工门禁保持 NotRun/Pending。
5. 历史 evidence 保留，但明确不覆盖当前代码。
6. 新增第四阶段总整改回应，逐项映射 P1/P2 到代码与测试。

## 最终自动门禁

- fmt/check/clippy；
- Agent 所有专项测试；
- Agent E2E 串行；
- workspace 连续两次完整测试；
- React typecheck/lint/test/build；
- `git diff --check`；
- 文档链接与状态一致性检查。

## 验收标准

- soak 假阳性路径有测试；
- 09、migration-status、Schema、代码术语一致；
- 第四阶段整改回应完整；
- 自动门禁稳定通过；
- 正式 package/8h soak 明确 NotRun；
- 结论只写“第四阶段整改完成，等待复审”。

## 明确不做

- 不运行正式 package；
- 不运行 8 小时 soak；
- 不执行人工门禁；
- 不 commit、不 push；
- 不宣称 S2-03/S2-04 最终关闭。

