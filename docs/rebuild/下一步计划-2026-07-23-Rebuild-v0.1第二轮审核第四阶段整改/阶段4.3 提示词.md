请在仓库 `C:\Users\WangZhenchi\work\WUJI\Win-ui-tauri` 中实施阶段 4.3。

先完整阅读根目录 `AGENTS.md`、本目录 `README.md`、第四阶段审核报告、
`03-阶段4.3-统一CaptureCoordinator与生产接线.md`，以及唯一一份
`阶段4.2-完成说明-*.md`。前置阶段未通过或实际代码不一致时停止。

任务目标：让唯一 CaptureCoordinator 成为 Lifecycle、settings_reload、
reconciler 和系统事件的生产控制入口，关闭 P1-02 和 P1-03 的控制闭环。

必须完成：

1. `main.rs` 创建唯一 `Arc<CaptureCoordinator>`。
2. CommandServer/reconciler/session-power 不再直接构造 Barrier 或 WriterControl。
3. 删除分散的 `capture_lock`、重复通道字段和 settings_reload 双路径。
4. Coordinator 使用唯一 transition lock。
5. 建模 desired capture state、effective gate、transition/fault suppression 和 applied revision。
6. 每条路径等待 injected ack 与 Writer ack。
7. channel/task/ack 失败后 shared/watch/DTO 一致并保持安全冻结。
8. IPC timeout 不取消已接受副作用，同 request ID 返回原任务结果。
9. 增加生产接线测试，证明不存在旁路。

本阶段只搭好 Settings transition 骨架；完整 effectivity 在 4.4，Lock/Sleep
suppression 叠加在 4.5。不要提前把它们标成完成。

完成后运行：

```powershell
cd rebuild
cargo fmt --all -- --check
cargo check --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
cargo test -p wuji-rebuild-agent --lib
cargo test -p wuji-rebuild-agent --test ipc_protocol
cargo test -p wuji-rebuild-agent --test writer_watermark
cargo test -p wuji-rebuild-agent --test settings_lifecycle
git diff --check
```

新增 `阶段4.3-完成说明-YYYY-MM-DD.md`，附生产拓扑、状态字段所有权、失败时序、
测试证据、未做事项和 `git status --short`。不要开始 4.4，不 commit/push。

