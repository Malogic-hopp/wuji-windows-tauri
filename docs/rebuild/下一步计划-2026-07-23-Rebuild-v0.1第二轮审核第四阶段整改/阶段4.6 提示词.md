请在仓库 `C:\Users\WangZhenchi\work\WUJI\Win-ui-tauri` 中实施阶段 4.6。

先完整阅读根目录 `AGENTS.md`、本目录 `README.md`、第四阶段审核报告、
`06-阶段4.6-真实拓扑测试与E2E稳定化.md`，以及唯一一份
`阶段4.5-完成说明-*.md`。如果 4.1–4.5 的代码不符合完成说明，先报告，不用测试掩盖。

任务目标：关闭 P2-01/P2-02，用真实生产拓扑验证全部不变量，并消除父进程 E2E flaky。

建立真实 harness：

```text
Coordinator → BarrierRequest → Capture Loop → FIFO → Processor
→ Writer data/control → SQLite/SharedState
```

直接注入 Writer 的测试只能保留为单元测试，不能作为端到端证据。

至少覆盖阶段文档列出的 backlog、同时 ready、先 Barrier、Full/Closed、任务退出、
两级 drop、Settings 规则、Lock/Sleep 叠加、pending 和失败一致性场景。

修复 `agent_survives_parent_exit_and_offline_read_works_after_kill`：

- 使用确定性 PID/句柄/握手；
- 不使用全局映像名猜测；
- 只清理当前 `rebuild-v01-test-*` channel；
- 不扩大 timeout 或加入随机 sleep。

完成后运行：

```powershell
cd rebuild
cargo fmt --all -- --check
cargo check --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
cargo test -p wuji-rebuild-agent --test agent_e2e -- --test-threads=1
cargo test --workspace
cargo test --workspace
git diff --check
```

如沙箱阻断 `%LOCALAPPDATA%`/Named Pipe，确认唯一测试 channel 后申请权限重跑。
必须保留首次失败与最终结果，完整 workspace 连续两次通过且不得 skip。

新增 `阶段4.6-完成说明-YYYY-MM-DD.md`，列出真实拓扑覆盖矩阵、E2E 稳定化方法、
两次全量结果和遗留风险。不要运行 package/8h soak，不开始 4.7。

