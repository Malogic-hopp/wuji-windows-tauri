请在仓库 `C:\Users\WangZhenchi\work\WUJI\Win-ui-tauri` 中实施阶段 4.2。

先完整阅读：

1. 根目录 `AGENTS.md`
2. 本目录 `README.md`
3. `00-第四阶段代码审核报告.md`
4. `02-阶段4.2-可靠Barrier基础设施与WriterPending.md`
5. 唯一一份 `阶段4.1-完成说明-*.md`

如果 4.1 完成说明不存在、结论不是建议进入 4.2，或代码与完成说明不一致，停止实施并报告，不要自行跨过前置门禁。

任务目标：只建立可靠 BarrierRequest、injected ack 与 Writer pending，关闭 P1-03
的底层问题；保持每次修改后仓库可编译。

必须完成：

1. `BarrierRequest` 携带 token 和 injected ack。
2. Capture Loop 只在 Barrier 成功写入 Sample FIFO 后确认。
3. request/FIFO Closed、任务退出均返回稳定失败。
4. 删除 Barrier `try_send` 静默忽略和 settings_reload 双 Barrier。
5. Writer 普通 data 分支保存 Barrier，不得跳过。
6. pending 按 ID 索引，具备容量、TTL、重复/冲突、overflow、shutdown 规则。
7. 按 ID + kind + expected revision 匹配。
8. 注入失败与 WriterControl 之间没有悬挂等待。

本阶段允许为旧生产调用方做最小兼容适配，但不要提前统一 CommandServer、
reconciler 和 Lock/Sleep；统一接线属于 4.3。

必须新增确定性测试：

- Barrier 先于 control；
- 同 kind 不同 ID；
- 同 ID 冲突；
- request/FIFO closed；
- pending TTL/overflow/重复；
- Processor/Writer 退出；
- queue 满时 Barrier 可达；
- timeout 不提交边界。

不要使用随机 sleep 或扩大 timeout 掩盖竞态。

完成后运行：

```powershell
cd rebuild
cargo fmt --all -- --check
cargo check --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
cargo test -p wuji-rebuild-agent --lib
cargo test -p wuji-rebuild-agent --test writer_watermark
cargo test -p wuji-rebuild-agent --test settings_lifecycle
git diff --check
```

新增 `阶段4.2-完成说明-YYYY-MM-DD.md`，记录协议时序、pending 规则、失败闭环、
测试结果和未做事项。只写“等待复审”，不要开始 4.3，不 commit/push，不运行 soak。

