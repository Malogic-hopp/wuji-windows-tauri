请在仓库 `C:\Users\WangZhenchi\work\WUJI\Win-ui-tauri` 中实施阶段 4.7。

先完整阅读根目录 `AGENTS.md`、本目录 `README.md`、第四阶段审核报告、
`07-阶段4.7-Soak判据文档同步与收口.md` 和唯一一份
`阶段4.6-完成说明-*.md`。4.6 未连续两次完整全绿时停止。

任务目标：关闭 P2-03/P2-04，修复 soak verdict 假阳性，同步权威文档并完成自动收口。

必须完成：

1. soak 显式记录 shutdown_attempted、hello_ok、shutdown_ok、willExit、exit code、forced kill。
2. 任一受控退出条件失败都必须使 verdict=fail；提前 exit 0 也不能通过。
3. 把 verdict 判定提取成可测试纯函数，覆盖 IPC 失败、未执行、timeout、强杀和成功。
4. 更新 09：
   - DB metadata + 双槽完整内容；
   - BarrierId + injected ack + Coordinator；
   - 删除 content_json/watermark 过时合同。
5. 更新 migration-status 为当前真实实现、日期、测试结果和状态。
6. 历史 evidence 保留但标明不覆盖当前代码。
7. 新增 `Rebuild-v0.1-第二轮审核第四阶段整改回应.md`，逐项映射 P1/P2。
8. package、8h soak、真实 Lock/Sleep 和 UI 人工门禁保持 NotRun/Pending。

最终运行：

```powershell
cd rebuild
cargo fmt --all -- --check
cargo check --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
cargo test -p wuji-rebuild-agent --test agent_e2e -- --test-threads=1
cargo test --workspace
cargo test --workspace

cd apps\desktop
pnpm.cmd typecheck
pnpm.cmd lint
pnpm.cmd test
pnpm.cmd build

cd ..\..\..
git diff --check
git status --short
```

本阶段不要运行正式 package 或 8 小时 soak，不执行人工门禁，不 commit/push。

新增 `阶段4.7-完成说明-YYYY-MM-DD.md`，并在总整改回应中记录首次/最终测试、
NotRun/Pending 门禁和工作区状态。结论只能写“第四阶段整改完成，等待复审”，
不得宣称 S2-03/S2-04 最终关闭或 Rebuild v0.1 验收完成。

