很好，这一步进展很关键。你现在已经把最难的“控制闭环”先跑通了：

```text
WPF App
    启动 / 停止 / 暂停 / 恢复 Agent

Agent
    状态机
    heartbeat
    runtime_state / health_state
    agent_control fallback

WPF UI
    状态显示
    控制按钮
    打开数据目录
```

这说明你的重构路线是对的。现在不要急着做 IPC，也不要急着美化 UI。下一步我建议走这个方向：

```text
先做 SQLite + 最小统计链路
再接真实 Win32 前台窗口采样
```

原因是：**先把数据落库和统计口径跑通，再把数据源从 Mock 换成真实 Win32，会更稳。**

---

# 我建议下一步做什么？

推荐下一步做：

```text
SQLite / statistics / dashboard 最小数据链路
```

但不要一下子做完整统计页，而是做一个最小闭环：

```text
Agent 生成 Mock foreground sample
    ↓
写入 SQLite foreground_samples
    ↓
SessionAggregator 合并 app_sessions
    ↓
WPF Dashboard 读取今日 summary
    ↓
主窗口显示今日总时长、active 时长、最近会话
```

也就是说，下一阶段目标不是“真实采样”，而是：

```text
从 Agent 到 SQLite 到 WPF 统计展示的主数据链路
```

这个链路跑通后，再把 Mock sample 替换成真实 Win32 sample。

---

# 为什么不建议现在直接做 Win32 采样？

真实 Win32 采样会马上引入很多细节：

```text
GetForegroundWindow
GetWindowText
GetWindowThreadProcessId
Process.MainModule
权限不足
窗口标题脱敏
进程路径失败
UWP / Store app
管理员进程
空窗口句柄
锁屏 / 桌面切换
```

这些都很容易把你拖进底层细节。

而你现在更需要先验证：

```text
SQLite 表能初始化
Agent 能写入数据
WPF 能只读查询
app_sessions 能统计
Dashboard 能展示
```

这个更符合你方案里的核心主线：**Agent 是唯一写入者，WPF 只读查询，统计优先基于 app_sessions。**

---

# 下一阶段推荐目标

## 阶段目标：MVP 数据闭环

这一阶段完成后，你应该能看到：

```text
1. Agent 启动后，每隔 3 秒生成一个 Mock sample
2. Agent 写入 foreground_samples
3. Agent 合并生成 app_sessions
4. WPF 显示：
   - 今日总使用时长
   - 今日 active 时长
   - 今日 idle 时长
   - 最近 5 条 session
   - 应用排行 Top 5
5. 暂停时不再写 sample
6. 恢复后继续写 sample
7. 停止时关闭当前 session
```

这就是真正的 MVP 主链路。

---

# 具体开发顺序

## 第 0 步：先做编译验证

你说当前 shell 没法重新跑 build，这一步最好先补上。

在 PowerShell 里执行：

```powershell
cd C:\Users\WangZhenchi\work\WUJI\Win
dotnet build QuantifiedSelf.Windows.sln --no-restore
```

如果有错误，先修到：

```text
0 errors
```

不要在没编译验证的状态下继续堆新功能。

---

# 第 1 步：定义数据模型

放在 `Core/Models`。

建议先建这些：

```text
src/QuantifiedSelf.Windows.Core/Models/ForegroundSample.cs
src/QuantifiedSelf.Windows.Core/Models/AppSession.cs
src/QuantifiedSelf.Windows.Core/Models/AppUsageSummary.cs
src/QuantifiedSelf.Windows.Core/Models/DashboardSummary.cs
```

第一版字段不要太多。

## ForegroundSample

```csharp
namespace QuantifiedSelf.Windows.Core.Models;

public sealed class ForegroundSample
{
    public long Id { get; set; }

    public DateTime SampleTimeUtc { get; set; }

    public string ProcessName { get; set; } = string.Empty;

    public string? WindowTitle { get; set; }

    public string? ExecutablePath { get; set; }

    public int IdleSeconds { get; set; }

    public string ActivityState { get; set; } = "Active";
}
```

## AppSession

```csharp
namespace QuantifiedSelf.Windows.Core.Models;

public sealed class AppSession
{
    public long Id { get; set; }

    public DateTime StartedAtUtc { get; set; }

    public DateTime? EndedAtUtc { get; set; }

    public string ProcessName { get; set; } = string.Empty;

    public string? WindowTitle { get; set; }

    public int TotalDurationSeconds { get; set; }

    public int ActiveDurationSeconds { get; set; }

    public int IdleDurationSeconds { get; set; }

    public int UnknownDurationSeconds { get; set; }

    public string CloseReason { get; set; } = "Open";
}
```

---

# 第 2 步：创建 SQLite 初始化器

放在：

```text
src/QuantifiedSelf.Windows.Infrastructure/Database/SqliteDatabaseInitializer.cs
```

职责：

```text
1. 创建 data 目录
2. 打开 SQLite
3. 开启 WAL
4. 创建 foreground_samples
5. 创建 app_sessions
6. 创建 health_state / agent_events 后面再补
```

第一版 SQL：

```sql
CREATE TABLE IF NOT EXISTS foreground_samples (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    sample_time_utc TEXT NOT NULL,
    process_name TEXT NOT NULL,
    window_title TEXT,
    executable_path TEXT,
    idle_seconds INTEGER NOT NULL,
    activity_state TEXT NOT NULL
);

CREATE TABLE IF NOT EXISTS app_sessions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    started_at_utc TEXT NOT NULL,
    ended_at_utc TEXT,
    process_name TEXT NOT NULL,
    window_title TEXT,
    total_duration_seconds INTEGER NOT NULL DEFAULT 0,
    active_duration_seconds INTEGER NOT NULL DEFAULT 0,
    idle_duration_seconds INTEGER NOT NULL DEFAULT 0,
    unknown_duration_seconds INTEGER NOT NULL DEFAULT 0,
    close_reason TEXT NOT NULL DEFAULT 'Open'
);

CREATE INDEX IF NOT EXISTS idx_foreground_samples_time
ON foreground_samples(sample_time_utc);

CREATE INDEX IF NOT EXISTS idx_app_sessions_started
ON app_sessions(started_at_utc);

CREATE INDEX IF NOT EXISTS idx_app_sessions_process
ON app_sessions(process_name);
```

---

# 第 3 步：创建 Repository

放在：

```text
src/QuantifiedSelf.Windows.Infrastructure/Database/ForegroundSampleRepository.cs
src/QuantifiedSelf.Windows.Infrastructure/Database/AppSessionRepository.cs
src/QuantifiedSelf.Windows.Infrastructure/Database/OverviewQueryService.cs
```

第一版先实现这些方法：

```text
ForegroundSampleRepository
    InsertAsync(ForegroundSample sample)

AppSessionRepository
    GetOpenSessionAsync()
    StartSessionAsync(ForegroundSample sample)
    ExtendOpenSessionAsync(ForegroundSample sample, int deltaSeconds)
    CloseOpenSessionAsync(string closeReason)

OverviewQueryService
    GetTodaySummaryAsync()
    GetTopAppsTodayAsync(limit: 5)
    GetRecentSessionsAsync(limit: 5)
```

先别抽象太复杂。能跑通比设计完美更重要。

---

# 第 4 步：Mock 采样器

在 Agent 里先新增：

```text
src/QuantifiedSelf.Windows.Agent/Services/MockForegroundSampleProvider.cs
```

它每次返回一个模拟前台应用，比如在几个进程里轮换：

```text
Code
chrome
devenv
explorer
```

这样你能测试：

```text
进程相同时扩展 session
进程变化时关闭旧 session，开启新 session
```

第一版可以这样：

```csharp
public sealed class MockForegroundSampleProvider
{
    private readonly string[] _processes = ["Code", "chrome", "devenv", "explorer"];
    private int _index;

    public ForegroundSample Capture()
    {
        var process = _processes[_index / 5 % _processes.Length];
        _index++;

        return new ForegroundSample
        {
            SampleTimeUtc = DateTime.UtcNow,
            ProcessName = process,
            WindowTitle = $"{process} - Mock Window",
            ExecutablePath = null,
            IdleSeconds = 0,
            ActivityState = "Active"
        };
    }
}
```

这一步非常有价值，因为你不用碰 Win32，也能把整个数据链路跑通。

---

# 第 5 步：SessionAggregator

在 Agent 里新增：

```text
src/QuantifiedSelf.Windows.Agent/Services/SessionAggregator.cs
```

第一版规则：

```text
1. 没有 open session：
      开启新 session

2. open session.process_name == sample.process_name：
      扩展当前 session

3. process_name 变化：
      关闭旧 session，close_reason = ProcessChanged
      开启新 session

4. Pause / Stop：
      关闭当前 session
      close_reason = Paused / Stopped
```

这一步要和你已有 `AgentStateMachine` 结合。

---

# 第 6 步：Worker 中接入 Mock 数据链

Agent 当前应该已经在做：

```text
heartbeat
读取 agent_control
状态切换
```

下一步在 `Running` 状态里加：

```text
每个采样周期：
    sample = MockForegroundSampleProvider.Capture()
    写 foreground_samples
    SessionAggregator.HandleSample(sample)
    更新 runtime_state.LastSampleUtc
```

在 `Paused` 状态里：

```text
不 Capture
不写 foreground_samples
只写 heartbeat
```

在 `Stopping` 时：

```text
CloseOpenSessionAsync("Stopped")
```

---

# 第 7 步：WPF 读取 Dashboard

在 App 里新增：

```text
src/QuantifiedSelf.Windows.App/Services/OverviewDataService.cs
```

它调用 Infrastructure 的 `OverviewQueryService`。

主窗口先不要做复杂页面，直接在顶部或下方增加几个 TextBlock：

```text
今日总时长
今日 active
今日 idle
最近会话
应用排行
```

先显示出来即可。

---

# 第 8 步：跑通验收

这一阶段验收标准：

```text
1. dotnet build 0 error
2. 启动 WPF
3. 点击 Start Agent
4. 等 10-20 秒
5. 今日总时长开始增长
6. Top Apps 有 Mock 应用
7. 最近会话有数据
8. 点击 Pause 后，LastHeartbeat 继续更新，但今日时长不再明显增长
9. 点击 Resume 后，继续增长
10. 点击 Stop 后，当前 session close_reason = Stopped
```

这就是一个非常扎实的 MVP 数据闭环。

---

# 不建议下一步做 IPC

虽然最终架构里 IPC 是主通道，但现在不急。

你现在已经有：

```text
agent_control.json fallback
runtime_state.json
health_state.json
```

这足够支撑 MVP 验证。IPC 可以放到：

```text
SQLite + Mock sample + Dashboard 跑通之后
```

否则你会同时面对：

```text
IPC
SQLite
采样
统计
UI
```

变量太多。

---

# 不建议下一步直接做真实 Win32

真实 Win32 采样建议放在 SQLite 链路之后。

顺序应该是：

```text
MockForegroundSampleProvider
    ↓
IForegroundSampleProvider
    ↓
Win32ForegroundSampleProvider
```

也就是先定义接口：

```csharp
public interface IForegroundSampleProvider
{
    ForegroundSample Capture();
}
```

第一版用 Mock 实现。
后面换成 Win32 实现，不影响数据库、统计、Dashboard。

---

# 你现在的下一步任务清单

建议你按这个顺序做：

```text
1. 先重新 dotnet build 验证当前状态
2. 新增 Core.Models：ForegroundSample / AppSession / DashboardSummary
3. 新增 SQLite 初始化器
4. 新增 ForegroundSampleRepository
5. 新增 AppSessionRepository
6. 新增 MockForegroundSampleProvider
7. 新增 SessionAggregator
8. Worker Running 状态接入 Mock sample 写库
9. WPF OverviewDataService 读取今日统计
10. MainWindow 显示今日 summary
```

---

# 总结

你现在已经完成了：

```text
控制闭环
```

下一步最合理的是完成：

```text
数据闭环
```

也就是：

```text
Agent 产生数据
    ↓
SQLite 落库
    ↓
session 合并
    ↓
WPF 查询
    ↓
Dashboard 展示
```

等这个闭环跑通，再接真实 Win32 采样。这样重构会非常稳。
